# 项目二 · 单元关系 lab（Lab.13 起的一组）

> 立项：用户 2026-09-03「我们先命名一个环形的压缩表皮形成平台的结构叫做一个单元，
> 我们这个 lab 或者说接下来要做的一组 lab、就来研究几个单元比如两个或者三个四个九个
> 单元之间可形成的关系」。
> 本文件 = 单元的定义 · 关系维度的可行性审计 · 第一台（Lab.13）的落点 · 下一轮候选与待拍板。
> 数字以 `src/lib/space/unit-cluster.ts` 与其守门为准，这里不复制成第二份真理。

## 0. 一句话

一个单元 = Lab.10 那种圆筒环；单元之间不相互作用（引擎是单剖面的），所以「关系」全部是
**编排**——把单元已有的旋钮（形态 / 半径 / 高度 / 蒙皮）在几个单元之间拉开差别，再加一个
单元自己没有的量：**距离**。第一台做了现成的三维（距离 × 高度 × 形态），2 / 3 / 4 / 9 个单元
各一种排法，关系五档；**同日第二轮（用户拍板「按你的想法做」）加了时序（同步 / 错相）与
连接（相切处的织物网）**，异径留给下一轮，嵌套明确做不了。

## 1. 什么是「一个单元」

| 项 | 值 | 出处 |
|---|---|---|
| 构造 | 20 条 202 节窄带绕竖立杆一圈，站位半径 R（滑块 26–90，默认 30），同一张键谱、同一收缩协议 | skin-ring `buildRingUnits`（Lab.10 / Lab.12 同一份，本 lab 一个数不改） |
| 形态 | 目录四键谱：袋 / 蘑菇挑台 / 直挑台 / 阶梯挑台方箱（默认方箱） | skin-data |
| 高度 | `lead` = 形状在带上的位置（2 px/节）；起伏编制验过「形状逐点不变」的量程 14–58 节 ⇒ 平台可抬高 88 px = **0.35 m** | skin-ring `RING_WAVE` |
| 尺度 | 装置 ×0.5 摆进房间，下缘钉在离地 1.08 m；1 世界单位 ≈ 7.9 mm | skin-grid `RIG_*`、`MM_PER_UNIT` |

四种形态的实测量（2D px；守门 `unit-cluster.test` 跑引擎逐一核对）：

| 形态 | 终态挑出 | 全程挑出峰值 | 平台面离下缘 | 折叠体高（终态） | 折叠体高（成形期峰值） |
|---|---|---|---|---|---|
| 袋 | 63.4 | 63.4 | 113.9 | 62.4 | 90.7 |
| 蘑菇挑台 | 99.5 | 99.5 | 67.5 | 33.7 | 36.1 |
| 直挑台 | 101.0 | 101.0 | 67.6 | 26.6 | 30.4 |
| 阶梯方箱 | 82.2 | 82.2 | 67.5 | 68.1 | 77.0 |

两条值得记住：这一族的**挑出峰值 = 终态**（没有回缩，与 Lab.10 时期「阶梯在 step 519 鼓到 43.5
再收回」那条老记录已不同——那是放大 2.1× 之前的谱）；但**折叠体在成形期会鼓得比终态高**
（方箱 77 vs 68，在 step 449），交叠的高差要按它算。默认形态下平台 ⌀0.89 m、平台面离地 1.35 m。

## 2. 关系维度：先列全，再说哪些现成

| 维度 | 取值 | 靠什么 | 状态 |
|---|---|---|---|
| 距离 | 离 / 切 / 叠 / 嵌 | 站位间距 vs 平台外缘（§3） | 离·切·叠现成；**嵌不可**（平台伸进邻居的芯带，要碰撞，引擎没有） |
| 高度 | 齐平 / 错层 | `lead`（形状不变只挪高度） | 现成（一圈起伏那套） |
| 形态 | 同形 / 异形 | 每单元一份编制 | 现成（Lab.12 每行一种的机制）；第一台先只做同形 |
| 尺寸 | 等径 / 异径 | 每单元一个半径 | **要台架扩展**：半径烘进顶点，多半径 = 多份几何；现在一个滑块管全场 |
| 时序 | 同步 / 错相 | 每单元一个起步延迟 | **第二轮已做**（§10）：台架加法式 `delay`，引擎不动 |
| 连接 | 平台之间的膜 | 跨单元织物网 | **第二轮已做**（§11）：`bridgeWeb`，只在相切·齐平下 |

线稿 = `scripts/unit-cluster/atlas.mjs`（unit / dims / matrix 三张），几何整套查站上模块。

## 3. 距离四态是量出来的，不是分的

两个单元芯心距 d，平台外缘 ρ = R + 挑出峰值，芯带在半径 R：

- **离** d ≥ 2ρ + 缝：Lab.12 那种独立，格距直接用 `ringCellPitch`（R=30 ⇒ 312.3 px = 1.23 m）
- **切** d = 2ρ：两圈平台边贴边（方箱 224.4 px = 0.89 m），读成一整片
- **叠** 2R + 挑出 < d < 2ρ：平台在平面上盖过去——只有两个单元**不同高**时成立（一个从另一个
  底下穿过）。取 d = 2R + 挑出 + 6（平台外缘离邻居芯带留 6 px 净距）⇒ 方箱 148.2 px = 0.59 m，
  盖过去 0.30 m
- **嵌** d ≤ 2R + 挑出：平台伸进邻居芯带，范围外

间距按**每一对**单元各自的要求取最紧（`clusterPitch`）：站位按「相邻 = 1」归一，
p = max(需要的距离 / 归一距离)。分离与相切下相邻对就是最紧的；交叠下要多看**同高的对角对**
（方阵 / 九宫棋盘的对角同级、归一距 √2，只许贴边）——这一条把方阵与九宫的交叠间距从 148.2
推到 158.7 px（0.63 m），盖过去的宽度从 0.30 降到 0.26 m。守门卡「每对都满足、最紧的一对恰好贴着」。

## 4. 高度：级数由编制定，形状不动

- 高度级从低到高等分 lead 量程 58 → 14（`clusterLeads`），lead + tail 恒定 ⇒ 筒的上下缘不动、
  形状纯平移（skin-ring 起伏编制的守门就是这条）。齐平 = 一级 = RING_LEAD 本身 ⇒ 与 Lab.10/12 的环逐位相同。
- 错层的级数：一对 2 级（Δ 0.35 m）· 三角 3 级（0.17 m ≈ 一级台阶）· **方阵对角 2 级**（0.35 m，
  两张高桌配两张矮凳）· 九宫按离中心的圈数 角 0 / 边 1 / 心 2（0.17 m，中心最高，一座台地）。
  方阵首版是四级顺着绕（0.11 m，读作一段旋转的台阶），用户 2026-09-03 拍板改两级：0.11 m 的高差
  在真机上读不出「级」，而 0.35 m 一眼就是两层。
- 交叠的级数：**棋盘两级**（一对 / 方阵 / 九宫）——任何相邻对都不同高、同高只在对角或隔一格；
  三角不是二分图，只能三级，每级高差只剩一半。
- lead 是整数节点：四级等分 44 节取整成 15/14/15，`clusterLevelStep` 取最小那对（保守），HUD 说「每级 ≥」。

## 5. 交叠的物理门槛

下层的平台从上层的平台底下穿过去，成形期最鼓的那一刻也不许碰 ⇒ 相邻两级高差 ≥ 折叠体成形期
竖向跨度 + 6 px（`overlapFeasible` / `overlapMargin`）：

| | 袋（90.7） | 蘑菇（36.1） | 直挑台（30.4） | 方箱（77.0） |
|---|---|---|---|---|
| 一对 / 方阵 / 九宫（两级，高差 88） | ✗ | ✓ | ✓ | ✓（余量 5） |
| 三角（三级，高差 44） | ✗ | ✓ | ✓ | ✗ |

装不下的组合按钮变灰、留位不隐藏（§18.3 的规矩），当前若正停在交叠上则退到「相切·错层」
（同一组高度，只是不盖过去）。这五个组合仍在差分清单里——是「有这一档但这一档不成立」，不是丢了。
判据是保守的（用整段成形期的竖向包络），袋在两级下只差 2.7 px；要放行得按时刻逐对做真正的相交测试，
不在这一轮。

## 6. 第一台的落地（Lab.13）

- 模块 `src/lib/space/unit-cluster.ts`（纯几何）：单元实测量表 · 四种编制的站位 · 五种关系 · 级表 ·
  间距推导 · 可行性 · 房间/小人/取景（走 skin-grid 新抽出的 `roomBoxesBySpan / figureSpotBySpan /
  viewFitBySpan / sceneBySpan`，Lab.12 的四个函数委托到它们 ⇒ 逐位不变）· HUD 读数。守门 9 项。
- 台架 `components/lab/SkinClusterBench.tsx`：SkinSolidBench 薄壳，与 Lab.12 同一条阵列路径
  （cells + ringPlans + rig + scene + camScaleFor）。几级高度就解几条引擎、每格指向自己那一级。
- **换关系只重摆不重解**：SkinSolidBench 加法式 prop `cellsKey`（默认 undefined ⇒ Lab.07–12 逐位不变）
  → `reflowCells()`：先作废布景缓存（它只按半径缓存，而房间随簇的占宽变），再重读 cells、相机重取景。
  `unitsKey` 只跟形态与级数走——一对错层与方阵交叠都是两级 ⇒ 同一组引擎，换过去不倒带。
- /lab 第五段 Ⅴ Between units（尺度主轴到「一间房」封顶，Ⅴ 开第二条轴：单元**之间**），编号顺延 13
  不重排；主页第 13 卡、网格 13 列、Demos 13；差分清单 +80（4 编制 × 4 形态 × 5 关系），
  `/lab#lab13-<plan>` 直达。
- 默认档：**一对 · 交叠·错层 · 方箱**（用户拍板：这台的题目是「关系」，开场就该给一个 Lab.12 里没有的
  关系；首版是相切·齐平）。**房间固定用 Lab.12 那一间**（同半径下地板与两面墙逐位相同，守门卡着）——
  首版房间随簇的占宽变，切编制时墙跟着缩放，会让人误以为单元也变了大小；取景仍按簇 + 一圈留距推进，
  墙允许出画，小人仍站簇的近侧角外。

默认形态下各格的读数（R=30）：

| | 分离·齐平 | 相切·齐平 | 分离·错层 | 相切·错层 | 交叠·错层 |
|---|---|---|---|---|---|
| 一对 | d 1.23 m，空地 0.35 | d 0.89 m | 2 级，Δ 0.35 | 2 级 | d 0.59 m，盖 0.30，Δ 0.35 |
| 三角 | 同上 | 同上 | 3 级，Δ 0.17 | 3 级 | d 0.59 m，盖 0.30，3 级 Δ 0.17（方箱不可 → 蘑菇/直挑台） |
| 方阵 | 同上 | 同上 | 对角 2 级，Δ 0.35 | 对角 2 级 | d 0.63 m，盖 0.26，棋盘 2 级 Δ 0.35 |
| 九宫 | 同上 | 同上 | 3 级心高，Δ 0.17 | 3 级 | d 0.63 m，盖 0.26，棋盘 2 级 Δ 0.35 |

## 10. 第二轮 · 错相（用户拍板「先做错相」）

- 机制：`SolidUnitDef.delay`（协议步）——SkinSolidBench 的步进循环加一个全场时钟 `tick`，每条引擎
  只在 `tick > delay` 后才推进；「跑完」按全场最后一条算（最晚起步的还没到头，别人不许先重播），
  重播全体归零。默认 0 ⇒ Lab.07–12 逐位不变。引擎一行不动。
- 波序（`clusterWaves`）：一对 / 三角 / 方阵按站位顺序绕一圈；九宫按 行 + 列 分五波——一道波从一个角
  推到对角。相邻两波间隔 `STAGGER_STEPS = 150`（收缩全程 900 步的六分之一：看得出先后，又不至于前一波
  已经定形）。九宫最后一波比第一波晚 600 步，全程 2100 步。
- **错相下每格一条引擎**（同一条引擎摆多处的单元不可能各有各的相位）：一对 2 · 三角 3 · 方阵 4 ·
  九宫 9 条，仍在 Lab.10 渐变（11 条）的量级内，速率 80。同步下仍是几级解几条。
- 控制条第一层加「时序」轴（同步 / 错相），差分清单 80 → 160。`unitsKey` 含时序与（错相下的）编制
  ——引擎数一变就重建。

## 11. 第二轮 · 连接（用户拍板「再做连接」）

- 只在**相切·齐平**下：相邻（归一距 1）的每一对单元之间搭一块织物网（`clusterBridges` 给对子，
  `skin-solid.bridgeWeb` 给几何）。错层的相切不糊——那道缝是两级之间的落差，糊上是一道斜坡；
  分离与交叠没有「缝」可糊。
- 几何：两个相切的圆在切点两侧张开一对楔形缝。沿切点两侧各取 8 个方位角 φ，A 圈外缘 θ+φ 处与 B 圈
  外缘 θ+π−φ 处（关于连心线中垂面互为镜像）拉成一格——顶面一层（两圈的走面连成一片）、底面一层、
  外端一块竖直封板。φ 取到「外缘弦宽 = 40 px（16 cm）」为止：再往外缝已经宽过几条带深，糊上就是
  一大片平板而不是圆角。
- 外缘半径与顶/底高度每帧从引擎现读（外缘 = 站位半径 + 最大挑出；顶/底 = 离轴 ≥ 85% 最大挑出那些
  节点的 y 范围），所以网跟着平台长。**不是随时都在**：两圈外缘差 ≤ 14 px 才搭——收缩前两圈外缘
  隔着老远，那时的网会是一张横在两根立杆之间的悬空布；网在挑出到位前的最后十几像素里长出来。
- 半透明面，随蒙皮滑块同一个透明度；在实体与环内膜之后画，按每对的中点视深从远到近。几何按世界坐标
  逐对烘（每对不同，不走摆放表），一对 68 三角、九宫 12 对，开销可忽略。

## 7. 局限

- 单元之间不相互作用（Lab.09 起的局限在这里更要紧——交叠是靠几何门槛保证不碰，不是碰撞求解）。
- 房间随簇的占宽变（小簇小房），与 Lab.12 一样只有地板和两面墙。
- 三角与方阵只做了一种排法（等边 / 2×2）；一线排三个、四个，八围一的九个，都还没排。
- 「异形」（一簇里几种形态）机制上现成（Lab.12 每行一种），还没开这个按钮——关系 5 × 时序 2 × 编制 4
  × 形态 4 已 160 个差分。
- 连接只糊相切·齐平；错层相切处的落差没做斜坡。网的宽 40 px / 起网门槛 14 px 是手感常量。

## 8. 下一轮候选（按改动量排）

1. **异形簇**：每格一个形态——零机制，只加编制。
2. **异径**：每单元一个半径——台架要为每个半径各填一份顶点；小簇（≤9）开销可接受。
3. **一线 / 八围一**：新排法只是站位表，`clusterPositions` 加分支即可。
4. **错层相切处的斜坡**：连接的第二种读法（两级之间的落差用布接上）。

## 9. 拍板记录（2026-09-03，用户「按你的想法做吧」）

| 问题 | 定案 | 理由 |
|---|---|---|
| 默认档 | 一对 · 交叠·错层 · 方箱 | 开场给一个 Lab.12 里没有的关系 |
| 错层读法 | 九宫中心最高不变；方阵改对角两级 | 0.11 m 在真机读不出「级」，0.35 m 一眼两层 |
| 交叠净距与判据 | 不动（6 px / 6 px，按成形期包络） | 袋被判掉不影响主线；精确相交测试不在这一轮 |
| 房间 | 固定用 Lab.12 那间 | 背景不该跟着主体呼吸；一对时靠取景按簇推进补 |
| 下一维 | 先错相、再连接（本轮已做，§10–11） | 时间轴 + 相切成片，从「摆法研究」变「空间提案」 |

待真机拍板的手感常量：波间隔 150 步 · 网宽 40 px · 起网门槛 14 px · 一对的默认取景。
