# 项目二 · 收缩张紧外皮单元 Lab（Lab.06–09）

> 项目二（双层多智能体空间仿真，尚未定名）的第一台站上台架。
> 依据 = 用户交接件（原样存档于 `项目二参考/`）：`HANDOFF_claude_code.md`（项目定位、
> 结构系统定义、键谱语法、冻结决定）+ `skin_sim_v7_final.py`（唯一最终引擎）。
> 立项 2026-08-18，用户一句话开工：「这个是我项目二的第一个lab」。

## 0. 一句话

天花垂下的软皮条带，芯收缩挤出富余材料，皮上预埋的吸附键位图决定材料扣合成什么——
四种键谱在**同一收缩协议**下分别成形为袋 / 蘑菇挑台 / 直挑台 / 阶梯挑台；
键锁定永久 = 滞回的物理来源。物理阶段已在 Python 侧收口（交接件「冻结决定」七条），
本仓库做的是**语言搬运与上站，不做内核改进**。

## 1. 移植（src/lib/space/，站上第三个内核）

- `skin-unit.ts`：v7 引擎的 1:1 移植。纯数学、零依赖、零 DOM，与两个连杆内核
  （`src/lib/linkage` 2D/3D）平行、互不引用。批量 API 改成步进式
  （`SkinUnit.advance()` 推进一个协议步），供台架逐帧驱动。
- `skin-data.ts`：四单元键谱，逐字取自 v7 `UNITS`（含目录顺序与双语题名）。
- **移植要点（错一处即与参考漂移，写进了文件头注释）**：
  - numpy 切片批量更新 = **Jacobi 快照语义**（corr 全从更新前位置算出，再先整段 +=、
    后整段 -=）；逐点边算边改（Gauss–Seidel）是另一套动力学。
  - 每步迭代内约束顺序不可乱：钉贴合段 → 拉伸 → 多尺度抗弯 → 锁定键+梯挡垂直化 →
    层直化/找平 → 面板 → **拉链判定（迭代末尾）** → 重钉贴合段。
  - `np.linalg.norm` 的逐元素语义 = `Math.sqrt(dx²+dy²)`，**不是 `Math.hypot`**
    （hypot 又慢、舍入路径又不同——首版用 hypot，全程 16.6s；换 sqrt + 锁定键链内
    清单改增量维护后 5.2s，位置对照结果不变）。
- **性能账**：单元一步 ≈0.87ms（node 实测）。台架四单元同时推进，RATE=110 步/s、
  每帧封顶 3 步——慢设备表现为放慢而非轨迹漂移（定步纪律，同 Lab.02/04 取向）。

## 2. 对照验证（守门 = skin-unit.test.ts，23 项）

- 基准 `skin-ref.json` 由 `scripts/skin-ref/dump_skin_ref.py` 生成——物理部分
  **逐字取自 v7**（只加检查点记录），四单元在 step {0,100,300,600,900,1000,1200,1499}
  处存全部节点位置（9 位小数）+ 锁定键完整追加序列。
- 实测：四单元全程位置最大偏差 ≈**5e-10 = 基准自身的舍入量级**（即移植与 numpy 在
  基准精度内逐位一致）；**锁定键序列四单元全部精确匹配**（含 rb 与追加序）。
  测试容差 2e-9。
- 另有结构性守门：build 一致（节点/贴合段/链序/面板）、step≤950 锁定按跨度降序
  （拉链纪律）、终态贴合段钉死 + 锁定键保持键长（滞回）、done 后 advance no-op、
  四形态终点互异、renderSmooth 不碰物理数据。

## 3. 台架呈现（components/lab/SkinBench.tsx，/lab 第六台）

- 四单元并排一幅 SVG（照 v7 目录图的窗口 xlim/ylim 与条纹画法），暗色适配：
  条纹双色 = 原稿 GREEN/PALE 映射到 token；**键线保留原稿 BOND 橙**（提亮版
  `#e06a3e`）——证据色，滞回图同源，有意不并入站内绿/紫内核编码。
- render_smooth（w=3 移动平均）照移植，**只用于绘图**；残影两帧（step 500/1000，
  = 目录图 ghosts）路过时快照。
- 协议跑完（1500 步）静置 3.2s 自动重播——每次重播是一次全新收缩（键锁定不可逆）。
  控制条：运转 / 键线 / 重播 / 速度（0.5–2×，播放倍率不是物理量）。
- 无拖拽交互：v7 引擎没有交互输入，不代设计。reduced-motion 不自动播
  （点「运转」= 显式意愿）。
- /lab 页从此按项目分组（ProjectRule 分组头）：Project I = Lab.01–05（两个连杆内核），
  Project II = Lab.06。页头「six live instruments」、页脚「06 instruments · 03 kernels」。
  项目二分组头措辞与 log 页 PROJECT_GROUPS 一致（「Spatial simulation」，描述而非
  标题——项目定名后一并改）。
- 主页 S2 第六张卡，网格 5→6 列（顺带结清「小屏两列 5 张有一张单行」的待优化项）。

## 4. 范围外（交接件明令或顺延，不要替用户做）

- **暂缓项**（交接件原文）：可崩键、tunnel 形态、锁定微皱的物理级根除。
- **下一阶段**（阵列化进房间剖面、交叠带标注、行为矩阵→键谱翻译规则、阵列版滞回）：
  翻译规则**由用户手写，模型不代拟**（协作规则原文）。
- 滞回对照实验（v5 脚本 + hysteresis_loop.png 证据图）未随最终引擎交来，站上
  Lab.06 只跑 v7 收缩协议；滞回在台架里以「锁定不回弹 + 重播即新生」呈现，
  完整去-回程曲线等用户交 v5 或阵列版再说。
- 手感常量待用户真机拍板：RATE 110、REPLAY_HOLD 3.2s、速度滑块量程、键线默认开。

## 5. 站方修正：根部贴轴（2026-08-18，用户看真机拍板）

- **事故**：真机上四单元的根部（键谱围合区之外的缓冲料）被形态拖成离轴的斜漏斗——
  用户红圈圈出，配手绘 P2：「没被键拉起的地方就还是贴着最开始的轴」。
  排查确认这不是移植 bug——v7 本身就是这个几何（对照逐位一致），是设计层修订。
- **机理**：形态的嘴（最外键对）没有任何东西把它锚在芯上，整个形体外漂，
  把缓冲料拉成斜线。v7 是纯 2D 剖面，也没建「芯是实心轴、皮不可穿透」这条物理事实
  （实测 v7 全程 minX ≈ −0.005…−0.012，轻微越轴）。
- **落地**（`SkinUnitOpts`，两项都默认关 ⇒ Python 对照守门跑的仍是 v7 逐字路径）：
  `coreWall`（x≥0 单侧墙，只作用自由节点，迭代末施加）+ `rootHug`（围合区之外的
  自由节点向 x=0 贴靠）。台架用定案 `SKIN_ROOT_FIX = { coreWall, rootHug: 1.0 }`。
- **调参实录**：0.08 完全无效——拉伸约束每迭代跑 55 次，把缓冲又拽回斜线；
  0.3 仍剩小漏斗；**1.0（硬贴轴）才是答案**，形态直接从轴上长出，与手绘一致。
  几何自洽账：缓冲弧长 + 键长 ≈ 收缩后围合跨度（蘑菇 12+10+14≈37px），
  所以硬贴轴后几乎零富余、根部走直线；袋是唯一超预算的（rb 0.30 > 可用跨度），
  嘴会带一点倾斜——物理决定，不强掰。
- **验证**：四单元锁定数与集合在修正下与 v7 逐一相同（2/9/11/11，拉链不受影响）；
  step 400/800/1499 三相位渲染全程贴轴无漏斗。守门 +2 项（修正路径贴轴不穿芯键照锁 /
  默认路径确实不带修正），共 25 项。

## 6. 站方调形二轮：袋收浅 + 梯身去锯齿（2026-08-19，用户看真机拍板）

- **袋收缩终点 r₁=0.66**（用户：「第一个收缩到图1的程度就是最好的」，图1 = 原协议
  step≈400 帧，彼时 r=0.661）。系统本就是「每单元一个收缩自由度 ℓ」（交接件），
  目录图为对比才共用终点；站上给袋独立终点是把这个自由度用起来，不是改协议——
  **时间表不变（step 900 走完、四台同步呼吸），只改深度**。实验对照 0.72/0.66/0.60
  三档，0.66 与参照帧几乎重合。袋照旧锁 2 键（只是锁得晚些）。
  落地 `SkinUnitOpts.r1`（默认 SKIN.R1 ⇒ 对照守门不受影响）+ `SkinUnitDef.r1`。
- **蘑菇/直挑台梯身锯齿**（用户：「收起来之后有点不平滑,现在都是锯齿感」）：
  即交接件暂缓项「锁定微皱」，明令**由渲染平滑覆盖**、物理级根除暂缓——故走渲染层：
  ① 这两台的绘图平滑从 v7 默认 w3·1 遍加强到 **w5·2 遍**（`SkinUnitDef.smooth`，
  四档对照后取最平的一档；袋/阶梯保持 w3——阶梯的方角不能被抹圆）；
  ② SVG 条纹折线补 `stroke-linejoin: round`（miter 会把微皱折点放大成毛刺，
  这占锯齿感的一半）。物理数据零改动。
- 台架与守门共用 `skinSiteOpts()`（预览与线上同一段代码的老纪律）；HUD 的 r 读数
  改取全深单元（阶梯）——拿袋当读数会在张紧阶段挂着 0.66 误读成没收完。

## 7. 站方调形三轮：阶梯挑台方箱（2026-08-19，用户两条拍板）

- **诉求**：「最终的形态应该是一个方形」+「一方面是比例问题（长宽），一方面是
  右端的弧度——方形就需要右端没有弧度」。
- **诊断（逐节点量出，不是猜）**：终态「不方」有四个来源——
  ① 箱体浮在轴外 8px（嘴角没锚在轴上，手绘里箱体内面就是轴）；
  ② 右端面外鼓 4px：**找平每迭代把两个端角拽向顶/底面，把角旁的段抻长到
  2.64px（超伸 32%）→ 面弧 > 弦 ⇒ 必然外鼓**；
  ③ 底面波动 6.5px：v7 只找平上层台面（猫站的面），底面只分段直化不找平；
  ④ 比例是键谱编码的：v7 键谱 = 宽 40 × 高 24（rb=0.24）的扁条。
- **落地一：`SkinUnitOpts.boxSquare`**（默认关 ⇒ 对照守门不动；仍全部用 v7 自己的
  约束词汇）：嘴角贴轴（最外键对 x=0）+ 底面找平（对称于 v7 台面找平）+
  端角键距迭代末重申 + **端面拉直**——最内键锁定后把面内节点向「两角连线等分点」
  投影（段长重申力道追不上找平的抻，试过无效；这是 v7 面板注释「端面弧长=键长 →
  必然拉直」的逻辑终点）。
- **落地二：键谱改近方比例**（用户明说比例是问题的一半，故动键谱；等长键纪律不破）：
  rb 0.24→**0.32**、k 从 8 起（10 颗键），端面弧长 16 节 = 键长 ✓，实测箱体
  43×32（4:3；原 46×24 = 2:1）。**rb 有几何上限**：嘴要贴轴 ⇒ rb < 收缩后 f 跨度
  39.6px；0.40 放不下（箱体会被顶离轴），0.36 会把 22px 缓冲挤进 3.6px（死疙瘩），
  0.32 是干净的极限档。**v7 原键谱在 `SKIN_V7_SPECS` 原样保留**——对照基准跑的
  永远是它（基准动了 = 对照失去意义）；展示键谱与基准从此允许分叉。
- **验证**：四角 (0,-103.6)/(43.1,-103.5)/(43.0,-135.5)/(0,-135.6)，面斜率 ≤0.1px、
  端面波动 1.4px、键长 31.9–32.0 全等、10 颗键全锁。顶底面 ±2.3px 起伏是键间富余
  的织物质感，有意保留（手绘的排线即此）。守门 +1（方箱四性质），305 测试绿。

## 8. 站方调形四轮：抖动 + 平直度（2026-08-19，用户「还是在抖动，不够平直」）

- **抖动的真相**：终态（step 950+）逐步位移实测 **0.00px**——抖的全是**成形期**
  （300–600 步最高 7.2px/步，约束互相拉扯、皱纹在爬）。v7 作者自己就没让人看裸帧：
  **GIF 用帧间指数平滑（0.55/0.45）盖的**（交接件「GIF 帧间指数平滑」原文）。
  台架活体渲染此前少了这层——补上同款：每帧按实际推进步数折算等效系数
  （keep = 0.45^(n/20)，n=20 步时恰为 GIF 的 0.45），物理零改、重播时重置。
  快慢设备观感一致（系数按步数不按帧数）。
- **平直度**：顶/底面 ±2.3px 键间起伏此前当织物质感保留，被否。boxSquare 补一手
  「面压平」：端角键锁定后，把顶/底面内部节点向「嘴角→端角连线」投影
  （**只压横向偏差**——垂足投影，沿线分布仍归拉伸/直化管）。实测顶面波动
  4.7→2.0px、底面 4.6→1.9px、键长 32.0 全等。
- 守门在方箱测试里加「顶/底面 y 波动」断言（不加测试数）。305 绿。
- **同日追打（用户「根部有一段错误的收紧」）**：0.5 混合的面压平不彻底——键间材料
  把面中段顶出 ~2px，而嘴角被键距钉在 rb=32，**面比嘴宽 ⇒ 靠嘴几节被段长约束拽成
  向内的斜坡**，读作根部脖颈。面压平与端面投影一律升为**硬投影**（迭代末面节点
  直接落在两角连线/等分点上）：四面波动全部归 0.0px、42.2×32.0 精确矩形、根部
  平齐入轴。面守门阈值 2.5→0.5px。渲染层的 w3 平滑只在四角留 ~2px 小圆角
  （织物 + 3.4px 线宽本就无法无限锐角）。
- **再追打（用户「左侧不要收紧，做到 P2 那样」）**：残留的左侧收紧 = **缓冲富余的
  疙瘩**——下缓冲 14px 弧长只有 4px 轴距可用，硬贴轴后自由褶皱在嘴角背后拱成
  上下折返的一团（实测 node 110 反而比嘴角高 5.8px），渲染平滑再把它带出来。
  boxSquare 再加一手：**缓冲段在两端锚点（贴合端 ↔ 嘴角）之间沿轴均匀排布**
  （硬投影，与端面同款）——根部即 P2 的直线入角。守门加「缓冲贴轴 + 等距 + 单调」
  断言。台架版式同轮微修：画布 400→448、题名行抬离 HUD 带（实况截图发现叠字）。

## 9. Lab.07 · 立体带（2026-08-19，用户立项「也是这四个，给外表皮一些厚度（立体的），加入立体的视角，复用之前用过的」）

- **物理零新增**：四台仍是同一套 2D 剖面引擎（skinSiteOpts 全套站方修正），Lab.07
  只做立体呈现——v7 本就是 2D **剖面**模拟、单元在交接件里通篇叫「带子」
  （阵列阶段 = 房间剖面内多根带子），故立体化 = **剖面挤出成带**：
  带深 80 · 织物厚度 5（剖面法向各偏 2.5）· 前后剖口切面（看得见皮是有厚度的材料）
  + 两端封口。条纹双色照 2D 目录按 STRIPE 节交替（索引分 A/B 两组各一次绘制）。
- **几何烘焙 = `src/lib/space/skin-solid.ts`**（纯数学零依赖）：拓扑一次定死
  （buildSolidTopology）、顶点逐帧重填（fillSolidVerts，Y 翻转：世界 y 向下 =
  屏幕同向 ⇒ 正视 = 恒等视角）。守门 5 项（拓扑账目/条纹分摊/厚度偏移/真剖面无
  NaN/boxVerts）。
- **装备零改，全部复用**：gl3d FlatRenderer（drawDynamicMesh 动态网格 + drawLines
  键线 + addMesh 天花静件）+ camera3d OrbitCamera（拖拽轨道/右键平移/滚轮缩放）+
  视角预设四元数 slerp（RingsBench 同款；本台世界系 Y 向下 ⇒ 正视=恒等，预设
  轴测/正/侧/顶另配）。锁定键画成前后两剖口上的橙色梯挡（证据色同 2D）。
- 台架 `components/lab/SkinSolidBench.tsx`：定步推进 / 帧间 EMA / 自动重播 /
  运转·键线·透视·速度·视角控件，纪律与 Lab.06 逐条同款。芯轨为逐帧小盒
  （长度随收缩变）、天花板条为静件。
- /lab 第七台（Project II 组 Lab.06–07），主页第七卡（WebGL 家族紫），统计条
  305→**310**、demos 06→07（内核数不变——同一引擎，无第四内核）。
- 手感常量待用户真机拍板：带深 80 / 厚度 5 / 单元间距 175 / 机位（PIVOT/SCALE/
  轴测角）/ 预设视角的斜度。

## 10. Lab.08 · 阵列过渡（2026-08-19，用户立项「左边是第二个，右边是第四个，中间十个单元逐级微变，实现从 2 到 4 的过渡」）

- 交接件「下一步任务①：单元阵列化——多根带子、不同键谱」的第一口。
- **过渡从物理里长出来，不是形状插值**：十二条窄带（带深 26）每条都是独立的
  真引擎单元，形变差异全部来自键谱逐级微变（`src/lib/space/skin-array.ts`）——
  主驱动 = 键长 rb 0.10→0.32（v7「端面弧长=键长→必然拉直」：rb 逼近端面弧长时
  头部环自然摊平成方箱端面，这就是过渡的机理）；梯挡 9→10（t=0.5 处离散加一颗，
  离散键位图之间只能如此）；段落表（贴合段/自由段/键谱中心）四舍五入微调。
- **两个端点逐字 = 站上原谱**（守门对象同一断言）：左 = Lab.06 蘑菇挑台（全套
  站方修正），右 = 阶梯方箱（含 boxSquare）。中间十级纯 fan 键谱 + 根部修正，
  不加 panel/boxSquare——方正度在末端补完，读作渐变收尾而非断层。
- **台架整台复用 SkinSolidBench**（参数化：units/gapX/depth/pivot/camScale/
  ceiling/rate/hud，默认值 = Lab.07 现行为 ⇒ Lab.07 零回归）；阵列专属差异：
  间距 80 密排、通长天花（per-unit 板密排下共面重叠会 z-fight）、RATE 降 80
  （十二台同时推进，成形期单帧物理量是 Lab.07 的三倍）。
- 守门 4 项（skin-array.test.ts）：端点原谱同一 + 逐级纪律（等长键/缓冲≥4/rb 递增/
  梯挡不减）+ 相邻微小形变（rb 步长≤0.021、梯挡至多+1、段落表±1）+ 中间级物理
  冒烟（i=3/6/9 跑 1000 步键谱全锁、贴轴不穿芯）。demos 08。
- **边界申明**：交接件明令「行为矩阵→键谱的翻译规则由使用者手写」。本插值只是
  两张既有键谱之间的形态学串联（演示编排），不是那套翻译规则；正式规则交来后
  skin-array 随时按其替换。
- **并拢排列（同日追加，用户「把他们并一起看效果」）**：12 条带沿深度密排成
  一个剖面渐变的连续体（默认），与原来的沿 X 分列做成「排列」切换：
  - 并拢 = gapX 0 / gapZ 27.5（**略大于带深 26**——贴平会让相邻剖口共面 z-fight，
    1.5 的缝读作织物带的分节，不是断层）；分列 = gapX 80 / gapZ 0（原排布）。
  - **切换只改渲染偏移与机位，引擎不重建**——两种排列看的是同一次收缩（CDP 实测
    切换前后 step 连续推进不归零）。三件加法式扩展支撑：`OrbitCamera.retarget`
    （换枢轴/基础缩放，姿态/zoom/pan 不动 ⇒ 机位语义连续）、`fillSolidVerts`
    尾参 `offZ`（默认 0 = 旧行为，既有按位调用零改）、SkinSolidBench `layouts`
    prop（≥2 出「排列」段控件；省略 = 单排布 ⇒ Lab.07 零回归）。
  - Z 排向取「单元 0 在 +Z（轴测近端）」——分列的「左」= 并拢的「近」，
    HUD hint 与规格表口径一致（近端蘑菇、远端方箱）。
  - 通长天花每排布各烘一条（X/Z 范围随排布变），绘制取当前排布那条；芯轨与
    键线梯挡随 offZ 走。守门 +2（retarget 不动姿态/zoom/pan；offZ 纯 z 平移、
    XY 逐位不变），统计条 314→**316**。
- **过渡系列 v2（2026-08-20，用户否决首版「转变根本不平滑」并立工作纪律：先出
  纯线稿形态系列 → 确认形态 → 才上 3D）**：
  - **诊断**（线稿工具 = 每键谱跑完整 1500 步真引擎、等弧长重采样量相邻形态
    距离）：首版 1–10 号全是缓慢变胖的圆头肉瘤、11 号硬切方箱——方箱的「方」
    来自 panel + boxSquare 而非 rb，首版中间级两样全无，断层 = 其他步的 7 倍
    （5.17 vs 均值 0.7–1.5）。
  - **引擎两件加法式扩展**（v7 对照路径零涉及，true=1 逐位不变）：
    ① `boxSquare` 接受 (0,1] 数字强度——贴轴/底面找平/端角重申/顶底压平/缓冲
    排布按强度混入；② 端面硬投影改**弦弧比几何门**（0.85→1.00 smoothstep ×
    强度）——它的前提是弦≈弧，rb 明显小于端面弧长时硬压 = 把富余材料逐迭代
    挤出端面，实验版 candC 全程硬压直接崩（相邻距离 11+，已入引擎注释与守门）。
  - **三个假杠杆/暗坑**（七版迭代实测）：panel 半跨渐入近乎无感（±5/6/7 相邻
    差 0.13–0.2），只有到 ±8（=最内键跨）那一格才真起作用 ⇒ 提前在低强度段走完；
    lead 贴合段 1 节取整跳变单步贡献 ≈ 一整格形变 ⇒ 手排时间表、零跳变步对准
    别的杠杆最重的格；生成器脚本残留 `boxSquare = true` 曾把全部渐变强度写死
    成 1（candB/candD 逐行相同才暴露）——**排查依据是数字完全相同，不是猜**。
  - **定版时间表**（skin-array.ts v2，用户线稿拍板）：rb 0.10→0.32 恒步 0.02；
    sq 0→0.05→…→0.85→1；PW 0,0,0,2,4,6,8…8；lead 58,57,57,56,55,54,53,53,52,
    52,51,50。实测相邻形态距离 12 步全落 0.69–1.76（均值 1.30、最大/最小 2.6×，
    对比首版 7.2×）；绘图平滑 sq≥0.5 才降 [3,1]（低强度段梯身微皱仍要 [5,2] 盖）。
  - 守门 +3：方化渐入纪律（sq 单调、步长 ≤0.15、中间级 <1、panel 半跨单调至 8、
    lead 单调不增）+ 强度真实分级（sq 0/0.5/1 垂度单调收平、true≡1）+ 几何门
    回归（rb 0.22 全程与无 box 基线同量级、端面保留鼓弧）。统计条 316→**319**。
- **并拢改切片陈列（2026-08-20，用户「居中对齐 + 每一个宽度 0.6 倍」）**：
  带深 26→15.6（间距 27.5 不动 ⇒ 片间留缝、重新读作 12 片）；排布新增
  `center`——逐帧把每片折叠体（自由段节点）的中线对齐到全员均值，**纯渲染
  纵移**（`fillSolidVerts` 尾参 offY，默认 0 = 旧行为；形态/物理/lead 全不动
  ——lead 定过渡平滑度、端点 lead 又是原谱锁死，对位只能在渲染层做）+
  `frame:false` 收掉天花板条与芯轨（居中后带子各自纵移，板/轨对不上挂点）。
  分列排布原样吊挂。渲染改两遍（先算全员剖面求公共中线再画）；随收缩的整体
  升降照常发生，只有相互错位被对齐。
- **对位改物理（同日二轮纠偏，用户「上端要贴天花对齐，改形状在线上的位置来
  居中」+「间距也收 0.6 倍」）**：渲染纵移方案整个撤除（center/frame 旗标、
  offY 尾参全回退）——它让接天花的顶端跟着错位。**正解在键谱层**：lead
  （贴合段节数）就是形状在带上的位置，纯平移参数（重力均匀、约束全是相对量
  ⇒ 折叠形状严格不变），每节挪 SEG·100 = 2px 且与收缩进度无关（贴合段以
  未收缩间距钉轴，从 coreLen = 贴合节数·SEG + 键谱段·SEG·r 反推证实——
  首版按 SEG·R1 = 0.6px/节标定，方向反、量差 3.3 倍，实跑一轮即暴露）。
  标定结果：**全员 lead = 54 一个常数**即把折叠中线散布 14.79→1.22px
  （< 1 节粒度），顶端天然全部贴天花，天花板条与芯轨照常显示；「lead 手排
  错峰」时间表退役（位置分量被对位归零，相邻形态距离降到 0.31–1.42）。
  端点键谱段仍对象同一（守门改卡 spec[1]/spec[2] 同一 + lead = ARRAY_LEAD）；
  间距 27.5→16.5（0.6×，片间 0.9 缝防 z-fight）。守门升级：12 台全员跑到
  step 1200，卡键谱全锁 + 贴轴 + 顶端钉天花 + 中线散布 ≤2px（改形状后超差
  = 提示重标 ARRAY_LEAD）。
- **二次标定（同日，用户「居中对齐还是没齐」）**：查出判据取错了东西——上一轮
  量的是「自由段整段中线」，而自由段两端的缓冲料是**贴着轴、藏在竖带里看不见**
  的；眼睛对的是离轴那团（凸出体）。判据改为「离轴 >8px 的节点的上下缘中点」后，
  现行散布实为 2.95px（不是 1.22px）。重解整数最优 lead 表 [55×7,56×4,55]，
  散布 → **1.64px**，已到量子下界（1 节 lead = 2px，凸出体中线小数部分决定
  理论下界 1.72px）。另发现**帘子下缘参差 10px**（各带总长差 5 节 = lead+f+尾段
  之和不等），加尾段配平表使总长恒定；尾段是贴合段、逐迭代钉轴不对折叠体施力，
  纯外观量。守门同步改判据 + 加总长一致断言。
  **仍在的观感**：12 个形状高度本身 19→32px，居中就必然顶底各让一半（实测顶让
  6.06、底让 7.74），故画面读作「顶平底沉」。若要顶缘或底缘成一条直线，是另一个
  对齐目标（线稿对比见 scratch align-options），一句话可换。
- **「还是不齐」的真凶是视角（同日第三轮，用户再交截图）**：**决定性实验**——把
  12 条全换成**同一个形状**（同一单元复制 12 份），在同一机位下照样呈阶梯。
  ⇒ 画面上的斜，是**深度错位**不是形状没对齐：并拢排布把 12 片沿深度叠着，
  屏幕 y 里的深度系数 = −sin(俯仰)·cos(环绕)，相机只要有俯仰，远片就整体偏移
  （默认轴测机位 pitch −0.34 ⇒ 12 片跨 181 世界单位深度，错位达 49px，是对位
  残差 1.64px 的 30 倍）。**俯仰 = 0 的机位（正/侧）该系数恰为 0 ⇒ 一排恒水平。**
  落地：SolidLayout 加 `home` 机位——并拢默认「侧」（水平、读得清 12 片的渐变），
  分列默认「轴测」（gapZ=0 无深度展开，任何机位都不错位）；切排布自动带机位、
  按钮高亮同步。拖拽仍自由（转盘模式），一旦拖出俯仰角，阶梯感会按上式回来——
  这是 3D 几何，不是 bug。
- **放置规律 + 嘴心基准（2026-08-20 第四轮，用户「根据理想的最终形态反推位置，
  找出一条规律」）**：不再试凑，把终态的轴向账写成解析式——
  `S = f·SEG·r`（自由段轴向跨度）、`M = 100·rb`（嘴的开口 = **理想终态直接定义的
  高度**）、剩余 `S−M` 由上下缓冲按节数比分掉 ⇒
  **嘴心 = 2·lead + (S−M)·bT/(bT+bB) + M/2**，体心 = 嘴心 + Δ。
  - **关键发现 Δ（形态自身的下垂量）**：松散卷料的蘑菇端体心比嘴心低 **+1.8px**，
    越方越小，方箱恰 **0**（箱体被嘴对钳住不下垂），全族差 **2.8px**
    ⇒ **体心与嘴心不可能同时对齐**，只能二选一。此前四轮都在对体心，而
    体心齐 ⇒ 嘴（接合处）必然单调散开 3.4px——这就是「体心已对到量化下界、
    看着还是不齐」的真正原因：**基准选错了，不是精度不够**。
  - 用户拍板取**嘴心**（接合处居中；嘴的开口就是键长 rb，是理想终态定义的量）。
  - 反解落格用两个整数旋钮：`lead`（2px/节，严格线性、纯平移）与 `dFan`
    （扇形在自由段内的位置，上下缓冲此消彼长，实测 ~1.9px/节；f 不变 ⇒ 折叠体
    形状与高度逐位不变，实测高度差 0.00、键数不变）。dLead 限 ±1——贴合段以
    未收缩间距钉轴，各带横向条纹靠它对齐，挪多了会乱相位。
  - 标定脚本入库 `scripts/skin-array/calibrate.mjs`（网格实跑取整数最优）。
    实测嘴心散布 **3.38 → 1.01px**（量子下界）；体底连带从 7.74 → 4.93px。
  - 守门判据同步换成嘴心（≤1.5px），端点断言从「对象同一」放宽为「形态逐字
    相同、只允许整体平移」——位置不属形态。
  - **必须全程对齐，不能只对终态（第五轮，用户「网站上的也没有真正对齐」）**：
    首版标定只做了终态。把 `S = 2f·r` 代回规律，嘴心是 r 的一次式
    `嘴心(r) = [2·lead + M·(1/2−φ)] + [2f·φ]·r` ⇒ **斜率由 f·φ 定、截距由 lead 定**。
    台架 19 秒里绝大部分时间在收缩过程中（r 0.95→0.30），实测首版**早期散布
    4.78px、终态才 1.01px**——屏幕上看到的就是那 4.78px，与部署无关。
    标定改为在四个检查点（r≈0.87/0.66/0.44/0.30）上取整数最优：dFan 调斜率、
    lead 调截距，**全程最大散布 4.78 → 1.93px**。守门随之改为逐检查点卡 ≤2px
    （只卡终态会漏掉这一整类问题）。教训：**动画件的对齐要按整个时间轴验，
    不能只验静止的那一帧。**
- **对位改成解析构造，标定表退役（2026-08-22 第六轮，用户「你真的看不出来有高有
  低吗？」+ 极限放大截图）**：上一轮 1.93px 的残留在放大后仍读得出台阶。**根因是
  路子本身**：那是在「φ≠1/2、各带 f 各异」的前提下逐带补偿出来的一张表，补偿量
  受节点整数位（lead 量子 = 2px/节）限制，永远只能逼近。
  - **构造替代标定**：把规律式 `嘴心(r) = [2·lead + M·(1/2−φ)] + [2(f−1)·φ]·r`
    读一遍就有答案——令 **φ = 1/2**（扇形正居中在自由段上 ⇒ 上下缓冲等长），
    含 M（键长）的那一项**直接消失**；再令全员 **lead 与 f 相同**，则
    **嘴心(r) = 2·lead + (f−1)·r，与键长、形态、梯挡数全都无关，在每一个 r 上
    逐位相等**。对齐从此是构造出来的，不是标定出来的：DFAN/LEAD/TAIL 三张表
    退役，换成三个常数 `LEAD 54 / FREE 61 / TAIL 57`。
  - **实测**：收缩早期散布 **0.000 / 0.000**（严格恒等），终态 **0.73px**；
    全程最大 1.93 → **0.73px**。附带收益：全员 lead 相同 ⇒ 12 条带的横向条纹
    相位也一致，此前截图里那种条纹错半格的锯齿一并消失。
  - **残留 0.73px 是什么**：不是放置误差。自由段的**几何中点**全族散布仅
    0.18px（账本身是准的），差在嘴心相对几何中点的偏移 δ——上下缓冲拉伸不等
    把嘴拽偏，δ 随「方化到什么程度」在 +0.17 ~ −0.59px 间变化。它是形态自身
    的材料不对称，节点整数位抹不平。
  - **两处顺带压小 δ 的改动**：
    ① 引擎 `boxSquare` 的**缓冲料均匀排布这一遍不再随 sq 减力**（改硬投影）——
    缓冲排布是**位置**机制不是形态机制（缓冲料贴轴、藏在竖带背后看不见），
    半强度会让上下缓冲拉伸不等、把嘴心拽偏且偏量随 sq 非单调（sq=0.85 时
    −0.96px、sq=1 时 −0.03px ⇒ 末两条带之间出现 1.1px 台阶）。实测形态零变化
    （终态高度差 ≤0.03px、锁定键集合不变），只有对位改善。Lab.06/07 的四台
    只有阶梯挑台开 boxSquare 且 sq=1（旧代码走的就是硬投影分支）⇒ 逐位不变；
    Python 对照基准不开 boxSquare ⇒ 不受影响。
    ② `FREE` 取纪律允许的**最小值 61**（kMax=26 的六条带缓冲只剩 4 节，再小
    就吃穿「≥4 节缓冲」）——自由段越长、富余材料越多，上下缓冲的拉伸越不
    对称，实测全程最大散布 f=61 0.73 / f=63 0.91 / f=65 1.10 / f=67 1.35px。
    形态不受影响（终态高度逐位相同、凸出宽度整体窄 1%、锁定键集合不变）。
  - 守门随之改：新增「对位构造」一项（三段等长 + 扇形正居中 = 恒等的全部前提，
    动一个就散），全程判据收紧为**早期 ≤0.05px / 成形后 ≤1.2px**。
    标定脚本 `calibrate.mjs` 退役，换成核查脚本
    `scripts/skin-array/check-align.mjs`（改形状后跑一遍看残留与逐单元 δ）。

## 11. 收缩的注册端反转（2026-08-22，用户拍板）

用户：「把所有的结构倒过来，然后对齐点都在最下面的点，收缩是向下收缩」，并当场
纠偏「**不要把天花板变成地板，我的意思就是把收缩的固定点和方向反转**」。

- **改的是什么**：v7 原行为是 node 0 钉在天花，自由段一变短，它下面的材料整体
  上移 ⇒ 帘子下缘随收缩往上跑。现在**末节点钉住不动**，材料往下聚拢，带子的
  顶端（连同芯轨）随收缩下降。Lab.08 全程降 79.3px
  （= 自由段 61 节 × 2px/节 × (0.95−0.30)）。
- **天花板条固定不动**（用户当轮纠偏「天花板是不动的啊，你现在做的天花板是动的」
  ——首版让板跟着单元顶端一起降了）。它是房间的天花板，不是单元的一部分；
  单元往下收缩时顶端会离开它，那道缝就是「向下收」本身。代价要说清：末态单元
  是悬空的（Lab.08 顶端离板 79px），画面上没有东西把它吊住——要不要补一根吊线
  /吊杆是设计选择，说一声就加。
- **落点只有一处**：引擎加 `SkinUnitOpts.anchorEnd`（默认 false = v7 逐字路径
  ⇒ Python 对照守门不受影响）；排完芯后给整条芯加一个统一纵向偏移，使末节点
  恒在锚位。**键谱、约束、整形全都不知道这件事**——约束全是相对量、重力逐节点
  均匀，所以这是一个纯平移。
- **「纯平移」有硬证据也有边界**：新增守门跑同一键谱的两条路径逐节点比对。
  四个单元全程实测最大偏差 **4.85e-5 世界单位 = 0.005px**——不是逐位相同：
  每帧被「传送」的是哪一段贴合料变了（默认是下面那段往上跑、反转后是上面那段
  往下走），Verlet 的隐式速度会因此有极小差别。终态高度 / 凸出宽度 / 锁定键
  集合逐位相同；Lab.08 的嘴心对位构造照旧成立（各带 L(r) 恒等 ⇒ 偏移量恒等，
  全程散布仍 0.726px）。
- **下缘对位**：Lab.06/07 的四台总长本就不同（150/170/166/166 节），底端注册后
  各自钉在自己的初始位 ⇒ 下缘参差。加每台一个**常数**落位纵移把四条下缘对到
  同一条线（Lab.08 十二条带总长本就配平 ⇒ 偏移全 0、逐位不变）。`fillSolidVerts`
  为此重新引入 `offY` 尾参——**与 2026-08-20 被用户否决的那个 offY 区分**：
  那个是逐帧的居中纵移（会让接天花的顶端跟着错位），这个是每台一个常数、
  不随时间变；天花板条是共用基准、不吃这个偏移。同一根带子上「形状落在哪」
  仍在键谱层解决。
- **机位一行没改（一次自己抓回来的错）**：先按终态把三处 pivot.y 下移 40px，
  实测起始态被顶出画外——**取景要按整个运动区间的并集算，不是按终态算**。
  并集其实一点没变：以前是顶端钉死、下缘往上缩，现在是下缘钉死、顶端往下走，
  空出来的那块从底部换到了顶部，范围一模一样。已回退。

## 12. Lab.09 · 圆筒环列（2026-08-23，用户立项「把每一个单元再收窄 0.5 倍，然后把表皮向外偏移一点然后复制 20 个围成一圈，形成一个圆筒，这个圆筒收缩就可以形成一个环形平台」）

用户拍板：**接受带间的缝** · **半径做成滑块现场调** · **天花改成圆环板、底下不加东西** ·
**一个环里二十条带用同一种键谱**（当轮纠偏：「我要选用一种形状形成一个连续的环形平台」——
首版把四种键谱轮流排开，一圈里四种形态混着，平台不连续）。编号 = Lab.09（项目二第四台）。

- **引擎一行没动**，变的只有剖面往哪儿摆：从「沿 X 排一列」改成「绕世界 Y 排一圈」——
  剖面的离轴 x 变成**离筒轴的半径**，挤出方向变成**切向**。收缩前 20 条窄带贴着轴垂下来
  ≈ 一个闭合的筒；收缩后每条扣出同一个挑台，20 个挑台连成绕筒一圈的环形平台。
- **这个排布反而更贴近引擎的原假设**：v7 的 `PRESS = 3.0` 原注就是「3D 皮环向刚度的
  2D 等效外撑」——皮本来就是绕轴一圈的，只是它按剖面解。
- **一圈一种形状，四种做成选择**：控制条上「形态」四个按钮（袋 / 蘑菇挑台 / 直挑台 /
  阶梯挑台），默认**阶梯挑台方箱**——顶面是找平过的，一圈连起来才读得出是「能站人的
  平台」而不是一圈卷边。切换 = 整环重解（step 归零），实测锁定键数 20×{2,9,11,10} =
  40 / 180 / 220 / 200，逐环齐整。
- **只解一条，摆二十处**：一圈同一种键谱、引擎无随机、同谱同初值 ⇒ 20 次解算逐位相同。
  故台架把「引擎」与「摆放」拆开（`units` = 解几条、`order` = 摆哪些位置），物理开销是
  **单条带**——比 Lab.06 那一排四台还轻，RATE 仍 110。EMA 与绘图平滑也按引擎算一次。
- **换形态不重挂组件**：走台架新增的 `setUnits`（就地重建引擎/实例/落位/天花），不是换
  React key 重挂。重挂会丢一个 WebGL 上下文再要一个，一页九台的场合是实打实的风险
  （CDP 实测四种形态来回切，canvas 数恒 6）。
- **对位复用 Lab.08 的构造**：四种键谱的原生带长各不相同（150/170/166/166 节）。一圈只用
  一种、环内不会参差，但四种之间仍要可比——切换形态时筒的上下缘、平台高度不该整体跳一截。
  故四条一律搬到三段等长构造上（`placeOnBand` + LEAD 54 / FREE 61 / TAIL 57，扇形正居中
  ⇒ 嘴心 = 2·lead + (f−1)·r，与键长、与形态无关），形态逐位不动、只换它在带上的位置与
  两端缓冲长度。实测同一收缩终点的三种形态**嘴心散布 0.078px、筒高逐位相同**。四条都留
  ≥4 节缓冲（阶梯挑台正好卡在 4，与 Lab.08 右端点同一处纪律边界）。
- **袋那一环整体更高、挑得更浅**：它的收缩终点是 2026-08-18 拍板的 r₁=0.66（另外三种到
  0.30），少收 43.9px、嘴心高 22.3px。一圈只用一种 ⇒ 这是「这一环长什么样」的差别，
  不再是环内部的参差。
- **缝**：带宽恒定而圆周随半径长。默认半径 30 时芯上净缝 1.6px、平台外缘 14.4px（外缘节距
  与芯上节距之比 = 半径之比），一圈约六成是空的。用户已拍板接受——真要连续就得把带子做成
  吃满 18° 的楔片，那样「收窄 0.5 倍」这个参数就不存在了。
- **半径滑块 26–90（默认 30）**。下限是紧的：`ringGap(26) = +0.37px`、`ringGap(24) = −0.26px`
  ——再小相邻带互穿；芯轨那一圈比站位圆更靠内，也一并卡过（净缝 2.3px）。上限 90 时筒张成
  鸟笼，看得见每条带各自的挑台。
- **几何是加法式扩展**：`skin-solid` 加 `RingPlace` 尾参（不传 = 直排路径逐位不变，守门第一条
  就卡这个）+ `placePoint` / `rotateVertsY`（键线端点与芯轨小盒搬上环）+ `ringPlateVerts`
  （圆环板天花，矩形截面扫一圈；半径可调 ⇒ 不能烘死，按半径缓存重烘）。gl3d / camera3d
  **零改**。天花板内外让量收到 12/11——首版 14/16 在顶视里盖掉了平台内圈约三成。
- **一圈起伏（用户 2026-08-23「一圈的形状 从低到高再到低一圈下来」，改为 Lab.09 的默认编制）**：
  又是 `lead` 那个旋钮的另一种用法——**同一种键谱，每个位置一个不同的 lead**，
  于是环沿圆周升上去再回来，读成一段绕筒的螺旋台阶。
  - **形状一个数没变**：在放大后的环族几何上重标——lead 12–58 全程剖面逐点偏差 ≤0.008、
    筒高恒 221.4、锁定键数不变，变的只有高度。守门直接卡这条（最低级与最高级剖面逐点相同）。
    lead ≤ 6 时贴合段撑不住、折叠体被拽变形（偏差跳到 17），故波峰留 2 节余量。
  - **波谷取 RING_LEAD**（环族当前落位，现 58），波峰 14 ⇒ 平台在筒高的
    30% ↔ 71% 之间扫，Δ 88px。
  - **幅度已经到头**：并线那轮把键谱放大 2.1×、并把贴合段匀给自由段（lead 96→58，
    自由段 61→129），可让的行程从 152 节掉到 46 节；而折叠体自己变高了（嘴高 ≈67px），
    所以这条起伏读作**一条升降的环带**，不是分明的台阶。要更像台阶只能回头动那两个
    决定（放大倍率 / 段长分配），那是并线拍过板的东西，不在这一轮里改。
  - **用余弦不用三角波**：三角波在最高最低处有折角；余弦两端平缓、接缝处斜率连续，
    一圈读下来是一条起伏而不是两段斜坡。11 级、20 位回文（位置 i 与 20−i 同级 ⇒ 解 11 条）。
  - **「形态」四按钮在这一档照样有效**（起伏是高度的事，与用哪张键谱无关）——
    渐变那一档才需要把它锁掉。
  - 回文编制抽成公共的 `palindromeOrder(count, levels, offset)`，渐变改为调用它（行为不变）。
  - 相位转四分之一圈，理由同渐变：不转的话波峰波谷落在正前正后，默认机位下看不出起伏。

- **挑台环挪到筒的下段（用户 2026-08-23 圈图拍板「把这个形状安排到我画的这个位置去」）**：
  机制里正好有这个旋钮——`lead`（贴合段节数）就是**形状在带子上的位置**，每多一节折叠体
  沿带下移 2px（SEG·100，与收缩率无关）。**lead + tail 保持 111 不变** ⇒ 带子总长、筒的
  上下缘、机位全都不动，动的只有环的高度。定 `RING_LEAD = 96 / RING_TAIL = 15`：
  实测平台从离底 51% 挪到 **18%**，与原分配的剖面逐点偏差 **0.000**（纯平移，形态一个数
  没变）。上限卡在尾段：tail=7 时偏差 0.756——尾段太短撑不住，形状被拽变形，故不再往下挪。
  **这两个数是 Lab.09 专用**，不是 skin-array 的 ARRAY_LEAD/ARRAY_TAIL（那副是 Lab.08
  十二条带的，改它会一并挪走那台的形状）；两种编制共用同一副，切编制时环的高度不跳。
  守门加「位置在离底 14–24% + 换 lead/tail 分配后剖面逐点相同」两条断言。

- **灰立杆不跟着外皮缩（用户 2026-08-23 看真机纠偏「起点始终和天花板在一起、尾端固定在现在
  固定的位置」）**：此前芯轨就是芯本身、长度随收缩变，注册端在底端 ⇒ 整根随收缩往下沉、
  与天花脱开一大截。环列改成**房间的固定立杆**——上端埋进天花圆环板、下端在注册端钉住的
  那一点，长度恒定；收缩时是外皮沿着它往下聚，不是它自己缩。纯几何拆成 `skin-solid.railSpan`
  （两种读法各一条，可单测），台架加 `rail` prop；**Lab.06–08 仍是「轨即芯」的旧读法**
  （2026-08-22 拍板的「那条缝就是往下收本身」没被动），要全站统一就是再传一个 prop。

- **一个当场撞见的事故**：环列实例仍带着「排一列」的 175 间距，被当成半径加了上去，
  二十条带被甩到半径 3355 处。ring 开时行/列偏移必须清零——现在写在构造与 applyLayout 两处。
- 台架 `components/lab/SkinRingBench.tsx`（薄壳，同 SkinArrayBench 的做法），/lab 第九台、
  主页第九卡（网格 8 列→9 列），统计条 325→**339**、demos 08→09（内核仍 03）。
- **局限**：20 个单元彼此不相互作用——引擎是单剖面的，围成筒只是排布，环向的布张力不模拟
  （`PRESS` 是它的等效外撑，不是真的耦合）。
- 手感常量待用户真机拍板：带深 7.8 / 厚度 3 / 半径默认 30 / 默认形态 / 轴测俯角 −0.45 /
  机位 camScale 0.95 / 天花圆环板的内外让量。

## 13. Lab.09 第二种编制：一圈渐变（2026-08-23，用户「蘑菇转化成直台然后再转变回来，渐变的在一圈里完成」）

按 2026-08-20 立的纪律先出线稿（`scripts/skin-ring/draft.mjs`：真引擎终态剖面 +
等弧长相邻形态距离，并排 + 叠合），确认后才接到环上。

- **端点换了**：用户点名的蘑菇↔直挑台实测形态距离只有 **1.86px**（蘑菇↔方箱 8.85、
  袋↔其余 25–28），11 级线稿叠起来几乎是一条线——一圈里根本读不出在渐变。
  改 **蘑菇挑台 ↔ 阶梯方箱**（用户看线稿后拍板 a），一圈走一个来回。
- **闭合是这件事的全部约束**：一圈是周期的，第 19 条与第 0 条是邻居。20 位做回文
  （0→10→0）⇒ **11 级键谱**，位置 i 与 20−i 共用同一条引擎 ⇒ 只解 11 条不是 20 条。
  守门里那条「含首尾在内每对邻居恰好差一级」就是闭合本身。
- **时间表按 11 级重排，不是把 Lab.08 的 12 格重采样**——重采样会漏行，漏掉那格的
  形变堆到下一步（实测 Δ2.82，是其余步的 4 倍，正是 Lab.08 当初被否的断层）。
- **两个离散跳变的落位是扫出来的**（端面找平到位的格 × 梯挡数 24→26 的格，16 种
  组合）。纯看等距最优是 panel@3 + 梯挡@8（0.71–1.80，2.5×）；**定版取 6+6**
  （0.71–2.04，2.9×），理由是等距指标读不出来的两件事：① panel 一到位蘑菇的卷边
  就被端面找平拉直，它落在哪一格「蘑菇」就只占到哪一格——放 3 的话一圈二十条里只有
  5 条还是蘑菇，放 6 则 11 条蘑菇 9 条箱，来回才读得出是来回；② panel@3 那版第 3 级
  有一颗键始终没锁上（9→8），环上会有两条带比邻居少一根梯挡。
  代价是 panel 到位那一格 2.04，它是硬跳变压不掉：半跨渐入实测更差（2.21，
  Lab.08 记的「半跨是假杠杆」在这里再次成立），把梯挡跳变挪开也只从 2.04 变 2.07。
- **平台仍然是平的**：对位构造（三段等长 + 扇形正居中 ⇒ 嘴心与键长/形态无关）在渐变
  下照样成立，实测 11 级嘴心散布 < 1.2px。这是能做环上渐变的前提——形状在变、
  平台高度不变。
- **相位转四分之一圈**（`GRAD_PHASE = 5`）：不转的话两个折返点（纯蘑菇、纯方箱）
  分别落在正前与正后，默认轴测机位下正好被自己挡住，看到的只有蘑菇那半。
  纯外观量，形态/级数/闭合都不受影响。
- 台架：Lab.09 控制条加「编制」两档（整环同形 / 一圈渐变），渐变下「形态」四按钮
  变灰（由 11 级键谱决定）。渐变要解 11 条引擎，推进速率随之 110→80（同 Lab.08）；
  `rate` 因此改成 ref 读取（主 effect 只建一次，直接用 prop 换不了档）。
- 守门 `skin-ring-gradient.test.ts` 7 项，重点是「相邻形态距离 0.5–2.5px 且比值
  ≤3.2×」——时间表一改就可能出断层，这条卡住它。统计条 339→**346**。
- 边界申明照旧：交接件明令「行为矩阵 → 键谱的翻译规则由使用者手写」。这条序列与
  Lab.08 一样只是两张既有键谱之间的形态学串联（演示编排），不是那套翻译规则。

## 14. Lab.10 · 4×4 环阵列（2026-08-25 用户纠偏定版：「我指的阵列是 4×4 的，每一个单元都是那个环形的，而不是 4×4 个小单元」）

**首版作废。** 2026-08-23 立项那句「一个 4×4 的阵列，间距就是每个膨胀到最大的时候有一点点
空隙就行」被做成了**十六个单条带**铺进平面网格；用户当轮说清楚，要的是**十六个 Lab.09 那种
圆筒环**——每格是一整个环（20 条带绕轴一圈、收缩后连成一圈环形平台），十六个环站成一片场地。
拍板一并给了另三条：**替换掉首版**（不留成编制选项）· **格距跟着半径滑块走** ·
**每个环各自独立**（不要挤成一片）· 编制取「整片同形 / 每行一种」。

### 14.1 物理开销不随格数涨，渲染涨 16 倍——所以改了渲染路径

- 一个环同一种键谱 ⇒ 引擎只解一条带、摆 20 处（Lab.09 的既有做法）。十六个同形环 =
  仍然**只解一条带**，摆 320 处。整片同形一条引擎，每行一种四条。仿真一点没变。
- 涨的是几何：每条带 172 节 → 1,372 三角，十六个环 = **439,040 三角**。照 Lab.09 的老路
  （每条带各自烘顶点、各自上传）要每帧传 320 份，是 Lab.09 的十六倍。
- 故 `gl3d` 加**「上传一份、多处摆放」**（`MeshPlace` = 绕世界 Y 的 yaw + 平移）：
  同一条带的顶点传一次，320 处只换 `uModelR`/`uModelT` 各画一遍。**逐帧上传量因此比
  Lab.09 还小**（那台是二十条带各传一份）。`drawDynamicMesh` 与 `drawLines` 各加一个
  可选 `places` 参数，**不传即旧行为**；`LINE_VS` 补了模型变换（每次调用都显式置恒等，
  故 Lab.01–09 与 src/demo 逐位不变）。
- 带子因此改在**本地系**烘：剖面的离轴 x 加上半径摆在 +X、挤出走 Z，方位角交给 GPU。
  Lab.09 单环仍走 CPU 那条（`placeOf` 在有 cells 时才返回 null），不给它添回归风险。

### 14.2 间距是推出来的，且跟着滑块走

环占的圆环带 = [R − 芯轨内偏, R + 全程最大膨胀]，故 **格距 = 2·(R + PEAK_REACH) + 缝**。
半径是滑块 ⇒ 格距跟着它算，拉滑块时整片阵列一起呼吸。

- **全程最大膨胀 52.1px**（直挑台实测 52.02，向上取到 0.1 ⇒ 常量是**上界**不是约等——
  首版写 52.0 比实测小 0.02，守门当场逮住）。必须按「全程」量而不是终态：
  **阶梯方箱在 step 519 鼓到 43.5，终态反而收回 40.6**。守门专卡这条。
- **缝 = 1.5 × 环内平台外缘的带间缝**（R=30 时 18.0 → 27.0）。这是「每个环都独立」的
  落地：环与环之间的空地必须**明显大于环内相邻带之间的缝**，否则读作「环内的又一道缝」
  而不是两个环之间的空地。比值 1.5 是手感常量待拍板，「必须大于」是硬约束、守门卡它。
  跟着半径走是必须的——R=90 时环内缝涨到 36.8，一个固定的缝会重新被环内的缝盖过去。
- **芯轨不参与**环间净距：它在半径内侧（R − 5.8），被环身自己包着。这与首版不同
  （那时单元是一片，杆露在外面，缝要算它）。
- 实测：R=26 格距 181.3 / R=30 191.2 / R=90 339.5；占宽 700 / 738 / 1303。

### 14.3 取景是**逐视角**的，不能一个数管四个

一个环的时候四个预设差不多大；铺成 4×4 之后差得很远——**轴测被宽度卡住，顶视被整片
地面的高度卡住**。738×738 的地面投进 520 高的画框，顶视所需 scale 只有轴测的 0.77 倍。
一个数管四个的话：按轴测定则**顶视裁掉两行**（首版即此，CDP 截图实测），按顶视定则
轴测两侧空出一大片。

- 每档 `K = 该视角在滑块全量程内允许的最小 (scale × span)` 再留 6% 余量：
  轴测 468 · 正 658 · 侧 591 · 顶 361（允许值 498 / 700 / 629 / 384）。
  `camScale = K / span(R)`。
- 允许值不是拍脑袋：`ringGridViewFit()` 把阵列包围盒（±span/2 见方 × 世界 y ∈ [−6, 336]）
  投到该视角，取宽高两个约束里紧的那个。**这个推导是模块里的纯函数、进了守门**
  （卡「实际取景 ≤ 允许值」且余量 ≤15%，改机位或改格距规则都会被逮住）。
  轴测角 `RING_GRID_AXON` 也移进模块，台架从这里取 ⇒ 推导与实际机位同源。
- 切视角时**取景与姿态一起插值**，否则会「先跳一下再转」。

### 14.4 两个坑（都不报错，只表现为「有时候不对」）

1. **函数 prop 被 []-deps 的 effect 捕成旧闭包。** `cells`（每格用哪一份编制）在换编制时
   是个新函数，而主 effect 只建一次 ⇒ 拿到的永远是挂载那一刻的那个，除第一份以外的编制
   一格都摆不出去。改走 ref（每次渲染同步）。`camScaleFor` 同办。
2. **视角过渡里的取景不能在点击那一刻算死。** 首版把两端 scale 算好存进 viewAnim，
   过渡途中拉半径滑块时，过渡的下一帧会把新取景**覆盖回旧半径的值**。慢设备上尤其容易
   撞上——单帧 dt 封顶 50ms，过渡在低帧率下要跑好几秒（本轮就是在 CDP 里踩到的：
   顶视 → R=90 → 轴测 → R=30 → 换编制，最后画面只有该有的 0.58 倍）。改成**逐帧现算**。

### 14.5 布景与尺度（2026-08-25，用户手绘草图：「建一个房间，做一个布景，里面放一个人型比例参考」「注意还原这个位置关系」）

**这是全站第一次有真实尺度。** 在这之前所有台架都是无尺度的——世界单位就是 2D 剖面的
px×100，没人说过一个单位等于多少毫米。草图把它定死了：它画了天花、地面和一个站在地上
的人，三者的比例就是换算关系。

按草图量得：吊件总长 ≈ 0.71 房高 · 吊件下缘离地 ≈ 0.27 房高 · 人 ≈ 0.455 房高。
站上吊件总长是 336 世界单位 ⇒

| 量 | 世界单位 | 真实 |
|---|---|---|
| 房高（天花 y=0 → 地面） | 473 | 3.74 m |
| 人 | 215 | 1.70 m |
| 吊件下缘离地 | 137 | 1.08 m |
| 一个世界单位 | 1 | **7.9 mm** |
| 环形平台外径 | 164 | 1.30 m |
| 格距（R=30） | 191 | 1.51 m |
| 十六个环占地 | 738 见方 | 5.8 m 见方 |

后六行**不是设定值，是推论**——要改尺度只改人的身高，别的会跟着走。

- **房间只做地板 + 两面墙，不做天花板面。** 相机在 (+X, +Z, 上方)，−X 与 −Z 两面在远端、
  露的是内表面，不挡装置；天花板面若真做出来会横在相机与装置之间，**整幅盖死**。墙顶收在
  y = 0，那条顶边就是草图里的天花线（每格的圆环板正挂在这个平面上）。读起来是一间被剖开
  的房间——与草图那张剖面图是同一种画法。守门里有一条专门卡「没有天花板面」。
- **房间随半径滑块走**：内净尺寸 = 阵列占宽 + 两侧各 120 单位（≈0.95 m，草图上墙离最外
  一件约 0.25 房高）。格距变了墙不跟着变的话，阵列会撞穿墙。
- **取景改按房间算**：包围盒从「阵列 + 带子高度」换成「房间内净尺寸 + 房高」，四档 K 随之
  重标（轴测 468→453、正 658、侧 591、顶 361→356），枢轴 y 从 166 移到包围盒中高 240。
  代价是阵列在画面里小了约四分之一——布景要读得出来就得把房间框进去。
- **人体比例参考是自己搭的**（`src/lib/space/figure.ts`）：本仓库没有人体模型资产，也没有
  取外部模型的管线（站上的实体网格全部来自用户自己的 .3dm）。故按建筑模型的惯例搭了个
  十四块体块的小人：7.5 头身、站姿迈一步、手臂微张，**任何视角看都是人形不是贴片**（守门卡
  宽/深都 > 0.15 身高）。以后要换成真实扫描/购买的模型，换 `figureVerts` 一处即可。
- **人站哪儿是硬约束不是构图偏好**：人的头顶在 y ≈ 258，而带子的下缘在 y = 336（更低）——
  **平面上一旦与环重叠，头就插进平台里**。站进环阵内部的十字过道也不行（被四个环围住、
  读不出比例）。定在阵列近侧那个角外扩 24 单位：全量程下与最近那个环净距 28–86 单位，且
  **正视图里人正好落在最外一列的右边**，那就是草图画的位置关系。守门逐半径卡这三件事。
- 台架接线是加法式的：`SkinSolidBench` 加可选 `scene`（给半径返回一组静件三角网格，按半径
  缓存重烘），**不传即不画**，Lab.07–09 的绘制路径逐位不变（CDP 复核：三台的画面占幅与
  改动前逐位相同）。
- 守门新增 `figure.test.ts` 4 项（身高精确 / 横向身量在人的范围 / 平移与转向 / 网格闭合）
  + `skin-grid.test.ts` 4 项（尺度自洽 / 位置关系 / 房间构成 / 布景件）。统计条 355→**363**。
- 手感常量待真机拍板：房间留距 120、墙与地板的明暗、人的站位与朝向。

### 14.5.1 装置整体缩到 0.5（2026-08-25 用户拍板「整体缩小至 0.5」，并选定「挂得更低」）

布景一落地，尺度关系就看得见了：十六个环把 5.8 m 见方的场地填满、几乎抵到墙，人被压得
很小。用户拍板**装置整体 0.5**——环、格距、吊长、织物厚度全按 0.5 缩，房间与人不缩。

| | 缩放前 | 缩放后 |
|---|---|---|
| 环形平台外径 | 1.30 m | **0.65 m** |
| 场地（十六个环占地） | 5.8 m 见方 | **2.9 m 见方** |
| 吊长 | 2.65 m | **1.33 m** |
| 房间 | 7.7 × 3.74 m | **4.81 × 3.74 m** |

- **只缩不移是不行的**：装置从天花往下吊，只缩的话下缘会从离地 1.08 m 升到 **2.41 m**，
  整片高过人头，读成吊顶而不是能用的平台。故缩完整体下移 `RIG_Y = 168`，**让下缘停在
  原来那个高度**；空出来的那截由芯轨补上——立杆仍从天花一直落到钉住点，装置挂在它的
  下半截。这一支是用户从三个选项里选的。
- **实现是摆放期的均匀缩放，不是把几何重算一遍**：`gl3d` 的 `MeshPlace` 加了一个可选
  `s`（uModelR 放缩放后的旋转矩阵；法向在着色器里 normalize，均匀缩放不影响明暗），
  带子与键线的顶点仍在装置自身坐标系里烘，缩放交给 GPU。**引擎、键谱、对位构造、止程
  全都不知道有这回事**，故与 Lab.06–09 仍是同一份东西。
- **两个件不吃这个缩放**：芯轨（房间的立杆，上端恒在天花、只有截面随装置缩）与天花圆环板
  （只有半径随装置缩，高度与板厚不动）——这两件按世界尺寸直接建，摆放时 s=1。
- **取景改成逐半径解析算，K 表退役**：装置缩半后房间的占宽减半而房高不变 ⇒ 房间的长宽比
  随半径变，四档允许值在量程内摆动 23%，按下限标定的常数会在大半径处白留四分之一画框。
  现在 `camScale = ringGridViewFit(半径, 视角) × 0.94`，每档都贴着自己的极限留同一份余量。
- **一个当场被守门逮住的单位陷阱**：`ringOuter`/`ringCellPitch` 说的是**装置自身坐标系**里
  的尺寸，而 `ringGridCells`/`ringGridSpan` 出来的是**世界**尺寸（已乘 0.5）。首版把两者
  混着用，人与环的净距算成负的、格距断言差一倍——两条测试同时红。守门 +1（缩放本身：
  下缘不动、世界尺寸确实是装置尺寸的一半、平台 ⌀0.65 m）。
- 人的落位随之调整：环变小而人没变，对角外扩量 24 → **40 单位**（≈32 cm），全量程与最近
  那个环的净距 35–48 单位。统计条 363→**364**。

### 14.5.2 平台挑得更远：真正的杠杆不是收缩量，是材料量（2026-08-25 用户「往外延出来的这个平台，展开的更多一些，意思就是收缩的更多一些」）

用户给了机制猜想，**实测不成立**，值得记下来：

- **收缩终点 r₁ 从 0.30 压到 0.18，挑出只多 5%**（52.0 → 54.8px）；压到 0.14 以下
  反而回缩（终态 51.8 而峰值 55.2），平台变厚不变长。
- 原因：挑出的长度由**键捕获的材料量**定。键一旦锁住，被它兜住的那段材料就定了；
  核心再往里收，只是把已经锁死的东西拉近，不会多出材料来。
- 而现有键谱**已经把 61 节的自由段吃满**——最外键跨 52 节，两端只剩 4 节缓冲，
  那是交接件的纪律下限。所以「在现有带子里再挑远一点」是没有余地的。

真正的旋钮是给它更多材料。落地 = **环族专属的放大构造**（`RING_GROW = 1.5`）：
键跨、嘴口 rb、自由段一律 ×1.5，自由段 61 → 91。

- **取 1.5 是因为它保结构相似**：扇形步长 2 → 3 是整数，于是**每种形态的键根数一根不变**
  （蘑菇 9 / 直挑台 11 / 阶梯 10 / 袋 2）——还是那个形态，只是大了一圈。自由段取奇数
  91，扇心仍落在整数节点上。等长键纪律照旧：阶梯的端面 24 节 × 0.02 = 0.48 = 放大后的 rb。
- 实测挑出：蘑菇 51.8 → **73.2**、直挑台 52.0 → **73.9**、阶梯方箱 40.6 → **58.9**、
  袋 34.3 → 48.6。平台外径（默认半径 30）⌀0.65 → **⌀0.82 m**。
- **连带跟着变的**：带子 172 → 202 节，装置竖向占高 336 → 393（顶端离天花近了一截、
  芯轨相应变短；**下缘离地 1.08 m 不动**——`RIG_ANCHOR_Y` 就是钉这件事的）；
  平台变大 ⇒ 格距变长 ⇒ 场地 2.9 → **3.73 m 见方**、房间 4.81 → 5.35 m。
- **Lab.09 与 Lab.10 一起变**：Lab.10 的定义就是「十六个 Lab.09 那种圆筒环」，
  只改一台会让两台的环不再是同一个东西。**Lab.06/07/08 的键谱一个数没动**
  （`SKIN_UNITS` 原样，放大只发生在 `buildRingUnits`/渐变这条路上）。
- **一圈渐变要重扫一格**：放大不是严格的相似变换（重力、PRESS、键刚度这些量没跟着缩），
  等距性因此变差——原来的 6+6 相邻距离比值从 2.9 涨到 3.75（守门当场红）。
  **用户钉死的 panel@6 不动**（那是按「一圈里蘑菇占几条」定的，与等距无关），
  只重扫梯挡那一格：3–7 一律 4.0 上下、8 是 3.5、**9 是 2.57**（比放大前还好）。
  故 `KJUMP` 6 → 9；RB / SQ / PW_FROM 一个数没动。
- 守门相应改口径：环族的缓冲/居中断言从 `ARRAY_*` 换成 `RING_*`；「形态未被搬动」
  改成「**按 RING_GROW 等比放大、根数一根不变、等长键纪律仍成立**」；渐变的距离阈值
  随构造同比（比值那条是形状量、不动）。测试数不变（364），改的是既有断言的口径。

### 14.5.3 再折深一档：总长钉死，把贴合段匀给自由段（2026-08-25 第二轮，用户标注红/黄线「进一步增加收缩程度，到黄色的程度」+「不是要增长带子，就这个长度，增加折叠程度」）

用户在截图上画了红线（现状）与黄线（目标），量下来是挑出再多约 **1.44 倍**；并明确
**不许把带子接长**。于是这一轮解决的是「同样一根带子，怎么折得更深」。

**先量出了一条干脆的关系**：`挑出 ≈ 最外那根键的半跨 × 2px`（k=36 → 72px，实测 73.9）。
挑出就是那根键兜住的材料对折之后的长度；内圈的键只负责把中间的材料排成梯身。
两条死路顺便钉死，省得再试：

- **收缩终点 r₁ 不是杠杆**：0.30 压到 0.18 只多挑 5%，0.14 以下反而回缩（上一节已记）。
- **嘴口 rb 也不是**：×0.7 / ×0.5 / ×0.3 逐档量下来挑出不升反降，还开始掉键。

所以旋钮是「最外的键吃掉多少自由段」，而自由段可以**从贴合段匀过来**——带子总长一个
节点都不用加：

| | 第一轮 | 第二轮 |
|---|---|---|
| 贴合段 lead | 96 | **58** |
| 自由段 free | 91 | **129** |
| 尾段 tail | 15 | 15 |
| 带子总长 | 202 | **202（钉死）** |
| 放大倍数 | 1.5 | **2.1** |
| 直挑台挑出 | 73.9 | **101.0** |
| 阶梯方箱挑出 | 58.9 | **82.2** |
| 平台外径 | ⌀0.82 m | **⌀1.04 m** |
| 场地 | 3.73 m 见方 | **4.74 m 见方** |

- 取 2.1 仍是为了**结构相似**：扇形步长 2 → 4（= round(2×2.1)）是整数，键的根数一根不变。
- **平台高度几乎没动**：折叠体自己变大、嘴心跟着抬高，但下缘离钉住点只从 +32px 变到
  +33~50px（筒高 221px）——两头几乎抵消。
- **等长键改成从端面板反算**：放大后 rb×g 与 panel 取整后各自算会差 0.008（0.672 vs 0.68），
  等长键纪律就不精确了。`growSeg` 改为先取整端面板、再由它反算 rb，纪律精确成立。

#### 一圈渐变：老结论「半跨是假杠杆」在新尺度下被推翻

折叠加深后，原来的时间表崩了：端面找平（panel）一到位就吃掉 13px 挑出**还掉一根键**，
逐级距离出现 12.3 的断层（比值 7.6–12.9，守门当场红）。查下来：

- **方化强度 sq 几乎不影响挑出**（±1px），元凶是 panel。
- **panel 的半跨在新尺度下是连续杠杆**：±0 挑 97 · ±6 挑 95 · ±9 挑 93 · ±11 挑 92 ·
  ±13 挑 90 · ±15 挑 88 · ±17 挑 84。**这推翻了 Lab.08 时期「半跨是假杠杆」的结论**
  ——那是在 ±8 的尺度上得到的，不到全跨端面投影根本不参与，所以当时只能做成硬跳变。

于是把 `PW_FROM` 那个硬跳变换成 `PW` 逐级渐入表 `[0,0,3,6,8,10,12,13,15,16,17]`，
梯挡跳变重扫定在 `KJUMP = 8`。结果：键数逐级单调（不再掉键）、相邻距离
**1.62–6.06、比值 3.73**（改之前 7.6–12.9）。只有 ±17 会掉键，故满跨只留给端点那一级
——把 PW[9] 也提到 17 试过，那一级挑出会从 86 塌到 72，比不提还差。

残留的最大一步是「末级 → 端点」（挑出 91 → 82）：端点是**真的阶梯方箱原谱**（自己的
键位图 + 全跨端面 + 满强度方化），比它前一级更方，这一步压不掉。守门阈值相应放到 3.9，
真正卡「有没有断层」的仍是比值那条。

### 14.5.4 环间织物膜（半透明蒙皮，2026-08-25 用户「给这些结构都加一个半透明蒙皮」）

带子本身就是布，缺的是**带与带之间**那块。环上二十条窄带绕轴一圈，圆周比带宽长，
于是天生留缝——R=30、挑出 101 时，芯上 1.6px、平台外缘 41px，一圈六成是空的。
加一层膜把缝糊上，筒就读作一个封闭的体、平台读作一整圈台面，而不是一把梳子。

- **几何 = 一张直纹带**（`skin-solid.membranePanel`）：A 边取本条带的后剖口
  （切向 −depth/2）、B 边取下一条带的前剖口（+depth/2，绕世界 Y 转过一个角节距），
  正好糊住那道缝、不与带子自己重叠。面片索引复用 `linkage/skin` 的 `bandTriIndex`
  ——**与 Lab.04 五环的织物蒙皮同一份**，不另起炉灶。
- **每帧只上传一份**：一圈同谱时二十块板逐位相同，故烘一份，二十个方位角（× 十六格）
  全走 gl3d 的摆放表。一圈渐变编制下每对邻居形状不同，才逐块烘。
- **半透明三条硬约束照 Lab.04 定案**：深度只测不写 ⇒ 必须**实体全画完之后**才画 ⇒
  面之间没有 z 排序，故调用点自己按视深从远到近下单（`bandsFarToNear`）。
  同谱那条路把 (方位角 × 格子) 一起排；渐变那条按板自身视深排，格子间不排（环彼此
  分得开，误差看不出来）。
- **控件**：0–1 滑块「蒙皮」，默认 0.35 —— 与 Lab.04 的默认一致。0 = 只剩带子
  （与加它之前逐位相同），1 = 完全封闭的筒。
- 两台一起有：Lab.09 单环与 Lab.10 十六个环共用 `SkinSolidBench` 的同一条路径。
  **Lab.07/08 没加**：那两台的带子是排成一列的目录/帘子，不是绕轴闭合的一圈，
  膜会把不相干的样本连起来；Lab.08 的并拢帘子若要也可以加，但那是另一个决定。
- 守门 `skin-solid.test.ts` +3（直纹带的顶点/索引规模 · 两条边正好搭在缝的两侧 ·
  两侧剖面不同也能搭）。统计条 364→**367**。

### 14.6 其余

- **天花 = 每格一块圆环板**（`ceiling='ring'` + 每格一处摆放；随半径重烘）。首版那套
  「每列一根梁」（`ceiling='beams'`）与平面网格站位（`grid` prop）随首版一起删掉，
  没有第二个调用点。芯轨沿用 Lab.09 的固定立杆（`rail="fixed"`），20 根 × 16 格。
- **编制**：整片同形（默认阶梯挑台方箱——顶面找平过，一圈连起来才读得出是能站人的平台；
  形态四选一）/ 每行一种（行 0–3 = 袋 / 蘑菇挑台 / 直挑台 / 阶梯挑台，四条引擎，
  形态按钮变灰）。两种都是**一份编制里只有一条引擎**——环内不混形态，2026-08-23
  那条「一圈用一种形状才连得成平台」在阵列里照样成立。每行一种解四条引擎，速率 110→80。
- 环本身一个数没改：`buildRingUnits()` 与 Lab.09 同一份（RING_LEAD 96 的三段构造、
  20 条带、带深 7.8、厚度 3）。变的只有「这个环被复制到哪些站位」。
- 守门 `skin-grid.test.ts` 7 项：全程峰值（且常量是上界）· 环间缝 > 环内缝且全量程成立 ·
  格距与取景随半径单调 · 逐视角取景不裁边不空 · 十六格居中且行列间距 = 格距 ·
  编制与格子对得上 · 环与 Lab.09 同源。统计条 353→**355**。
- 手感常量待用户真机拍板：缝的比值 1.5 / 默认半径 30 / 默认形态 / 四档取景 K。
- **验证环境注记**：这台（以及所有 WebGL 台架）在 `next dev` 下**看不出效果**——本机
  headless 里 HMR 的 websocket 握手失败会把 chunk 加载带崩，页面根本不水合（effect 一次
  没跑、canvas 空白、STEP 恒 0，且**干净树上的 Lab.09 一模一样**）。要看真效果必须
  `npm run build && npm run start`。别把它当成渲染 bug 排查。

## 15. 双结构带线稿（2026-08-26，用户草图立项「我现在想做右边这种 就是条可以出现两个结构的」）

用户手绘：左 = 现有单结构带（一条带、一个折叠结构），右 = 同一条带上**上下两个结构**。

### 15.1 机制：内核零改，五段谱

- `SkinSpec` 本就是通用分段列表——v7 的四个单元只是恰好都只有一个自由段；
  `buildUnit` 对每个带键谱的自由段各建一条**独立拉链**，`advance` 里锁定 / 层直化 /
  吸引全部按链循环。双结构 = 五段谱 `[贴合 lead | 自由 f₁(键谱A) | 贴合 mid | 自由 f₂(键谱B) | 贴合 tail]`。
- **不是交接件暂缓的 tunnel 形态**——那条暂缓的原因是「需皮-芯键新键型」，
  这里没有任何新键型，只是既有键谱的分段组合。
- 构造落在 `src/lib/space/skin-dual.ts`：`buildDualSpec`（五段谱）+
  `buildDualBand`（目录形态两两成带，`placeOnBand` 同一份、每个结构在自己的自由段正居中
  ⇒ φ=1/2 对每个结构各自成立）+ `buildDualControl`（解耦对照带，守门用）。

### 15.2 解耦：构造前提 + 实测

- **mid ≥ 16 是解耦前提**（`DUAL_MID_MIN`）：全局约束里跨距最大的是 span-16 抗弯，
  中间贴合段不短于它，就没有任何一条约束同时抓住两个自由段的节点；贴合段每迭代
  两次钉回芯上，两个结构之间只隔着被钉死的材料。
- 实测（双直挑台 vs 把另一个结构键谱拿掉的对照带，终态逐节点）：上结构偏差
  **0.090px**、下结构 **0.006px**；锁定键集合逐位相同，锁定数恰为单结构两倍
  （直挑台 22=2×11、阶梯 20=2×10、蘑菇 18=2×9）。另一个结构在不在，这个结构不知道。

### 15.3 单收缩自由度：搭配的合法性

双结构带仍是**一个单元**（交接件「每单元一个收缩自由度 ℓ」），两个结构同步收缩。
r₁ 与 boxSquare 都是单元级选项，两个结构必须同值——`buildDualBand` 对不合法的
搭配直接抛错（袋 r₁=0.66 不能与全深形态混排；阶梯方箱的 boxSquare 作用于全部链，
不能与非方箱形态混排），而不是悄悄用一边的值。合法搭配：四种同形对 + 蘑菇/直挑台混排。

### 15.4 线稿（纪律：先线稿、拍板后上 3D）

工具 `scripts/skin-dual/draft.mjs`（真引擎终态、整条带、锁定键橙线）：

- 模式 `pairs`：单结构参照 + 双直挑台 / 双阶梯方箱 / 双蘑菇 / 蘑菇＋直挑台。
  尺度 = 目录原谱（1.0×，free 61 正居中），lead 24 / tail 24。
- 模式 `gap`：双阶梯方箱的间距系列 mid ∈ DUAL_MID_OPTIONS（结构不变，只变间距）。

### 15.5 解耦的第二个下限：mid ≥ 29 = 逐位相同（上 3D 前钉准）

首版守门把「mid ≥ 16 ⇒ 解耦」当成一句话，实测打脸：mid=16 时对照带偏差
0.40px（折叠体）/ 0.86px（贴轴缓冲）——锁定键集合仍逐位相同、肉眼不可见，
但不是「不变」。逐档扫出干脆的边界：**mid=28 偏差 7e-2px、mid=29 起精确 0.000**。
机理有推导：钉回发生在迭代末，一个迭代**内**扰动经贴合段的最大传导距离 =
16（span-16 的读窗）+ 13（前序 stretch/bend4/bend8 的累计写入 1+4+8）= 29 节。
故两个下限并存：16 = 合法（无一条约束跨两段），29 = 逐位解耦——守门对 29 卡的
是 `toBe(0)`，不是「很小」。

### 15.6 Lab.11 · 台架（同日上 3D，/lab 第十一台）

- 台架 `components/lab/SkinDualBench.tsx` = SkinSolidBench 参数化复用（Lab.08 同款
  薄封装，零第二份实现）：五条带沿 X 排开（gapX 150）、通长天花、剖口梯挡。
  五条 = 四种同形对（目录序：双袋 / 双蘑菇 / 双直挑台 / 双阶梯方箱）+ 蘑菇＋直挑台
  混排（两条拉链各自独立的直接演示）。
- **间距做成控件不替用户定死**（Lab.05 转速滑块先例）：三档 16（草图紧凑，默认）/
  29（逐位解耦下限）/ 48（拉开读），切换走 setUnits 就地重建、不重挂 WebGL 上下文。
- 机位按全程运动包络定（`scripts/skin-dual/envelope.mjs`，Lab.05 先例）：三档里
  最长的 mid=48 带（218 节）全程在画内，四边余量 ≥53px。
- /lab 第十一台（Project II 组 Lab.06–11）、主页第十一卡（网格 10→11 列）、
  统计条 370→**377**、demos 10→11（内核仍 03——同一引擎无第四内核）。
- **验证环境注记**：无头 Chromium 要 `--use-angle=swiftshader --enable-unsafe-swiftshader`
  才有 WebGL——带 `--disable-gpu` 时 FlatRenderer 构造抛错、台架早退，表现为
  「水合了但 step 恒 0」，与 §14 的 next dev 注记是两回事。软件 GL 下帧率极低
  （~4 协议步/s），锁定态等不起，包络与终态校核一律离线跑。

### 15.7 守门

`skin-dual.test.ts` 7 项：五段谱纪律（缓冲 ≥4、mid ≥ 合法下限，三档全查）·
冲突搭配拒收 · 台架序列（四同形对 + 一混排，键各不相同）· 两链各自锁定齐全且为
单结构两倍 · 解耦（默认档：锁定键集合逐位同 + 折叠体 ≤0.6px / 全段 ≤1.2px）·
**mid=29 对照带逐位相同（精确 0）** · 读得出是两个结构（两折叠体上下分离、
中间贴合段钉在芯上）。

**待用户真机拍板**：默认间距（现 16）/ gapX / RATE / 机位。

### 15.8 过渡组：发芽式被否 → 捏分式线稿（2026-08-27，两轮草图）

**发芽式（首稿）已删**：草图二首稿做成「第二个结构从上结构底下从无到有长出来」
（顶端锚定、kMax 逐级加键），用户看图否决——「不要这种发芽式的，要我画的这种渐变的」：
第三张草图 = **捏分**（单个加高方箱 → 面中央凹谷逐级加深 → 两个方箱）。
`buildDualTransition` 及其守门已从 src 撤下（统计条 379→377），两条机理留档仍有效
（小结构+长缓冲=轴向位置欠定 ⇒ 顶端锚定；评平滑用剪影口径）。

#### 捏分的四版试错（scripts/skin-dual/split-draft.mjs，机理全入档）

1. **双向 rb 插值**（两套键谱叠着开、rb 各自向对方端点的实测自然距离插值）：
   硬约束系统不能叠加两套不相容的形——中段互相打架绞成一团、拉链卡死。
2. **母箱+双塔（两端配对梯 + 键桁架）**：桁架键要**锁上**才起作用，而拉链按跨度
   放行、宽键晚锁一卡全卡；sq=0 下大捕获被 PRESS 吹成气球（实测 75–105px）。
   键当杆用（Lab.03 斜杆先例）在这台引擎上行不通——**形来自拉链的形成序，
   不来自终态约束集**：形状在宽键锁完的前 200 步就定了，晚锁的整形键救不回来。
3. **全嵌套单链**（母箱居中扇 + 塔扇嵌面 + 嘴角锚键 + 壁弦键）：终态约束齐了
   照样成袋。另一条基本面实测钉死：**大嘴浅箱只存在于 boxSquare 之下**
   （目录 sq=0 形态嘴全 ≤40px；76px 嘴无嘴角约束必然吹弧），而 boxSquare 的
   flatten/嘴角贴轴假设「一条链 = 一个箱」，复合链用不了。
4. **三箱（定稿候选）**：回到引擎被证明的区制——每条链都是嘴在轴上的正规扇形
   + boxSquare。[方箱 A | 发丝缝 | **窄嘴深箱 M** | 发丝缝 | 方箱 B]：凹谷就是 M
   （嘴 4px 恒在轴上，深度 = 壁长），A/B 全程目录方箱一字不动；M 逐级变浅、
   塌平后中段只剩压紧的缝材料 = 终态双方箱。级表按实测深度曲线标定
   （可控区间 dv 0–11 = 35.2→10.3px，dv≥12 塌平；dv9 非单调跳过；BUF=2 时
   dv0 数值发散、BUF=1 反而全锁干净）。9 级全锁、箱深稳定 36.5–37.9、
   剪影相邻 0.74–10.52（大步 = 开缝那两步）。

#### 词汇边界（重要结论，入档）

**悬空半深谷（单腔捏成双腔、谷底不触轴）在冻结的键谱词汇内做不出。**
多链构造里每个腔的嘴必须回轴（mouth-hug 语义），单链复合谱又造不出方角大嘴——
四版试错从不同方向撞的是同一堵墙。要做真正的悬空谷需要交接件暂缓的
**皮-芯键（tunnel 机制）**：一根「皮点 ↔ 芯上定距」的键就能把谷底钉在任意深度。
是否为此解禁引擎扩展由用户定。三箱版的代价 = 箱间两道切到轴的发丝 V 缝。

**流程回正（用户 2026-08-27 当轮指示「先只做这两条线之间过渡形态组，先把线是
什么形态确认下来」）**：目标形态与实现路径解耦——先用**纯几何线稿**（不跑引擎，
`scripts/skin-dual/line-draft.mjs`）把过渡的线形定下来：外包络恒定（H74 × D40
圆角矩形），面中央挖圆角凹谷、谷深逐级 0→D 触轴即分成两瓣；谷口宽做三档
（8/12/18）供拍板。线形确认后再回头选实现（三箱 / 皮-芯键 / 其它）。
线稿工具：目标形态 = `line-draft.mjs`；引擎实现探索 = `split-draft.mjs`（四版注记）。

#### 目标线形三轮（用户逐轮纠偏，2026-08-27）

- **v2「谷口应该更宽——本质是上下两个台，不是一个大台中间挖个坑」**：参数化整个换掉。
  单箱 = 两个 LOBE 高的台合在一起；过渡 = 两台**拉开**（缝嘴领先、缝深随后），
  终态 = 两台 + 宽缝，**总高随缝一起长**（台的尺寸全程不变 ⇒ 读作「长开」而非「切开」）。
- **v3「上下两个台更薄一些」**：每台 32 → 22。**v4「上下再薄一点」**：22 → **16**
  （副行台高三档 12/16/20 供拍板；缝恒 28 ⇒ 缝比台自身还宽，读作两块薄板）。
- **v4「做好过渡」= 节奏按视觉差量等分**，不再手调幂函数。可行的前提是本形族
  **横向凸**（每个 y 处材料恰占 [0, xmax(y)]）⇒ 剪影 xmax(y) 完整决定形状，
  两形的 L1 积分就是两个区域的对称差面积。另外把缝做成**有锥度的裂口**
  （尖宽/嘴宽 0.4→1：早期窄楔、末期平行缝）。

**两个测量坑入档（都表现为「形没问题、数字瞎报」）**：
① **弧长重参数化在这里不成立**（首版步长比 3.37×）——它累积的是路径长度，而形族
   在形状空间里是弯的：同一个 y 处的轮廓先被台占据、再被裂口吃掉，逐桶非单调
   ⇒ 路径长 ≫ 相邻直接距离。改为**松弛迭代**直接均衡相邻距离。
② **剪影必须按线段光栅化，不能按顶点打点**——折线密采样步长 1px 而分箱 0.1px，
   逐点写只填满十分之一的桶、其余留 0 ⇒ 剪影成「梳齿」，两形梳齿错位产生巨大
   伪距离（实测把 t 0.423→0.441 的微变量成整程量级，松弛因此收敛到荒唐的级表）。
   修正后步长比 **1.01×**（10 级：t = 0 / .108 / .253 / .397 / .523 / .637 / .741 / .834 / .920 / 1）。

**v5 定版（用户 2026-08-27 拍板「台高 12 做吧」）**：每台高 **12** · 台深 40 ·
终态缝 28 · 10 级 · 相邻视觉步长比 **1.02×**
（t = 0 / .108 / .254 / .397 / .523 / .638 / .741 / .834 / .920 / 1）。
比选副行撤掉——`line-draft.mjs` 输出的这张就是**目标线形本身**，引擎实现照它对。

#### 引擎实现：目标线形定版后的第二轮攻关（2026-08-27，仍未通过）

线形定版（台高 12）后按它重攻实现，`scripts/skin-dual/split-engine.mjs`。

**新机制（本来最有希望，实测否决）——「贴合锚键」**：键的一端可以落在自由段之外的
贴合段节点上；贴合节点每迭代被 `pinGlued` 钉回 (0, ys)，而锁定键对两端对称施力
⇒ 净效果 = 把皮上那个点拉到「离轴上某定点距离 L」的圆上。**这是既有键型**
（bond 的下标越界到贴合段即可），不需要解禁皮-芯新键型。用上下两条等长锚键
三角定位，理论上能把裂口尖钉在半深处。两轮实测都不成立：

1. 锚键跨度夹在包络扇形中间 ⇒ **拉链卡死在它这里**（同一条链里只有「当前第一条
   未锁的」有资格锁），它后面的键一条都锁不上（实测 8/14）。
2. 把锚埋深调成恒为最大跨度（先钉尖、再让箱体成形）后，尖确实钉住了（实测尖 = 0），
   **但箱体不再成形**：2·dv 的富余材料要向内折，而 PRESS 恒向外推，整条散开
   （实测高 100+ / 深 56，目标 52 / 40）。

⇒ **原判定加强而非推翻**：缺的不是「一个向内的锚」，是**整套向内的体制**。
键只锁距离、不定侧；PRESS 恒向外；富余材料的折向由拉链的形成序定，而形成序里
没有任何一步是朝轴的。这就是悬空半深谷做不出的根。

**三箱在定版比例下重做**（`split-engine.mjs` 定稿模式）：台 A/B = 窄嘴深箱
（嘴 12px = 定版台高、深 40px、等长键），M = 裂口箱（深 D−dv，随级 43 → 0）。
物理健康——**全级锁定齐全**（33/33 … 22/22）、台高实测 12.4–12.8（目标 12）、
台深 43–45（目标 40）。**但形态读作三片鳍**：M 的嘴也必须回到轴上
（boxSquare 的 mouth-hug 对每条链都成立），所以中间不是悬空的裂口，
而是「第三片鳍 + 两道到轴的发丝缝」，与定版目标不是一回事。
（另：t≥0.83 时 M 塌缩不稳，中段出现自交。）

**结论 = 一个待用户拍板的岔路**（模型不替用户破交接件的冻结）：
- **甲 · 解禁皮-芯键（推荐）**：加一个默认关的单元级选项，把指定皮节点的离轴距离
  约束到给定值（`coreWall`/`rootHug`/`anchorEnd`/`boxSquare` 已有四处同款先例，
  Python 对照守门不受影响）。裂口尖直接可钉，定版线形整族都能做出来。
  代价：动了交接件「暂缓项」的一条，需用户点头。
- **乙 · 接受三箱近似**：能做、全锁定，但读作三片鳍 + 两道发丝缝，不是定版线形。
- **丙 · 改线形迁就引擎**：把裂口改成全程切到轴（只有缝宽从 0 长到 28），
  每一级都是合法的两箱态、随时可做；代价是早期几级两台已经分开，
  与草图「一片先裂开」的读法不同。

#### 皮-芯键解禁 + 首轮实装（2026-08-27，用户拍板「解禁吧」）

**引擎加法式扩展 `SkinUnitOpts.coreTether`**（`[节点下标, 半径]`）：该皮节点
**不得越出**这个离轴距离。**单侧**——与 `coreWall`（皮不得内穿 x≥0）正好是一对镜像；
默认 undefined = 不启用 ⇒ 默认路径逐字不变（守门：给 undefined / 空表与不传
**逐位相同**；Python 对照 29/29 全绿）。全套 377→**379**。

**为什么必须单侧**：双向硬钉先试并否决——被钉的点确实到位、点与点之间的材料
却绞成死结（中段自交成环）。**这是本项目第三次「量对了、形错了」**：读数
（尖的离轴、缝宽、台高）全部命中目标，图却是一团环。单侧限位下材料是被 PRESS
自己压上去的、且可沿限位滑动，形状仍由物理找平衡。

**构造现状（6/10 级可用，中段 4 级未过）**：一条链 = 等长键包络箱 + 尖那一段的
平限位。已解决的三件：
- 裂口尖**精确落位**（32.3 / 26.8 / 21.6 / 16.7 / 12.1 / 7.8 / 3.8 / 0.0，逐级命中）；
- 端面材料按裂口**四段**分账（[台立面][壁][尖][壁][台立面]，壁长按斜边算），
  否则尖宽 wt 被算进台的立面、台高随之涨到 26px；
- **不能放裂口嘴键**：boxSquare 的顶/底面压平取「跨度最小的键」当端点，
  裂口嘴键正好最小 ⇒ 会把台的立面和顶面拉成一条斜线（实测深 51 vs 40）。
  去掉后最小跨度回到端面梯档，压平只作用在顶/底面。
- **壁不能给逐节点斜坡限位**：那等于强迫材料的落位，材料沿限位滑几节就被自己的
  天花板顶到错处，中段整条打旋。

**仍未过**：t≈0.40–0.74 四级中段打旋（端面 80px 的富余卷起来）。两端各三级
（单箱侧 t≤0.25、双台侧 t≥0.83）已经干净且读法正确。下一步方向：给裂口本身
一副**渐变梯档**（把裂口当成一个「嘴宽 w、深 dv」的小箱），但要先解决
boxSquare 的压平只认「一条链一个箱」这条——它取最小跨度当端点的做法与
「链内嵌套第二个箱」不相容。

#### 逐级独立设计定案：刻缝方箱 v3（2026-08-29，用户「针对每一个结构单独做结构」+「按你给的继续做」）

上一节「构造现状」整体推翻重来——用户否决单参数生成器路线（「目前还是乱的…
完全不可用」），改为**每级一份自己的谱与选项、单独磨到干净**（`scripts/skin-dual/levels.mjs`，
带单级大图 / 成形快照 / 配方扫描三种模式）。四轮架构迭代后定案：

**架构（每个中间级）**：外箱梯（完整等长键扇 rest=H=2·台高+缝嘴，boxSquare 全套）
+ **缝链**（嘴键 wm + 等长壁键 + 底键 wt，自己拉链嘴→底）+ **面角同侧键**×2
（面角↔缝角 rest=台高，各自单键链——垂直化把两角拉到同一 x = 面竖直的硬锚，
且面板端点成为锁定键 ⇒ 既有端面硬投影自动激活，零新整形代码）+ coreTether
（缝底钉 rTip + 缝壁斜坡单侧上限）+ 三面板（两半面 + 缝底）。

**两个引擎加法式扩展**（默认路径逐位不变，各有守门）：
- 自由段谱第 5 元素 = **附加链**：缝键混进外箱链会被台面找平（核心 v7 行为，
  i 侧全跨 y 均值）把面和缝一起压毁——「一段一链」是移植格式的产物不是物理决定。
- `sqChains` / `levelChains`：boxSquare 的作用域拆分——外箱链吃全套（嘴角贴轴 /
  flattenToLine 对缝链是破坏性的），缝链经 levelChains 只吃「底面找平 + 端角重申」
  （补 v7 只找平上层的不对称：下缝壁锯齿即此；端角重申让缝宽逐级精确命中目标）。

**三条机理入档**：
- **缝料的去向在成形期就要有约束**：缝链靠吸引在 step 250 前就预锁成刚性手风琴
  （快照实测锁 9/9），随后被 PRESS 外翻成兜、箱合拢时关在台外——单试键框架
  （外翻/气球）、单试斜坡限位（S 折绞缠）都失败，**键框架+斜坡组合才成立**；
  上一节「壁不能给斜坡」的结论在有键框架后不再成立。
- **缝料要配平不能富余**：coreTether 单侧、拦得住外翻拦不住过深——壁弧富余
  会把缝底往里顶（L3 实测底比目标深 3.7px），逐级裁 wallN/a 是主要调参轴。
- **验收只认剪影Δ**（引擎终态 vs 目标线在同一 y 网格的平均横向差，按线段光栅化）：
  尖/深/缝/锁定数四项全对时形仍可能全错（第四次「量对了、形错了」）。

**定案成绩**（对 08-27 拍板的 v5 目标线，台高 12 / 深 40 / 终态缝 28）：
L0–L9 剪影Δ = 0.33 / 0.48 / 0.88 / 0.88 / 1.29 / 2.93 / 3.25 / 4.77 / 5.54 / 4.38px，
十级全锁、无绞缠无气球。浅缝级（L1–L3、L5）斜坡+稀键，深缝级（L4、L6–L9）
斜坡+壁键；**L9 也走刻缝方箱**（dv=40、缝底钉在轴上=裂到底，与 L8 同族连续）
——五段谱双台构造实测 Δ17（缝区过高+折料）弃用，代码留作对照。深缝级残余
±2px 织物波纹与嘴角圆化是引擎本性（目录台架同款），非缺陷。

**待用户拍板**：线稿条（levels.svg）十级形态；通过后才排 3D/台架接入
（08-20 纪律：先线稿确认再上 3D）。

## 16. 方法纪律：给引擎做目标形态（2026-08-29 立，源自捏分过渡四轮返工）

> 用户拍板「以后再做类似工作要直接用这次的经验」。适用范围：一切「让 skin 引擎
> 长出一个指定形态/形态系列」的工作（新目录形态、过渡组、变体族都算）。
> 这轮的完整案例与机理在 §15.8；本节只留可直接照做的操作纪律。

### 16.1 动手前：硬机制审计（本轮最大的教训）

**这个引擎里凡是「方 / 直 / 平 / 宽度精确」，全部来自硬机制；软平衡（键长 +
PRESS + 弱直化）的输出永远是圆的。**目录里每一个方角都是 boxSquare 硬投影给的，
没开它之前阶梯挑台自己也是圆团。所以第一步不是写谱，是列表：

把目标形的**每个可读特征**（每块平面、每条直壁、每个方角、每个精确宽度）
逐一指给一个硬机制；**指不出来的那个特征，就是将来失败的地方**。可用的硬机制
与其触发条件（一个都不能想当然）：

| 硬机制 | 给什么 | 触发条件 |
|---|---|---|
| 嘴角贴轴（boxSquare） | 链的内面在轴上 | 该链在 sqChains 内 |
| flattenToLine 顶/底压平 | 外壁直 | 链尾键已锁定，且链吃全套整形 |
| 端面硬投影 | 面直且弦=弧 | **面板端点恰好是锁定键** |
| 梯挡垂直化 | 键对两端同 x | 键已锁定（全局，不分链） |
| 台面/底面找平 | 水平壁平 | 链内锁定 ≥3；i 侧核心自带，j 侧要 boxSquare/levelChains |
| 等键长分段直化 | 壁直 | 同链内 ≥2 根**等长**键连排 |
| coreTether 单侧限位 | 特征不越出某半径 | 已解禁；只拦外翻、**不拦过深** |

推论一：**特征角点必须是某根键的端点**——不然上表前四行全部不触发。本轮的
「面角同侧键」（面角↔缝角，rest=台高，独立单键链）一根键同时激活了垂直化、
端面硬投影、面板锁定键三件事，**零新整形代码**。设计新形态时优先用这一招：
补齐既有机制的触发条件，而不是写新整形。

推论二：链机制假设「一条链 = 一个单一梯子」（台面找平做 i 侧全跨 y 均值）。
两套键谱混一条链必互相压毁——**分链**（附加链，谱第 5 元素），再用
sqChains / levelChains 给每条链配它该吃的整形子集。

### 16.2 成形期：材料的去向要有约束

形状来自拉链形成序，不来自终态约束集（§15.8 老结论，本轮第三次验证）。新增
材料区（如缝料）在箱子合拢**之前**的去向必须被管住，否则 PRESS 替你决定
（外翻成兜 / 吹成气球）：

- **键框架 + 单侧限位是组合拳，单用皆败**：只给键 → 预锁成刚性手风琴被甩到
  错侧；只给限位斜坡 → 材料被半路按住、周围继续飞，S 折绞缠。两个一起给
  （键管有序、限位封死外翻）才成立。此前档案里「壁不能给斜坡限位」的结论
  **只在无键框架时成立**——单方案的失败结论不要外推到组合。
- **材料要配平**：单侧限位拦不住「过深」，富余弧长会把特征顶离目标位
  （缝底被顶深 3-6px）。修法是**裁材料**（wallN/a），不是调限位。
- 同一架构在不同参数区间最优配方不同（浅缝稀键、深缝密键）是常态不是缺陷，
  逐级独立调参本来就是干这个的。

### 16.3 验收与调参的三件工具（levels.mjs 已内置，直接复用）

1. **剪影Δ是唯一有效分数**（终态 vs 目标线在同一 y 网格的平均横向差，按线段
   光栅化）。尖/深/缝/锁定数四项全对时形仍可能全错——本项目已四次
   「量对了、形错了」。人肉看图前先看它，扫描按它排序。
2. **配方扫描（sweep + COMBOS 注入）**优先于手动一次一调：±1 盲扫没方向时
   立即停手，换快照模式。
3. **快照模式（成形过程六帧）是破案工具**：终态图看不出「什么时候坏的」。
   本轮靠它一眼看出缝链 step 250 预锁 + 外翻，此前三轮盲调全部白费。

另两条实操坑：绘图平滑会说谎（±3 节窗口抹平 12px 特征——判断形态前先降到
w=1 看原始折线）；每轮实验必须渲染看图，读数只做筛选。

4.（2026-08-30 追加，当日按 §15.12 的复查改写）**过程诊断（process.mjs）**：
   逐步位移表（基线 = 无事件段中位数，尖峰 = 猛动）+ 锁定时间线 + 全程逐帧
   （不只尖峰窗口）。**过程质量的铁律，按血的教训写**：
   - **过程验收的对象是渲染出来的中间形态，不是速度指标**。速度、自交环、
     终态Δ 全绿而过程乱七八糟已实际发生（§15.12）；「每步位移 ≤ 限速值」
     这类用改动自身保证的读数是循环论证，不算验收。
   - **看图三要求**：全程密帧（不按尖峰挑帧）、不加绘图平滑、从暴露问题的
     视角看（3D 台架必须含轴测/透视，侧视会被带子自身遮挡骗过）。
   - **动过程之前先定义合格基准**（合格的成形长什么样、拿哪台已拍板的件当
     对照）；没有基准的「顺滑化」只是在丑轨迹上调时间表——调快 = 啪，
     调慢 = 把丑摊开。**中间形态是设计对象**，与终态同等，走线稿→拍板流程。
   - 时序类改动（lockGap/stepClamp/warp）一律**重验终态**（剪影Δ + 锁定集合
     逐位比对）；限速不能裸用（成形期有材料竞速，全体拖慢会到达另一个平衡态）。

### 16.4 流程

线稿三步走不变：纯几何目标线拍板 → 逐级引擎实现（每级独立谱+TUNE，参考
前后但不从前后改出来）→ 看图拍板，过了才排 3D。被用户判「不可用」时，
**先写「方案哪里不足」的逐条审视再动手**——本轮五条不足对应五个修法，一轮
返工到位；此前跳过审视的三轮全部报废。引擎改动一律加法式 + 默认路径逐位
不变的守门测试。

### 15.9 上站：模块 + 守门 + Lab.12（2026-08-29，用户「把这次的一组工作做完然后推上去合并」）

定案的十级从脚本搬进站内模块 `src/lib/space/skin-split.ts`（**脚本与站上共用同一份实现**——
留两份必然漂；`scripts/skin-dual/levels.mjs` 从此只做测量与画图，三种模式不变）。

**对位构造换了一副**（这是搬站时唯一的实质改动）。先照 Lab.08 那副「全员同 LEAD/FREE、
结构居中」做，实测**十级剪影Δ 全线变差**（L1 0.48→1.76、L5 2.93→9.40、L8 5.54→20.11 直接崩）：
浅缝级的 M 只有 24，塞进同一个 FREE=107 的自由段等于给它 25 节多余缓冲，
skin-array 里「自由段越长、富余越多、残留越大」那条在这里被放大成了形态事故。

改用**固定尾段 + 顶端配平**：收缩注册在底端，缝心离下缘的账只有三项——
尾段贴合 ·2px + 下缓冲 ·2r px + 该级自身的半高 H/2（中间 M 节材料的弧长全被折叠吃掉，
对高度不贡献）。于是尾段钉死 69、两端缓冲恒 4，配平量全放进顶端贴合段（在结构之上，
不参与高度账）。自由段回到各级调好的自然长度 ⇒ **形态逐位复原**（Δ 0.33–5.54 与定案相同，
证实形态对整体平移不变），而**底边全程齐平**（实测散布 4.8px），顶边逐级抬升 28.7px
≈ 终态缝宽——正是「上台被推开」该有的量。

**守门 9 项**（`skin-split.test.ts`，十级引擎跑满 ≈14s，beforeAll 一次共用）：
① 硬机制的触发条件——**每块面板的两个端点都必须是某根键的端点**，这条一破端面硬投影
就静默失效、形态回到软平衡的圆团（§16 的第一推论，本族的命门）；② 对位构造（三段和恒定、
尾段与缓冲全员同值）；③ 键谱纪律（外箱等长键 + 扇形一步不缺、缝链键长嘴→底不增、
面角键 = 台高）；④ boxSquare 作用域（sqChains/levelChains）；⑤ 皮-芯键单侧且不越出台深；
⑥ 十级全锁无 NaN；⑦ 逐级剪影Δ 阈值 + 尖落位；⑧ 缝宽单调、底边齐平、顶边随缝抬升；
⑨ **相邻级形态距离无断层**（实测 2.42–3.42，比值 **1.42×**——比 Lab.08 定版 2.6× 与
环上渐变 3.7× 都匀）。

**Lab.12 台架**（`components/lab/SkinSplitBench.tsx`，SkinSolidBench 薄封装）：十条带沿 X
排开 gapX 80、通长天花、**芯轨 `rail='fixed'`**（各级自由段长度不同 ⇒ 顶端下沉幅度不同，
固定立杆让它读作「布沿杆往下聚」而不是「带子脱开天花」）；RATE 80。机位按实测像素
包围盒定（pivot 382/280 · camScale 0.60，四边余量 153/163/50/52）。

**顺手修掉的既有隐患**：`skin-unit.test.ts` 那条 boxSquare 分级用例实测 5.8s、正卡在 vitest
5s 默认预算上，套件并行负载一高就超时（加进第十二台后当场复现）——给了显式 60s 预算。
统计条 379→**391**、Demos 11→12。

### 15.10 并拢排布 + 收缩全程连续（2026-08-29 第二轮，用户看图指定「平着排成一列 如图所示」+「确保收缩过程里也是平滑的过渡连续形状」）

用户给的参考图是 Lab.08 的**并拢**排布（切片沿深度密排成一条连续体，侧视读剖面渐变）。
Lab.12 照做：带深 15.6（Lab.08 同款 0.6×）、gapZ 16.5、`ceiling='span'`、芯轨固定立杆，
两档排布（并拢默认 / 分列），切换只改渲染偏移与机位、不重建引擎。

**真正的活是第二句话。**终态本来就匀（相邻剪影距离 2.1–3.3，比值 1.4×），
但**过程中不匀**——实测 step 400 相邻Δ 最大 12.9px、比值 9.4×。两个独立病因：

**① 成形时刻不齐（时序问题）。**每级的拉链是「一瞬间全锁」，而这一瞬发生在
各自不同的 r 上（键长 rb 与跨度 f 的比值定的，实测十级 571→726 步）。十条带同推时，
画面上总有一段已成形、一段还是直带子，**那道边界扫过整排**就是用户看到的断层。
修法：引擎加法式扩展 `warp`（收缩时间曲线指数，`r = R0 + (r1−R0)·u^warp`，两端不动
⇒ 收缩终点与协议长度都不变），逐级取 `warp_i = ln(自然全锁 u_i)/ln(对齐点 u*)`，
把各自那一瞬搬到同一个 u。实测成形时刻 571→726 收成 **637–648 步（差 11 步 ≈ 0.14s）**。
**意外收获：warp 完全不影响终态形**——十级剪影Δ 在 u* = 0.68…0.88 全程逐位不变
（滞回没咬：换条路径到达同一个 r，锁定集合仍相同），于是它是一个**免费的过程质量旋钮**。

**② 缝底料被裁太狠（结构问题）。**L4/L5 在成形期把缝料绞成**肉眼可见的死结**
（自交环夹住 35 / 42 节）。根因：缝底键的静止长度是 wt，而它跨的 2a 节材料只有 4a px
——逐级调参时为压住缝底深度把 `a` 裁到自然值的一半（L5 用 2、按料量该是 4），
底键就把 8px 的布拽成 16px 的直线，四周松料被这股劲揪成环。修法是把料还回去并用
wallN 重新配平：**L4 a=3/wallN=6、L5 a=3/wallN=9**，最大环 35/42 → **3/2 节**（织物褶皱，
绘图平滑就盖住了），代价是终态Δ 1.29→1.92、2.93→3.26。

**判据的教训（两条）**：
- **结要按规模看，不能只数个数**：2–3 节的自交是褶皱、看不见；35 节的是死结。
  按个数扫会把两者混为一谈，也会把「有几个褶皱」的方案错判成失败。
- **瞬态要密采样**：结只在成形窗口出现，稀采样（每 200 步）漏过一次，
  差点把 L4 判成已修好——守门里改成每 50 步采一次。

**并拢排布的对位坑**：试过「按终态芯长配平顶端贴合段」让十片顶端在终态齐平，
实测反而把台架落位打散到 68px——台架按**末节点初始位置**对位（footAlign），
总长一变，初始位置就不齐了。故带总长仍全员 200 节；代价是顶端读成一道缓坡
（下缘齐、结构齐，只有顶端不齐 70px），那是「收得多的带子短」的老实结果。

守门 +3（成形时刻散布 ≤30 步 / 最大环 ≤6 节 / 五个检查点上相邻级距离都在带内）。
统计条 391→**394**。

### 15.11 顶端阶梯返工：配平垫构造 + warp 退役（2026-08-30，用户看真机「现在是这个效果，而且移动过程也不顺滑」）

上一节两件修正上线后用户看真机仍不过：顶端一道大阶梯 + 动作不顺。查下来
**两个根源都在上一节自己的决定里**：

**① 顶端阶梯 = 自由材料总量不等 + warp 各走各的 r(t)，两笔账叠加。**
芯长(r) = 贴合·2px + 自由·2r px。此前各级自由段长短不同（57→107 节）、贴合找补
总长——总长齐了，**收缩量**不齐，顶端随收缩越走越散、终态差 70px；warp 再让十级
中途各在各的 r 上，又添一笔瞬态 ~37px。修法分两层：

- **配平垫**（构造 v4，五段谱 `[贴合 8 | 垫 | 贴合 16 | 结构自由段 | 贴合 69]`）：
  每级 垫 + 结构自由段 = 107 恒定。垫是**无键自由材料**（随收缩变短——贴合不会，
  这正是此前的根源），富余弧长由 rootHug + boxSquare 均匀排布收拾在杆上；
  **垫夹在两段贴合之间**（第一版把垫直接贴结构上方，结构挂上软垫、垫越长垂得
  越多——L0 垂 26px，顶端齐了缝心又成新阶梯）；**隔离贴合取 16 节** = 全局约束
  最大跨距（双结构带 mid≥16 的老结论第三次复用）⇒ 结构的动力学环境与逐级定案
  **完全相同**，十级剪影Δ 原样复原（0.29–5.54）、缝心散布回 14.5px，不必重磨。
- **warp 退役**：锁定同步与顶端全程齐平在 r 这个旋钮上**不可兼得**（warp 的
  定义就是各走各的 r(t)）。实测不 warp 的过程连续性与对齐版同量级（相邻剪影Δ
  峰值 13.6 vs 14，锁后同收敛 ≤4），而顶端全程齐平、十次成形从「同时一响」变成
  **从左到右的有序级联**（自然时刻 571→726 与级序近乎单调——正好读作一道传播的
  裂开，动作摊开 2 秒，也就不再集体猛一下）。`warp` 留在引擎（加法式、默认
  逐位不变），「不影响终态形的过程质量旋钮」这条发现照旧成立、供后用。

**② 「移动过程不顺滑」的另一半就是 warp 的集体快门**：十级同一瞬间全锁 =
整面墙猛一下。级联化后逐级小响；其余帧间平滑（EMA）不动。

**判据教训（追加一条）**：对齐类性质要写成**全程量**——「终态芯长相等」「锁定
时刻相等」都各自成立过、又各自在全程视角下露馅（前者初始不齐被 footAlign 打散，
后者中途 r 不齐顶端开花）。真正稳的表述是「芯长在每一个 r 上逐级相等」，
它只有一个构造解：贴合总量与自由总量都全员同值。守门已按此改写
（级联窗口 ≤170 步 + L9 收尾 + 近乎单调；检查点连续性阈值不变）。

### 15.12 成形节拍+限速：上线当日被叫停回滚——「过程质量」四轮返工的总审视（2026-08-30）

用户「还是不够顺滑：从中间抽取一个，把它的变化过程抽取出来单独做顺滑，再总结
改法推广」。照做：抽 L5 逐步量位移（新工具 `scripts/skin-dual/process.mjs`，
已留作 §16.3 第 4 件工具），破案三类猛动——

- **外箱十颗键同一步全锁**（step 594，峰值 34.5 px/步 = 基线 0.30 的 114 倍）：
  吸引护栏早把全链键距备到 rb 附近，拉链只等最外键入窗；闸一开 55 迭代放行
  55 颗。全部整形机制（垂直化/直化/找平/硬投影）以锁定为开关 ⇒ 坯团 4 步变方箱。
- **无锁定的屈曲翻越**（23.3 px/步）：下缝壁材料被 PRESS 顶着、被自身折叠勾住，
  攒足了一口气翻到 ramp 限位上。
- **单键锁定触发的硬投影**（L3 29.6 px/步）：找平跨度随新锁定一步拓宽。

修法用了引擎两个加法式选项（lockGap 24 只圈外箱链 + stepClamp 4px），十级
剪影Δ/锁定集合逐位复原、每步位移全程 ≤4px、瞬态环 ≤4——**指标全绿，上线
当日被用户截图叫停**：「还是在出现这种乱七八糟的过程」。

**复查（抽 L5 全程逐帧、原始折线带键线、与上一轮并排对比）查明**：

- 节拍+限速把成形拖成了 **~3 秒的半成形皱巴态**——材料在已锁梯挡之间被逐段
  收拢，面上一排锯齿褶皱、s720 还有翻折卷；乘十条带级联铺开 = 截图里的乱。
  上一轮版本则是 s560 前卷团、s594 一步「啪」成干净箱——丑 0.05 秒 vs 丑 3 秒。
- 截图里拉长的橙线不是键被拉伸（实测锁定键拉伸比全程 ×1.0），是半成形箱的
  梯挡在 3D 透视里挂着晃——旧版里它们只在箱成形完那一刻出现。
- **根子**：这些单元的**中间形态从来没有被设计过**。十级的终态逐级磨过、
  用户拍板过；「成形到一半长什么样」没有任何目标，引擎自然给出的就是
  「先卷成团、再折叠」。前后四轮（warp 对齐、自然级联、节拍、限速）全在调
  这条丑轨迹的**时间表**——调快 = 啪，调慢 = 把丑摊开给人看。

**验证为什么四轮都没拦住（自查四条，进 §16）**：

1. **判据循环论证**：限速之后验「每步位移 ≤4px」——这是限速自己保证的，
   等于用改动验改动。
2. **指标盲区**：速度、自交环规模、终态剪影Δ 没有一条度量「中间形态整不整齐」；
   锯齿褶皱与翻折卷不触发任何守门。
3. **视角与形式错位**：3D 只截默认侧视（带子边缘朝屏、互相遮挡，皱巴看不见，
   用户转到轴测才看见）；2D 帧带加了绘图平滑、按速度尖峰挑帧。
4. **没有合格基准**：从没定义「成形过程在屏上长什么样才算合格」，也没拿
   已拍板的台架当对照。

**处置（用户拍板 A+B）**：A = 当日回滚——SPLIT_BASE 撤掉节拍与限速，回到
自然时序（丑的时段最短、成形前后都是定形态）；lockGap/stepClamp 保留在引擎
（默认关、逐位不变，注释已改口径），process.mjs 与本节账目保留。
B = **中间形态设计**立为独立工作：先在一个单元上把「成形前材料怎么整齐地
待着、成形怎么一段干净地展开」当设计对象，线稿 → 看图 → 拍板 → 上 3D → 推广
（§16 流程原样适用于时间轴）。

### 15.13 成形过程设计定稿并接入（B 路线 v3，2026-08-30 用户「按你的建议」立项、逐轮线稿拍板、「推上去我看看效果」接入）

§15.12 的教训落成正事：把「成形到一半长什么样」当设计对象。动手前先写死
合格基准——**全程任何一帧单独截下来都像一个有意的形态**（无卷钩、无锯齿
褶皱、无翻折；成形段每帧都是「更深一挡的箱」）；评审全程用原始折线、
密帧（40–55 步一帧）、不按尖峰挑帧。四轮线稿迭代，每轮用户看图纠偏：

1. **逐挡长出**（引擎 `zipUp`，外箱链拉链反向）：v7 拉链从最大跨放行、而
   最外键最晚够得着 ⇒ 全链等它、一到全放（啪的根源）；反向后哪挡材料够了
   哪挡锁，箱体从面鼻逐挡向外长出（L5 锁定从「594 一步 ×10」摊成 237→578
   的连续生长），**终态 Δ 逐位不变**（同一批键换放行顺序）。配 **吸引近程门**
   （`attNear 1.5` 只圈外箱链）：远程吸引把等待材料拧成卷钩（鼓包想收成环），
   收窄后等待材料是干净斜坡。近程门单用被否（等待态变锯齿）；对缝链无效
   （浅缝出生即在锁定窗口内，只能推迟不能取消预锁）。
2. **缝区折痕待命**（引擎 `coreTetherRel` 相对单侧限位，用户指 step250
   「很多无意义的错误的形状」后加）：缝区材料按终态缝深配料、成形前无处
   安放，以疙瘩形式骑在斜坡上几百步——把它压在缝角所在的面之内，等待期
   读作面上一道折痕，箱面越过缝底限位后缝再从零逐步裂开（先见折痕、后见
   裂开）。**同侧规则是硬前提**：缝壁贴同侧缝角、缝心贴双角——首版每个
   节点贴双角，跨侧耦合把 L5 拖塌（Δ3.29→16.5、下半瓣塌掉），同侧后十级
   全保持且无需逐级豁免。
3. **L5 补壁键**（SPLIT_TUNE[5] wallStep0→2、wallN9→10，用户圈图「这一步呢」
   后修）：L5 是十级唯一「缝深无壁键」的级，缝壁成形期自由晃、中段缝口
   歪斜下垂（折痕 v1 塌它同根源）。五组合扫描 + 逐帧对比取 wallN10——
   wallN9 版前 230 步整带不动、放进整排读出「L5 独自滞后」。终态顺带更准
   （Δ3.29→2.13→排整齐后 2.00）。
4. **缝壁排整齐**（引擎 `alignRuns` 显式均匀排布，用户「干净的方案不应该
   出现锯齿」后加）：锯齿 = 键与键之间的富余材料鼓成的小包串（等待期是
   折叠纹、成形后是缝壁波纹），不是渲染毛刺。治法与根部缓冲料 rootRuns
   同一份机制（两锚点连线上等距排布——**排布是位置机制不是形态机制**），
   给缝角↔缝底角两条 run 排上后**锯齿清零**。

**十级验收（括号内 = 08-29 定案旧值）**：剪影Δ 0.29–3.72（0.29–5.54）——
全面更贴目标线，L9 4.38→**1.42**（旧偏差大半是壁纹，目标线缝壁本来就直）；
密采样瞬态自交环**全部 0**（≤5——褶皱环随锯齿一起消失）；全程任意时刻
相邻级最大距离 **5.4**（同步全锁 13.6 / 被否的拉链波 15.1，过程连续性历版
最好）；锁定数不变、末锁 561–685 全在纪律解除前、终态相邻比值 1.80×
（守门线 2× 内）。滞回披露：换折叠路径后终态不再与 08-29 定案逐位相同
（Δ 反而全面变小）。

**守门重钉**：Δ 阈值按 v3 实测 + ~1px、打结收紧到 ≤2、检查点连续性收紧到
7/5.2（原 14/4.5）、新增「四件套配置齐全且同侧/区段正确」守门（+1，395）。
引擎四个选项全部加法式默认关，Python 对照不受影响；Lab.06–10 一个数没动。

**方法记一笔**：这轮的每一件机制都是用户看线稿逐轮点出问题后加的（乱形 →
折痕；歪斜 → 壁键；锯齿 → 排整齐），没有一件是预先设计出来的——中间形态
设计的正确流程就是「渲出来给人看、按指出的问题找机制」，§16 的线稿纪律
在时间轴上原样适用。

### 15.14 对位构造 v4：居中对齐（2026-08-30，用户「所有形状都居中对齐，按照我们之前的做法」）

「之前的做法」= Lab.08 嘴心解析构造：不标定、不逐级凑数，把对齐写成构造
性质、全程成立。落地 = **七段谱**：

    [杆帽 8 | 半垫 | 隔离 16 | 结构 | 隔离 16 | 半垫 | 尾 53]

**配平垫劈成相等两半放结构两侧**（free 恒奇 ⇒ 107−free 恒偶 ⇒ 两半精确
整数、零取整误差），各隔 16 节贴合（全局约束最大跨距，第四次复用 ⇒ 结构
动力学环境一字不变，十级 Δ 与 v3 同层 0.25–3.71）。居中的构造论证：结构
两端的锚都是贴合段（钉在芯上、位置解析），对称折叠体的缝心恒在两锚中点 ⇒

    缝心离下缘 = (尾 + 隔离)·2 + (下垫 + 缓冲 + 半结构)·2r = 138 + 114·r px

右边没有任何逐级量——下垫按 (107−free)/2 配平恰好消掉 free。带缘齐平照旧
（贴合总量与自由总量仍全员同值）；底边齐平让位（两台对称上下分开，各
~缝/2，这正是居中的读法）。

**残差的物理与量化下界**：实测缝心散布早期 1.7 / 成形波峰 5.5 / 终态 3.3px
（对比 v3 底边齐平口径下缝心系统性差 ~14px = 缝宽/2）。残差 = 折叠体在
自身松弛区间里的**重力落位差**（锚间距 = free·2r 由芯布局定，折叠体高度
< 松弛量，落位靠重力）——是形态量不是放置误差。补偿实验做过并否决：
按 H/2 贴合转移只能「终态准、早期差 14px」（转移量 2px/节 r 不变，而要
补的量随缝张开才出现），全程指标比现构造差，不取——「对齐类性质要写成
全程量」的教训第三次生效。

守门重写：七段谱构造、两半垫相等且无键、**缝心散布逐检查点 ≤6.5px
（全程量）**、对称张开（顶边逐级升 + 底边逐级降）。验证图的坑记一笔：
**对齐验证图必须用公共 y 基准**（此前线稿逐帧按 yMin 归零——恰好把要验
的东西归掉了），本轮新图以带底为零点、画十级均值线。

### 15.15 绘图平滑的放大 bug：九个刻缝级一直被放大 1.5 倍画着（2026-08-30，用户「最左边的没同步啊」）

居中构造上线后用户看真机指出最左边那条不同步。**不是没同步，是渲染缩放错了**：

`renderSmooth`（v7 render_smooth 的移植）的窗口是 `half = w>>1` 的对称窗，
一共采 `2·half+1` 个样本，却**除以 w**——奇数 w 恰好相等（正确），
**偶数 w 多采一个样本却少除一份 ⇒ 整幅放大 (w+1)/w**。全站只有 Lab.12 的九个
刻缝级用偶数窗口（`smooth:[2,1]`，当初写它是想「窗口窄一点别把缝抹平」，
而 `2>>1` 与 `3>>1` 都是 1、窗口根本没变窄，只是把归一化撞出了 1.5×）；
同台 L0 用 [3,1] 是真尺寸，于是它看起来小一圈、短一截、位置不对。

**证据链（先量后改）**：① 物理侧十级 node0/foot/带高在每一步逐位相同、
`footAlign` 偏移全 0 ⇒ 不是对位问题；② 台架「透视」本来就是关的（正交）
⇒ 不是投影错觉；③ 分列 + **正视**（正交、无深度错位）逐列量像素：L0 底 650、
其余九条底 882，比值 (882−180)/(650−180) = **1.49** ≈ 3/2 ✓。

修正两处：引擎按实际采样数归一化（奇数窗逐位不变 ⇒ Lab.06–11 零影响）；
skin-split 的窗口改 [3,1]（与 L0 及全站一致）。修后十级在正视图里逐条相同
（顶/底/高逐位一致，结构中心散布 ≈1px）——居中对齐在真机上成立。

**连带**：机位是按「被放大的带子」框的——那时带子垂到芯轨下缘之外把画面撑满，
修好后装置回到真尺寸（天花→钉住点 387 世界单位），取景空出四成，故按真实
包围盒重取：并拢 pivot(45,200)/scale 1.0、分列 pivot(400,205)/scale 0.76
（实测两档均无裁切、居中）。守门 +2：引擎「平滑不改变尺度」（常量场与线性场
逐位复原，w=1..5）、Lab.12「十级同窗口且为奇数」。统计条 395→397。

**教训**：`§16.3 绘图平滑会说谎` 此前只当它是「抹平特征」，这次是**改变尺度**
——同一族里只要有一台用了不同窗口，就该立刻怀疑归一化。判据仍是老规矩：
用户说「看起来不对」时，先在**正交、无遮挡**的视图里逐像素量，再动手。
