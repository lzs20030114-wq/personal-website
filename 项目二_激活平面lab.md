# 项目二 · 一个人走过（Lab.14，激活平面）

> 立项：用户 2026-09-04「现在我们做下一个 lab……我们这个项目本质上是要做人在空间中出现不同的行为，
> 然后我们这个结构就从天花板上下方下来，在人猫交互带上形成结构」→「这一次的 lab 可以先做平面，
> 就是研究一个人在空间中运动时，这些单元哪些被激活」→ 同轮纠偏「每一个单元其实都要再小一些，然后是
> 触发一群单元围绕着人形成空间，而且为了这个空间的多样性数量肯定是要大于 4×4，甚至 8×8」「空间的变化
> 是要围绕人的行为（不是字面意思的围绕）」。
> 本文件 = 这台在项目里的位置 · 模型的三件机制与它们的出处 · 线稿读出来的结论 · 简化与边界 · 待拍板。
> 数字以 `src/lib/space/unit-activation.ts` 与其守门为准，这里不复制成第二份真理。

## 0. 一句话

Lab.06–13 全是空间层（单元长什么样、怎么排），输入全是编排；这台**第一次把行为层接进来**：一个人在
Lab.12 那间房的平面里走动，地面记下他的存在，每个单元读自己脚下那片地面，读满就成形、成形不回退。
问的只有一个问题——**人这样走一遍，哪一群单元被激活、这群单元连成什么形**。平面、不做立面；单元按
密度缩小（同一块场地摆 4×4 / 6×6 / 8×8，默认 8×8）；成形对象是「一群」不是「一个」。

## 1. 它在项目里的位置

- 案例页「方法」节：行为层（人 / 猫，规则取自实证）与空间层（天花吊件，规则自拟）之间**唯一的信息通道是
  痕迹**——痕迹随使用加深、随闲置消退，空间因此**滞后**于身体。这台把这条通道第一次做成看得见的东西。
- 交接件「下一步任务」的 1–2（阵列化进房间、验证形态命中交叠带）在 Lab.10–13 已做；第 3 条「行为矩阵 →
  键谱/ℓ 的翻译规则，**由使用者手写**」——这台**止于「激活程度」**（哪些单元、多深），不选形态，翻译表
  留给作者。
- 「从天花板上下来」的画面已经有：收缩注册端反转（08-22）后带子顶端沿固定立杆下滑、形态在下面长出来。
  这台缺的不是下降机制，是**谁来决定它收缩多少**。

## 2. 三件机制，全部来自作者自己的原型

作者 2026-07-20 的第一个脚本（项目二_工作日志原稿.md）：20×20 网格上五个点，每拍在落到的格子上加一份痕迹，
所有痕迹每拍衰减 2%，某格痕迹超过 15 就固化、从此不可进入。这台照搬三件事，只给它尺度：

| 机制 | 原型 | 这台 | 旋钮 |
|---|---|---|---|
| 痕迹 | 落到的那一格 +1 / 拍 | 人在场的每一秒，**影响半径**以内的每块地面（0.1 m 格）各记 1 秒的存在——「那一格」放大成一个圆 | 影响半径 0.22–1.2 m，默认 1.0 |
| 衰减 | 每拍 2% | 两条路：**指数**每秒 2%（原型，半衰期 ≈ 34 s，模块默认）／**封顶 + 线性褪去**（台架，见 §2.1） | 「散掉」2–60 s（台架默认 **6**） |
| 阈值 | 超过 15 固化 | 一个单元脚下地面**平均**记满阈值即下来 | 阈值 1–40 s（**模块默认 15 = 原型值**；台架默认 **2 s**） |
| 响应 | 固化 = 不可逆 | 两档：跟随（结构追读数、人走了收回去）/ 锁定（键锁死不回退 = 滞回） | 按钮（**模块默认锁定 = 原型值**；台架默认跟随） |

四条站方决定，写明：

- **痕迹是强度语义不是守恒量**：圈里每块地都完整感到人，不是把一秒分摊给圈里的格子。原型本就是「那一格 +1」
  的强度语义，这里只是把一格放大成圆；若做成守恒量，圈越大每格摊到越少，「靠近就有感」与「站久才成形」
  会互相抵消。
- **读数取均值不取和**：阈值因此不随单元大小变——整格都在圈里站 20 s 就成形，只有一半在圈里就只到一半。
  这正是「一群单元围着人一起长，边上浅、中间深」的来源；取和的话小单元永远先成形，密度一变阈值就得重标。
- **影响半径默认 1.0 m = Hall 个人距离的远相（0.76–1.2 m）**，人类层已在用 Hall 的数。它是行为的量（走过
  与站着可以不同，招引可以更远），所以做成旋钮而不替作者定死；线稿里 0.6 只点亮脚下那一个，1.0 长出十字，
  1.2 长成 3×3 的块。

- **响应两档（2026-09-04 用户「作为演示这单元触发得太慢了、没有视觉效果」「人走了之后这个单元就应该变成
  不激活的状态」）**：`follow` 跟随——程度追当前读数，人走了读数退、结构收回去，追的速度有上限
  （收 1.25 s / 放 2.5 s，读作绞盘收放需要时间）；`ratchet` 锁定——只升不降，就是项目立论的那个滞回
  （交接件冻结决定 4「键一旦锁定不解开」，案例页「方法」节的「形态不返回初始状态」）。
  **台架默认跟随**（演示要看见结构跟着人涨落），**模块默认锁定**（守门与线稿跑的是原型行为，逐位不变：
  锁定档 dt 传 Infinity，走的还是旧路径）。两档 = 同一台机器的两种键谱：跟随 = 可解开的吸附，
  锁定 = 键锁死；哪个对是作者的研究问题，这里两个都留着，按钮切换。
  台架的演示默认还包括**阈值 2 s、散掉 6 s**（原型是 15 s / 每秒 2%，滑块可以拉回去）——站两秒
  单元就下来，人一走六秒退光，一次交互十几秒看完；慢的那一档是研究口径，快的这一档是给人看的口径。

### 2.1 「2 秒触发、6 秒散掉」为什么得换一套衰减（2026-09-04 用户第五轮「还是慢，改成 2 秒触发 6 秒散掉」）

**指数衰减给不出「几秒散掉」**：它没有封顶也没有零点。人站着时读数一路升到稳态（衰减率 2%/s ⇒ 稳态
1/0.02 = 50 s），人走开后要先从 50 掉回阈值，程度才开始动——把半衰期调到 6 s 也一样，掉的是那段
**阈值以上的富余**，用户看见的是「人走了半天单元还挂着」。这与阈值调多低无关：阈值越低，富余越大、
拖得越久。

改成**封顶 + 线性褪去**（只在台架这条路上；模块默认仍是原型的指数）：

- `TraceField.cap` = 阈值（默认 `Infinity` = 原型不封顶）。人站着的格子最多记到阈值，**不再攒富余**；
- `relax(dt, fade)`：没被人覆盖的格子每秒退 `cap / fade`，`fade` 秒退光；**人站着的格子当步不退**
  （当步被 `imprint` 标记过），所以站着照样涨到封顶；
- 于是**触发时间 = 阈值、散掉时间 = fade，两个数各管各的**，互不牵扯。这是把它们做成两个独立旋钮的
  前提——指数那套里只有一个数（衰减率），触发快就必然散得慢。

配套：机构追读数的速率上限 `PLAN.RESPONSE`（升 1.5 / 降 0.5 每秒）取得**故意不成为瓶颈**——节奏由痕迹
的涨落定，这两个数只负责把每帧的跳变磨平。绿盘与地面热力的尺子随之改为「封顶了就用那个顶」
（`planDraw.traceScale`）：读数满 = 绿盘满 = 刚好成形；不封顶时仍取 2×阈值（原型下站着的读数会升到
阈值之上，按阈值封顶画的话人一走头几秒绿盘满着不动）。

守门三项：封顶后站着的读数不超阈值且 `fade` 秒退光 · 人站着的格子当步不褪 · 端到端「站 2 s 成形、
走后 9 s 内退光」。**原型那条路一位不变**（`cap = Infinity` + `decay`），线稿与既有守门跑的还是它。

## 3. 单元要小、要多：怎么缩

场地与房间钉死 = Lab.12（场地 4.74 m 见方、房间 6.64 m），同一块场地摆 N×N：

| N | 格距 | 平台 ⌀ | 芯 ⌀ | 单元数 |
|---|---|---|---|---|
| 4 | 1.23 m | 1.04 m | 0.24 m | 16 |
| 6 | 0.81 m | 0.68 m | 0.16 m | 36 |
| 8 | 0.60 m | 0.51 m | 0.12 m | 64 |

格距 = 场地 ÷ (N − 1 + 0.84)，平台 ⌀ = 0.84 格距、芯 ⌀ = 0.19 格距（Lab.12 默认半径下的两个比例）；
N = 4 时与 Lab.12 逐位相同（守门第一条）。**平面上的「小单元」只是等比缩**——真做小的结构单元
（带数 / RIG_SCALE / 键谱在小尺寸下成不成立）是另一轮的事，这台不替它回答。

## 4. 读法：一个单元读哪片地面

- **按格**（默认）：整片地面按最近的芯归片——规则网格的 Voronoi 就是过道正中的格线，每块地都归且只归
  一个单元，人在哪儿都有单元在读；
- **脚下**：只读平台正下方（半径 = 平台外缘），平台之间的缝是盲区。

两种都留着做成按钮；差别在 4×4 上明显（缝宽），8×8 上小（缝只剩 0.09 m）。

## 5. 线稿读出来的结论（默认规则：8×8 · 影响半径 1.0 · 阈值 15 s · 步速 1.2 m/s）

`scripts/unit-activation/atlas.mjs` 四张，每格真跑（`runScenario`，与守门、台架同一份 `PlanSim.step`）：

| 行为 | 8×8 的结果 | 说明 |
|---|---|---|
| 穿行（中线） | 成形 0 · 半成 0 · 最高读数 1.5 s | 路过一块地面只在圈里待 ≈ 2 m ÷ 1.2 m/s ≈ 1.7 s |
| 斜穿（横过整片场地） | 成形 0 · 最高 2.0 s | 同上；慢到 0.3 m/s 也不到阈值一半 |
| 驻留 20 s | **成形 5 · 半成 9**：脚下 + 四个正邻居成形，四个斜邻居半成——一个十字 | 一群围着人长出来，⌀ ≈ 1.8 m |
| 绕圈 6 圈 | 成形 0 · 半成 14，连成一圈 | 反复经过攒不到阈值（每圈只在圈里 ~1.7 s、圈间衰减） |
| 折返 8 趟 | 成形 0 · 半成 11，中线两侧一条走廊 | 同上；阈值压到 8 就整条成形 |

密度的意义直接从图上读：**4×4 读不出「群」**——同一个人同一处站同样久，只点亮脚下那一个（半成也只有 1）；
6×6 出一个小十字（1 成形 + 5 半成）；8×8 才有形状。影响半径扫描（0.22 / 0.6 / 1.0 / 1.2）：驻留分别
0 / 1 / 5 / 9 个成形——0.22 连脚下那格都填不满（身体 ⌀0.44 盖不住 0.6 m 的格）。

驻留五时刻（进门 / 站 10 s / 离场 / 离场后 60 s / 180 s）：地面最深 1.7 → 10.4 → 16.9 → 5.0 → 0.4 s，
成形一直是 5——**痕迹在退、成形不退**，这一段就是滞回本身。

## 6. 台架（`components/lab/WalkPlanBench.tsx`，/lab 第十四台、第六段 Ⅵ A person）

- 一张 2D canvas（逻辑 700×520，与 WebGL 台架同尺）：热力场几千格逐帧重画，SVG 逐格改属性不划算；
  配色从容器 CSS 变量取（/lab 深色与 `on-light` 修饰符各自解析）。
- 控制条两层：第一层 行为（穿行 / 斜穿 / 驻留 / 绕圈 / 折返 / 自由）· 格数（4/6/8）· 读法 · 影响半径 ·
  阈值 · 散掉；第二层 运转 · 痕迹显示 · 重播 · 离场（自由模式）· 步速 · 时间倍速（×1/×3/×8）。
- 画法（2026-09-04 用户看真机「衰减不明显、每个单元的状态显示不明显」后改）：两个量分开画——
  **绿盘** = 当前读数（半径从芯长到平台外缘随「读数 / 阈值」涨缩，人走了它就缩回去，这是会退的量）；
  **紫环** = 成形进度（同一尺子，只涨不退；长满 = 平台外缘一圈粗紫环 + 淡紫底，读作「这台已经下来了」）。
  绿盘缩回去而紫环留着，就是滞回本身。**绿盘与地面的尺子都取 2×阈值**：站着的读数常到 16–20 s，
  按阈值封顶的话人一走开头几秒画面纹丝不动（读数从 17 掉到 15 之前满盘），按 2×阈值读数一掉盘就缩；
  地面透明度按 √(v / 2T)，走过留下的 1–2 s 浅痕迹仍看得见。首版用指数压缩（痕迹减半透明度只从 0.74
  掉到 0.49）、当前读数是芯外两像素的细弧，几乎看不见。HUD 加「地面最深 x s」读数；Lab.15 加「清人」
  一键撤掉全部人看衰减。绿虚线圈 = 人的影响半径；细墨线 = 走过的路。
- 预设走完、离场后再看 30 s 衰减自动重播；自由模式点地面走过去、停着就是驻留、「离场」从最近的门出去。
- `/lab#lab14-<path>` 直达某种行为；差分清单 +36（行为 6 × 格数 3 × 读法 2）。

## 7. 简化与边界（如实带着）

- **棘轮是连续的，真引擎的锁定是逐键的**：这里「程度 0.6 不回退」在引擎里对应「收缩到 0.6 时已锁上的
  那几根键不解开」，形态会停在那一档；两者一致的只有「不回退」这一点。
- 单元之间不相互作用（引擎单剖面），一群单元「一起成形」只是各读各的地面、各自越过阈值。
- 人只有一个，没有猫；「围绕行为」现在只体现为「行为在地面留下什么形状的痕迹」——矩阵里的猫轴、
  招引、共视等要行为层接进来才有。
- 平面只到「激活程度」，不选形态、不定 ℓ 的数值映射（翻译表是作者的）。
- 走过与站着用同一个影响半径；行为不同半径不同是自然的下一步，但那是规则，留给作者拍板。

## 8. 待用户拍板

- 默认格数 8×8、默认影响半径 1.0 m、默认行为「驻留」、时间倍速 ×3——手感常量。
- 下一步的方向：① 把猫接进来（第二个身体，规则取自 C1–C8）；② 行为 → 半径 / 阈值的差异（走过窄、
  站着宽、招引更宽）；③ 从「激活程度」到形态（翻译表格式我可以起草，规则由作者填）；④ 把这张平面的激活
  程度喂给 Lab.12 那间房的立体台架（每个单元的 ℓ 由平面驱动）——那一步要引擎接受外部驱动的 ℓ，加法式选项。

## 9. Lab.15 · 几个人在场（2026-09-04 同日，用户「做一个 lab 模拟：人是可动的，也可以放多人，直接演示这些单元的变化」）

Lab.14 是一个人按预设走一遍、看结果；Lab.15 是**现场**：点地面放人（最多 8 个）、按住一个人拖着走、
开「自走」让他们自己漫步，地面实时记痕迹、单元实时长出来。

- **机制一个数不改**：痕迹 / 衰减 / 阈值 / 读法 / 棘轮全部引用 unit-activation 同一份（TraceField /
  Catchment / Activation / Walker）；新增的只有「多个人」与「人怎么动」（`src/lib/space/crowd-plan.ts`）。
- **拖**：悬停到人身上光标变抓手，按住就拖，位置直接跟指针（不经步速），走过的地面照样记痕迹；松手回到
  自走或站着。**Lab.14 同样能拖**（2026-09-04 用户「应该有一个用户可以拖动这个人移动的交互」）：按住那个人
  拖着走，松手站在原地，预设的路线随之作废，点「重播」重来。
- **自走 = 演示装置不是行为规则**：首版随机挑场地里任意一点走过去、站 2–30 s，人在房间里窜来窜去，用户看
  真机「移动得太快太远，不符合人类缓慢步行、停留的行为模式」。改成**慢走 + 短距 + 长停**：步速 0.7 m/s
  （室内，不是街上的 1.2）、每次只挪 0.5–2 m（两成概率远一次，≤ 3.5 m）、到了站 8–45 s、放下先站一会儿
  ——九成时间在站着。带种子的随机数（mulberry32），同种子逐位可复现，守门靠它（含「每段行走 ≤ 3.5 m」
  「站着的采样 ≥ 走着的 3 倍」）。Lab.15 默认时间倍速改 ×1，按真实节奏看；Lab.14 仍 ×3。
  作者的人类层规则（Hall / Goffman / Whyte）这里不代拟。
- **几个人叠加**：影响圈重叠处每秒记几份（强度语义，每个人各记满）⇒ 两个人站在同一处，脚下的单元比一个人
  早一倍成形（守门卡 0.4–0.6 倍）；两个人隔 3 m 站着，各长各的一群、中间不连。
- 台架 `components/lab/CrowdPlanBench.tsx`，画法与 Lab.14 共用 `components/lab/planDraw.ts`（两份画法必漂，
  Lab.04/05 蒙皮的教训）。控制条：格数 / 读法 / 影响半径 / 阈值 / 散掉 ‖ 运转 / 自走 / 痕迹 / +人 / −人 /
  清人 / 清痕迹 / 步速 / 时间倍速。开场两个人自走。
- 守门 10 项：放人 / 上限 / 钳位、拖着走记痕迹、命中半径、自走不出房、同种子逐位复现、关自走停原地、
  两人叠加早一倍、一个人的读数与 Lab.14 解析式一致、隔开各长各的。
- 差分清单只加格数 × 读法 = 6（人数 / 拖 / 自走是现场操作不是档）。
- 局限同 Lab.14：没有猫；单元之间不相互作用；止于激活程度。多人之间也**不相互作用**（不避让、不成簇），
  那是作者人类层的规则。


## 10. 人有朝向、有身体（2026-09-05，用户三问：视线约 180° / 激发眼前的会影响行进 / 身体有体积、现在会触发打到人的单元）

> 本节 = 三件机制的定义与出处 · 线稿读出来的结论 · 待拍板。**只做了模型可选项与线稿，台架没接**（纪律：
> 先线稿、看图拍板、再上台架）。数字以 `unit-activation.ts` 与守门为准。

### 10.1 两个量、三件机制（全部可选，默认关 = 旧行为逐位不变）

| 机制 | 选项 | 做什么 | 出处 |
|---|---|---|---|
| 视野 | `fov`（全角，弧度） | 痕迹只落在朝向前方的扇形里；π = 前方半圆 | 用户「视线范围只有约 180°」；朝向 = 最近一次走的方向（站着时沿用），视线 = 朝向是站方简化，作者行为层另有「视线方向」变量 |
| 让位距离 D | `clearance`（m） | **D = 平台半径 + 身体 0.22 + 让位**（芯到人；让位默认 0.15 ≈ 一肘）。同一个 D 用在两处：**闸**——芯在 D 以内的单元不许下来（跟随档退回去、锁定档不升，它的平台会打到人）；**洞**——离人 < D 的地面不落痕迹（那片地上方的平台都会打到人） | 用户「身体有体积，要留出空间」；D 随单元大小变：8×8 0.62 · 6×6 0.71 · 4×4 0.89 m |
| 走廊 | `lane` | 走动时正前方 \|侧向\| < D 的一条道不落痕迹（与洞连成胶囊）；站着时收掉 | 用户「一味激发眼前的结构会影响行进」——人正要走进去的地方不能长东西 |

**为什么闸和洞要一起**：单靠洞——按格读的一片地大半在 D 外，单元照样读到七八成、平台照样下来打到人；
单靠闸——锁定档下人一走脚下就长起来，站过的地方永远堵死。**走动的判据** = 这一子步位置动了（预设按步速走、
拖着按指针给，同一判据）。三个都开：站着 ⇒ 前方半圆减去 D 以内 = **一道朝前的弧**；走着 ⇒ 前方两侧各一瓣、
正前方留道。`CrowdSim`（Lab.15）同一套选项，闸按每个在场的人各算、取并集。

### 10.2 线稿读出来的结论（`scripts/unit-activation/attention.mjs` 四张，8×8，阈值 15 s，锁定档）

- **让位一开，影响半径 1.0 m 不够用**：D = 0.62 ⇒ 1.0 m 的圈只剩 0.62–1.0 那道 0.38 m 宽的窄环，
  盖不满一个 0.6 m 的格——站 20 s 一个也不成形（最高读数 7.9 s）。1.3 m 起前方长出一道 3 个的弧，1.6 m 弧 5 个
  （半成以上 8）。**影响半径在这一轮是「看到多远」不是「存在多远」**——Hall 社交距离近相 1.2–2.1 m；
  建议量程 0.22–1.2 → 0.22–2.0，这一档默认 1.5。4×4 下 D 已 0.89，默认半径什么也招不到（表见 body.svg）。
- **视野 180° 单独就把折返 / 绕圈的读数砍一半**（16.9 → 8.4 s、21.3 → 10.7 s）：身后不再补记，来回走只记来路那一侧。
- **走廊留出来了**：穿行 / 折返沿中线的那条 2D = 1.25 m 宽的道一格不记，结构只长在路两侧；绕圈的方形路里外两侧各一圈。
  走过留痕本来就攒不到阈值（§5），这里看的是**形**。
- **现行驻留点站在单元中心——那里是根杆子**（`unitsUnderBody` 体检 = 1 根，三种密度都是）：斜穿预设也从单元中心
  穿到单元中心。线稿一律改站过道交叉点（`nearestCrossing`），预设本身没改（改了旧守门的结论会动，等拍板一起改）。
- 锁定档下走进来那几步已在前方升起的一点点（≈0.05）闸不退（棘轮只挡涨）；人走后闸松开，被闸单元脚下若还有
  D 外的痕迹会长起来（「身后合拢」）——跟随档下痕迹几秒退光、不会。哪个对是作者的问题，两档都留。

### 10.3 简化与边界

- 视线 = 朝向（不转头看）；行走不避让已成形的单元（那是障碍物，绕不绕是作者行为层的规则）；没有猫。
- 洞不按单元格切（地面是无关单元的），D 圈边上有格子归了被闸的单元、也有格子归了没被闸的单元。
- 走廊只管痕迹落哪，不管路线绕不绕杆子。

### 10.4 待拍板

① 三件机制是否都要（可分开开关）；② 让位默认 0.15、做成 0–0.4 滑块；③ 影响半径量程放到 2.0、这一档默认 1.5；
④ 视野默认 180°、可调 60°–360°；⑤ 走廊随让位一起开；⑥ 驻留 / 斜穿预设改走过道。拍板后台架接线 Lab.14 与 Lab.15 一起
（控制条第一层加「视野」「让位」两个旋钮，走廊跟让位；HUD 加「闸住 N」）。
