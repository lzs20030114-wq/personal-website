# 轮回机器 · 整机台架 spec（Lab.05）v0.1

> 立项：2026-07-29，用户拍板「整机传动 + 真实实体」「与 Lab.04 并存」。
> 依据测绘：轮回机器_整机测绘.md（数字以 `scripts/model-extract/machine_729.json` 为准）。
> 与 LINKAGE_SPEC.md 的关系：**本文不改内核、不改公式**，是一个实例 + 台架的规格。
> 冲突时以 LINKAGE_SPEC 为准。

---

## 1. 范围

### 1.1 做什么

一台 3D 台架，展示**整台轮回机器被一台电机驱动**：

1. 中间轴转动 → 五个不同半径的单臂曲柄（同相）→ 五根不同长度的连杆 → 五个环的拱顶滑块；
2. 五个环各自按真机几何折叠/张开，每块角化三角板**挂自己的真实实体网格**随刚性三角运动；
3. 机架（滑轨架 ×5、底盘骨架 ×8、电机箱、中间轴）作静态实体；
4. **大触手**由三根肌腱牵拉（v0.3，§3.4）；两条**小触手**作静态形体摆在真机位置。

曲柄角 θ 一个滑块驱动整机，肌腱三个滑块驱动大触手；机位由固定视角按钮切换（无拖拽，§5）。

### 1.2 不做什么（默认答案是「不做」）

| 不做 | 因为 |
|---|---|
| ~~触手动起来~~ | **v0.3 解禁（用户 2026-07-29 追加：「把大触手的三个肌腱牵拉功能补上」）**。原因见 §3.4——它不是「编一套动作」，是查明大触手就是 Lab.03 那条已建模的三肌腱触手，运动学有出处。两条**小触手**仍是静件（另一种零件，无机构数据）。 |
| 真机转速 / 节律还原 | 电机减速比图上未标，无出处。θ 的角速度是手感常量，规格表须写明「非真机节律」。 |
| 用滑轨架实体反解脚槽 | 测绘 §6 已记：槽可能铣在滑轨架 Brep 里，但从面反解成不成没验证过。**不列入本次里程碑**；脚槽止程沿用 `shell3d.ts` 现行标定。 |
| 改 2D/3D 内核 | 加新机构 = 新数据实例 + 台架页（CLAUDE.md 架构速记）。本次连**实例**都基本复用（见 §3）。 |
| 动 Lab.04 | 用户拍板并存。`shell3d.ts` / `Rings.tsx` / 织物蒙皮**一行不改**。 |
| 零件下料排版（`默认值` 图层 222 件） | 是制造用的摊平排版，不是机器本体。 |

### 1.3 与 Lab.04 的分工

- **Lab.04 五环**：单看壳体五环怎么折叠，含可调遮罩 + 点阵织纹（07-29 刚定版）。
- **Lab.05 整机**：整机怎么被一台电机驱动，真实实体形体，含机架与传动链。

两台共用同一份环解算代码，视角与叙事不同。

---

## 2. 坐标系与变换

图纸有两簇：**摊开位**（各环摊平在 z=0 的 XY 面上）与**装配位**（整机）。求解在摊开位做，摆放用装配位的刚体变换。

装配位 → 台架世界系（沿用 `shell3d.ts` 约定：X = 体轴，Z = 上，Y = 横向，机器居中）：

```
station(n) = 85·(n − 3)          n = 1..5  →  −170, −85, 0, +85, +170
环局部 (x, y) → 世界 (station, x, y)        （无 roll，五环全竖直，CLAUDE.md v0.2）
```

图纸装配位到台架世界系的换算（测绘 §4.4 实测，四环逐位一致）：

```
x_asm = x_local − 5488.623
y_asm = −1112.406 − 85·(n−1)
z_asm = y_local − 40.250
```

**中间轴轴心 = 环局部的 (0, 0)**，即各环地线与中线的交点。这不是巧合：五条独立推导（每环各由曲柄销高减去自身曲柄半径）全部给出 z = −40.250。

> 坑：`中间轴` 实体包围盒中心是 z = −40.021，与轴心差 0.229——轴是 D 形截面，包围盒中心 ≠ 轴心。**取 −40.250**。

---

## 3. 运动学（复用，不新写）

### 3.1 环：`shell3d.ts` 原样复用

已核对：新文件五环的销坐标与 `shell3d-data.ts`（提取自旧文件）**逐位相同**，全部差值 ≤ 0.001mm（生成时的四舍五入位数差）；曲柄半径五环 Δ ≤ 0.0005mm。

因此 Lab.05 **不新建环数据、不新建环解算**：直接 `createShell()` / `stepRing()`。脚槽止程标定、定步积分、解支校验全部沿用。

这条要守住——重造一份会立刻引入两份会漂移的真值。

### 3.2 传动链：解析，不进求解器

中间轴 → 曲柄 → 连杆 → 拱顶滑块是标准曲柄滑块，且**滑块位移已被环的解算决定**（拱顶就是环的 apex 节点）。传动链只需按 θ 画出来：

```
曲柄销(n) = 轴心 + r(n)·(cos θ, sin θ)        环局部系
连杆      = 曲柄销 → 该环 apex 的实解位置
```

`r(n)` 取测绘表：S1 29.882 / S2 48.928 / S3 34.733 / S4 28.096 / S5 24.178。
连杆长 `l(n)`：97.096 / 173.512 / 124.195 / 91.978 / 80.345——**仅作校验**（画出来的销↔apex 距离应恒等于 l，偏差进 HUD），不作为约束。

θ₀ = +π/2（图纸姿态 = 全开 = 上死点），与 `SHELL_THETA0` 同。

### 3.2.1 中间轴只在 180° 内往复（用户 2026-07-29 更正）

**轴不整周转。** 真机的中间轴在 180° 范围内来回摆，所以机器做的是**张合**，不是循环转动。

区间 `[θ₀, θ₀+π]` 的两端恰好是曲柄滑块的上下死点：θ₀ = 全开（图纸姿态），θ₀+π = 全折叠。
于是 **180° 正好走完整个行程 2R**，一步不多一步不少——这不是巧合，是曲柄滑块的性质。

端点换向不需要额外缓动：死点处 `d(拱顶)/dθ = 0`，曲柄匀速反转时输出速度本来就是从零平滑折回的。

落地：`reciprocate()` / `runMachine()`（纯函数，machine.ts）——**反射而非钳制**，
钳制会让机器停在端点等人改方向，反射才是「来回转」。台架相位滑块量程随之 0–180°，不是 0–360°。

**口径与盘点一致**：φ=0 伸展 / φ=180 折叠（盘点 §「已验证的机构原理」第 4 条）。
该条原文写的「连续单向转轮 = 循环呼吸」已按本次更正改掉——那是本项目此前的错误假设，不是新分歧。

**摆向**：`MACHINE_DIR = −1`，即从伸展位起经 +u 一侧扫过 180°（用户 2026-07-29「转反了，应该是另一侧」；
首版取 +1 摆错了边）。两个摆向的拱顶轨迹**完全相同**（关于死点对称），差别只在曲柄与连杆从哪边走——
所以这是纯外观量，改它不动运动学、不影响任何行程结论。

**可读性**：往复这件事在整机视图里**看不出来**——环的动作在整周转与 180° 往复下逐帧相同，
唯一的区别在曲柄，而默认机位下曲柄小且被机架挡着。故 HUD 增加方向指示（`伸展 ↑` / `折叠 ↓`）；
要直接看曲柄摆动，用控制面板的「部件」只留传动。

### 3.2.2 转速

默认 `MACHINE_OMEGA = 0.8 rad/s`（模型定，用户 2026-07-29 授权）：半程 π/ω ≈ 3.9s，
一次完整张合（开→合→开）≈ 7.9s，与人的呼吸节律同量级——THESIS_NOTES 的「生命感」要的是这个，
不是机械循环感。与 `SHELL_OMEGA` 取同值，两台并读节奏一致。**仍非真机节律**（减速比无出处，见 §7）。

### 3.3 顶部止程

导轨端点 ≡ `l ∓ r`，五环偏差 ≤ 0.0001mm（测绘 §4.1）。这是曲柄滑块的解析结果，**不需要额外约束**——环按 θ 解出来自然落在区间内。

> **绝对值对不上是正常的，别去「修」**：图纸的连杆接在导轨滑块点（S4：0, 120.075），
> 而 `gen_shell3d.py` 的 `rod` 杆接在**拱顶销**（S4：0, 119.216），两者差 0.859——
> 滑块件坐在销上方。于是站上的 rod rest = 91.120，图纸标的 l = 91.978。
> 两边各自自洽，**不是误差**。
>
> 不变量是**行程长度 = 2r**，不是端点绝对值。

测试断言按此写：**180° 半程**扫掠中 apex 的行程 `apex_max − apex_min` 与 `2r` 偏差 ≤ 0.05mm，
且 `apex_max` ≡ 图纸姿态 apex（θ₀ = 上死点 = 全开）。

S2 图上只画了上端，下端按同式补 124.585（仅登记，不参与约束）。

### 3.4 大触手：三肌腱牵拉（v0.3）

**它就是 Lab.03 那条触手。** 729 装配位里大触手的椎节沿臂位置与 `tentacle3d-shape.ts`
的 STATIONS 逐位吻合（生成期闸门：残差 >1mm 直接拒绝），基座舵机总成的位置亦对得上。
所以运动学**整套复用 tentacle3d**（7 椎节 + 3 肌腱 + Kangaroo 式收缩），
本台架只负责「摆到机器上」——这不是编动作，是把已有机构放回它在整机里的位置。

**落位**（`gen_machine.py` 实测生成 → `ARM_PLACEMENT`，手写无效）：

```
world = ( ox − sy ,  oy + sx ,  oz + sz )      sim: y = 沿臂，(x, z) = 截面
```

即 sim x̂ → 世界 +Y、沿臂 ŷ → 世界 −X、ẑ → 世界 +Z；行列式 +1，网格法向不翻。

**滚转靠离轴地标定**：椎节质心全在轴线上，只定得住沿臂位置，定不出绕臂姿态。
用**梢端三根绑线柱**的方位与 TIES 比对——实测残差 2.6°（零件质心的测量噪声量级），
即截面坐标系与装配系不差旋转。生成期 >6° 直接拒绝出表。

> 坑：梢端有**两组**半径≈6 的三件，彼此差约 60°。按「最靠梢端」取会选中错的那组
> （首版即此错，算出 61° 滚转）。正确做法是先用椎节质心把 ax 原点拟合出来
> （这一步与滚转无关），再按 `ax = TIES 的沿臂位置` 去**定位**该取哪一组。

**待机摆动（用户 2026-07-29：「不要让它直挺挺的伸着」）**：运转中且用户没碰肌腱滑块时，
三腱按 120° 相位轮流轻收，合成一个缓慢回转的弯向（≈15s 一圈）+ 更慢的整体舒卷（≈27s）。
一碰滑块即交出控制权，「交还待机」交回去。

> **肌腱有松弛量，这决定了幅度不能小。** 实测单腱收缩率 **0.28 以下拉的全是空行程**——
> c=0.10 / 0.15 / 0.22 的梢端侧移都是 **0.0mm**（c=0.30 才 15.9mm、0.40 是 74.2mm、
> 0.50 是 128.5mm 的 J 形卷曲）。首版把幅度设成 0.22 想着「稍微收缩」，结果**对触手
> 完全无效**：参数设了、波形跑了、臂一动不动，而屏幕上确实在动的是五环——差点当成做完了。
>
> 定案：三腱共同施加 **base = 0.34** 的基线预张力（先把松弛吃掉；三向恒和为零，
> 不产生弯向），再在其上叠 **span = 0.34** 的差动（弯向由它给）。峰值 0.68，
> 实测梢端侧移 **43–150mm** = 臂长 358mm 的 12%–42%：始终带弧、从不回到笔直，
> 卷与舒之间有三倍多的落差。
>
> 幅度经用户两轮上调：首版峰值 0.22（**完全无效**，见上）→ 0.42（侧移 18–78mm，
> 用户「有点不明显」）→ 0.68。
>
> **起步缓入只能加在差动上**（用户第三轮：「第一下的收缩有点猛」）。
> 首版把缓入乘在整体上，没解决问题——松弛门槛以下臂完全不动，整体从 0 缓入
> 等于前 3 秒空等，等驱动腱越过门槛时缓入已走了大半，一越过照样是「一下子」
> （实测起步段峰值速率是稳态的 2.6 倍）。
> 定案：基线 1.5s 就位（它本身几乎不产生弯曲——三腱共缩 0.34 实测侧移仅 3.2mm、
> 臂长 357/358mm、不压屈），差动再用 5s 从零 smoothstep 拉起。
> 判据不是「看着顺眼」而是**起步速率 ≤ 稳态速率**：实测 67.0 vs 65.9 mm/s，比值 1.02。
>
> 排查中我自己读错过一次数：打印的是腱 0 的收缩率，而那一刻在出力的是腱 1，
> 于是看成「收缩涨 0.02、侧移窜 112mm」的诡异过冲。三腱系统里**必须看最大腱**，
> 不能盯着固定某一根。
>
> **残差是稳态量，不是收敛滞后**：把波形放慢 2.6 倍（15s → 39s 一圈），残差纹丝不动
> （6.44 → 6.63mm）；静态保持 15 秒亦然。这是该肌腱模型在大收缩下的固有量，放慢救不了。
> 上界按站上既有工况校准：Lab.03 已上线并经用户真机拍板的 c=1.0 全螺旋残差 **9.19mm**，
> 本波形峰值 0.68 约 **7.8mm**，仍在其下。

**代价与收益**：大触手不再烘进 `machine-mesh.bin`（载荷 113,906 → 82,553 三角），
改用 Lab.03 那份 `tentacle3d-mesh.bin`（与 Lab.03 同一份文件，浏览器命中缓存）。
控制面板加三个肌腱滑块 + 松开；限速用临界阻尼（同 Lab.03，真机肌肉不会瞬间到位）。

---

## 4. 形体载荷

### 4.1 网格来源

.3dm 的 Brep **整体层面取不到网格**（`GetMeshes` 返回空），但**逐面取得到**（`brep.Faces[i].GetMesh(MeshType.Any)`）——`gen_tentacle3d.py` 已用此法，照抄。

全文件 232,439 三角；装配位实际要用的：

| 组 | 件数 | 归属 |
|---|---|---|
| `实体_角化件` | 69（S1 16 / S2 14 / S3 11 / S4 14 / S5 14） | **动件**，逐块绑刚性三角 |
| `实体_单杆轮` | 5 | 动件，绕轴心随 θ 转 |
| `驱动杆` | 5 | 动件，两端跟销与 apex |
| `滑轨架` / `骨架` / `中间轴` / `中间轴驱动` | 5 / 8 / 1 / 2 | 静件 |
| `小触手` | 2 图块 | 静件 |
| `大触手` | 1 图块 | **不烘**——活件，复用 tentacle3d 的网格与解算（§3.4） |

体量与 Lab.04 那台真实扫描网格（10.7 万三角）同量级，可接受。**若装配位实测超过 15 万三角，按件抽样降面**——降面策略在实现时定，不在本 spec 预设。

### 4.2 绑定规则

- **角化件 → 三角板**：按**二维轮廓完整包含**判定——把件的网格投到环局部 (u, v) 取凸包，凡凸包含住某块板全部三个销的，即绑该板。实测五环全部**一一对应、无歧义**。
  > 用质心落点判定是错的（首版即此错）：`杆三角` 是细长杆的三角化表示，面积很小，
  > 质心法在任何容差下都会同时命中相邻两块板。**判据是销在不在件里，不是件心在不在板里。**

  组内顶点转到该板的局部标架（原点 = 销 A，ex = 单位(B−A)，ey = ex 逆时针 90°），运行时按三块销的实时位置重建标架。
- **配件（无板可绑）→ 最近销**：S1 多 2 件（外左脚 / 内右脚的滑块配件）、S3 多 1 件（拱顶配件）——**只随该销平移、不转**，且生成期必须逐件打印归属。不许静默吞掉。
- **单杆轮 → θ**：绕轴心旋转 θ − θ₀。
- **驱动杆 → 两点**：由曲柄销与 apex 两点定位姿（沿杆方向 + 环平面法向），刚体摆放。
- **静件**：一次性变换到世界系，不再动。

### 4.3 载荷格式

照 `tentacle3d-mesh.bin` 的做法：逐组索引网格（Float32 顶点 + Uint16/32 索引），分组元数据进 TS。产物：

```
src/lib/linkage/machine-shape.ts     分组元数据 + 绑定表 + 传动链参数
src/demo/assets/machine-mesh.bin     网格载荷（gitignore 外——同 tentacle 先例，进 git）
```

`scripts/copy-assets.mjs` 加一条复制到 `public/mesh/`。

---

## 5. 渲染

- 复用 `gl3d`（WebGL z-buffer）+ `camera3d`（OrbitCamera）+ `motion`，**装备零修改**；确有需要时只做加法式扩展（07-29 `drawPointCloud` 先例）。
- 2D 连杆仍守 SVG 红线——本台架是 3D 线，与 Lab.04/触手同待遇。
- 视角预设照 Lab 页既有六向按钮；HUD / 控制条照搬 Lab 页 CSS 度量（07-27 教训：**读设计稿不许剥样式**，此处等价于「不许自创版式」）。

**机位只由固定视角按钮控制**（用户 2026-07-29 拍板「取消拖拽的视角移动，只保留底下几个固定视角」）：
不挂任何指针/滚轮监听，故拖拽不转、右键不平移、滚轮不缩放。**与 Lab.01–04 的差异是有意的**——
那几台仍可拖拽。连带好处：滚轮不再被画布吞掉，鼠标停在这台上能正常滚页（/lab 五台叠起来时这点很实在）。
「归位」按钮随之撤掉——没有拖拽就没有要复位的东西，它与「轴测」完全等价（实测来回切换后画面逐像素复原）。

控制：整机专属面板（用户 2026-07-29 拍板「复用其他 lab 的[版式]，然后做一个自己的控制面板」）——
驱动（运转 / 透视）· 转速 · 相位 · **部件显隐**（环身 / 传动 / 机架 / 触手）· **蒙皮遮罩** ·
**单环隔离** · 视角（五个固定机位，无拖拽）。`.lab-ctl` 的 CSS 一行未加（本就 flex-wrap，多组自己换行）。
`controls` 开关沿用（主页语境不出控制条）。

**蒙皮**：与 Lab.04 同一套直纹带（纯几何件已抽到 `src/lib/linkage/skin.ts` 共用，
避免两台各留一份副本漂移），默认 0.22 比 Lab.04 的 0.35 更透——这台的看点是里面的
传动链，盖太实就白做了。半透明三条硬约束照 Lab.04 定案：深度只测不写 ⇒ 实体先画、
半透明最后画 ⇒ 带按视深从远到近自己排。环身关掉时蒙皮一并不画（皮附在环上）。

---

## 6. 里程碑

| M | 内容 | 验收 |
|---|---|---|
| **M1** | `gen_machine.py`：装配位网格提取 + 分组绑定 + 传动链参数 → `machine-shape.ts` + `machine-mesh.bin` | 生成期自检：每件角化件都归到组、三角形数报表、绑定残差（图纸姿态下逐组顶点回算误差）< 0.01mm |
| **M2** | `machine.ts`：组合 `createShell()` + 传动链解析 + 逐组世界变换 | 单测：180° 半程中 apex 行程 ≡ 2R（≤0.05mm）；销↔apex 距离 ≡ `l`（≤0.05mm）；往复不越界、端点折返不卡死；定步跨设备一致 |
| **M3** | `components/lab/Machine.tsx` + `/lab` 第五台 | typecheck + 全套测试 + 双构建；CDP 实测无 pageerror、五环随 θ 同步动作、视角预设可用 |
| **M4** | 用户真机手感拍板（θ 速度、默认机位 pivot/scale、静件明暗） | —— |

M1–M3 本次做完；M4 交用户。

---

## 7. 规格表须写明的局限

台架规格表（Lab 页右栏）必须出现，不许只说好话：

1. 转速非真机节律（减速比无出处）——**但 180° 往复的行程范围是真的**，两端即曲柄滑块死点；
2. **大触手可牵拉**（= Lab.03 那条，运动学有出处）；两条**小触手**仍是静态形体，不参与运动；
3. 脚槽止程是仿真标定值，非真机实测（沿用 07-17 定案）。

---

## 8. 处置记录

- v0.1（2026-07-29）：立项。范围与并存策略由用户拍板；「环解算复用不重造」由新旧文件逐位比对结果决定。
- v0.2（2026-07-29）：用户更正「中间轴只有 180° 往复运动，不能转一圈」⇒ 新增 §3.2.1；转速授权模型定（§3.2.2）；
  默认机位定为「全体都在画面里」（用户：后续要做触手的运动），不再考虑只框环身。
- v0.3（2026-07-29）：用户追加蒙皮（§5）与大触手三肌腱牵拉（§3.4）；
  机位改为只由固定视角按钮控制、取消拖拽与滚轮（§5）。
  §1.2 的「触手不动」条款相应解禁——解禁的依据是查明它有出处（= Lab.03 那条已建模的触手），
  不是放宽标准；小触手仍不动。
