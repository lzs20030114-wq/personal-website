# 轮回机器_拱环求解器.md — 真机 S4 环的求解器实例（2026-07-10）

**做了什么**：把轮回机器伏丘壳体的 S4 环（角化 Hoberman 剪式拱 + 曲柄滑块驱动）从
`模型求解器参考/求解器结构演示.3dm` 提取为 PBD 求解器实例。**内核零修改**（LINKAGE_SPEC
§1.2 范围锁死内：只用距离约束 + 锚点 + 软拖拽），复用 `LinkageController` 原样驱动。

台架：`npm run demo` → `/arch.html`（四杆台架首页有互链）。测试：`arch.test.ts` 7 项，
全套 30 项绿。

## 几何提取（权威来源与口径）

- 来源图层 `摊开骨架::S4_M3x1.000`（毫米，2D，z=0）：14 块角化三角板（杆三角层）、
  23 销（销层）、轮心 (0,0) + 曲柄销 r=28.10 + 中央杆 + 竖直导轨（驱动/导轨层）。
  与盘点 §7 对照：R\*=28 ✓、L\*=92 ✓、四脚共线 ✓（±158.6 / ±147.9）、行程 = 2R\* ✓。
- 变换到 viewBox：`x_px = 350 + 1.9·x_mm`，`y_px = 430 − 1.9·y_mm`（y 翻转）。
- 中央杆按图纸姿态取 `|销−拱顶| = 173.13px（91.12mm）`——比 L\*=92 短 0.88mm，
  应为导轨滑块与拱顶销的安装偏置（3dm 中杆画到导轨顶 120.07、拱顶销在 119.22）。
  对运动学影响 <0.05mm，已按图纸姿态取值保证装配精确。
- 生成链路（工具在仓库 `scripts/model-extract/`，依赖 `pip install rhino3dm`）：
  `dump3dm.py`（图层普查）→ `analyze_ring.py`（单环连接图 → ring_S4.json）→
  `gen_arch.py`（→ `src/lib/linkage/arch-data.ts`，勿手改）。中文路径注意先拷贝为
  ASCII 文件名再读（rhino3dm 在 Windows 中文路径上 Read 失败）。
- **2026-07-16 脚部边界事故与回退（定案，勿再改动）**：当日凌晨提交 `8e98502`
  自称按「伸缩外壳1.3dm」纠错，把外侧两脚改成 `fixed` 地面固定铰、内侧限 38mm 槽，
  连盘点 §6.1 原文（"脚（销）在水平直槽内开合，整环 Hoberman 式均匀放大缩小"）
  也被改成迁就此说。后果：台架上最外两脚被焊死，整环收缩被外脚顶住，
  行程中段残差 ~4.8px、折叠死点峰值 5.31px（原版 2.08px），增遍数无效——
  典型过约束慢模态，不是收敛预算问题。用户否决（"最外两点被焊死"），
  当日先试过按 S4 截面整体重提取恢复四滑块（`9cbc662`，姿态/比例全变，随即揭掉），
  最终定案 = 整体回退 `8e98502`，恢复 2026-07-10 真机拍板的**四脚同源水平滑块**边界
  （本文件实测表即该版数字）。教训：脚部边界以盘点 §6.1 原文与已拍板行为为准；
  真实脚槽止程若要入模需另议（求解器无不等式约束，止程只能落在渲染/手柄层）。

## 两个工程决策（面试/case study 可讲）

1. **导轨/脚槽 = 超长杆近似**：「点在直线上」不在求解器约束集内。用 rest=10⁶px 的杆
   连到远锚点——半径 10⁶ 的圆弧在 ±170px 行程内偏离直线 <0.01px（sagitta = s²/2R）。
   **这正是 Watt/Chebyshev 直线机构的原理**（IDEAS 画廊第一梯队），把范围外需求
   变成了范围内的机构学示范。实测：整周运转拱顶 |x−350| < 0.1px、四脚 |y−430| < 0.1px。
2. **支撑节点（brace）**：角化板极扁（高 ~10.5px），高度对杆残差的放大率 ≈ 臂长/高 ≈ 5.7——
   1px 瞬态残差即可把板压过共线、镜像隧穿（实测折叠中段翻面，同 SPEC §5 条 6b 现象，
   驱动瞬态即可触发、无需暴力拖拽）。每板加 1 个偏置 45px 的支撑点 + 3 杆成 K4 全连接，
   势垒从 ~10px 提到 ~45px（对标四杆板 73px）。**语义也更诚实：真机角化件是有面积的
   实体板，不是线段**。修后整周 144 步苛刻扫描零翻面（min|签名面积| ≥ 388，原先穿零）。

## 实测回填（arch.test.ts 锁定）

| 项 | 实测 | 测试上界 |
|---|---|---|
| 图纸姿态装配残差 | ~0（settle 240 遍后） | <0.5px |
| 整周瞬态峰值（2.5°/步苛刻工况，48 遍/步） | 3.59px，出现在折叠死点附近 | <4px |
| 整周瞬态峰值（720 步/圈 ≈ 运行时步距） | 2.08px；96 遍仅降到 1.99——慢模态非预算问题 | —（注释记档） |
| 松手恢复到 <0.5px | 64 遍/帧 × 17 帧 | ≤24 帧 |
| 呼吸行程 | = 2R = 106.77px（曲柄滑块运动学） | ±3px |

死点「变软」= SPEC §3.4 的雅可比降秩，**真机拿同一现象当自锁限位**（盘点原理 4）——
数值行为与机械行为在这里是同一句话，case study 的天然素材。

- **台架槽线口径（2026-07-16，用户拍板定案）**：槽线 = **各侧两脚实际到过的 x 范围，
  逐帧累计、动态绘制**（`arch.ts` render 内维护 min/max 包络）。原因：四滑块版存在
  一个近零刚度的软自由度——曲柄只锁定拱顶高度，四脚沿地线的停靠点随帧率/运动历史
  漂移（同一代码：720 步/圈外漂到 `[17.7,69]`、1440 步/圈内漂到 246、60fps 稳态
  `[20.3,35.7]`、用户设备内漂到 ~`[130,235]`，残差均 ≈0——是真自由度不是求解误差）。
  真机靠槽端止程/摩擦锁住该模态，止程是不等式约束、spec §1.2 范围外，故静态槽线
  在别的设备上必然对不上，只能跟着实测画。曾提议软定位杆（stiffness 门控）模拟
  槽内摩擦，用户未采纳——保持机构裸行为。
- **止程模拟实验（2026-07-16，回答用户「实体机要不要留外向余量」）**：实验脚本在
  求解器外侧模拟槽端止程（每步将脚的外向越界钳回名义全开位，与投影交错）。整周 × 3 圈：
  峰值残差 **0.56px**（对比：无止程=漂移不定、焊死外脚=5.31px——单向止程反而最健康，
  证实止程就是真机让机构确定化的设计件）；循环中脚对止程的外向压入峰值仅 **0.25mm**、
  只出现在全开死点附近，折叠时脚主动离开止程向内走。结论：**外向余量在运动学上不需要**；
  留 1–2mm 只为装配公差（全开=曲柄死点，传动比→∞，止程偏内零点几毫米即顶死，载荷
  直接压销与舵机），且余量内外滑自由模态复活，需配外向回位或可调止程标定。另：模型中
  内外脚行程分配不唯一（外端止程只锁一个方向），真机槽长/内端止程按实测标定，勿信模拟
  分配数字。锁死曲柄的自由模态实测：外滑（底部剪叉并拢+双脚同移）残差 0.0005px = 真
  自由运动、无恢复力；向内越过全开位约 5px 即自锁。

- **止程转正 + 定步积分（2026-07-17，用户拍板「实施丢了活动范围」后修正）**：
  用户双机实测同一 URL 形态分岔——一台正常呼吸，一台**几秒内**两脚并拢、整环压扁
  （残差仍 ≈0，合法解不是算错）。复盘：恒定帧率仿真（60/90/120/144fps）**全部**在首圈
  内塌进并拢态；「正常」那台实为 07-16 记录过的内漂盆地（左脚对实测 [137,223] ≈ 档案
  [130,235]）——两台都是自由模态的任意盆地，无止程则形态由设备帧历史决定。修正三件套
  （内核零修改，均在实例/台架层）：
  ① **双向槽端钳制** `ARCH_FOOT_SLOTS` + `archStopPass`（钳制与投影 8×6 交错）——
  槽外端 = 全开位（图纸装配位），内端 = 本模型理想准静态整圈实测极值（1440 步 × 96 遍，
  峰值残差 0.381px；行程实测：外脚 96.8px=50.9mm、内脚 177.6px=93.5mm，同侧脚距
  20.37→101.19px——折叠**不是**均匀缩放，内脚行程约为外脚 2 倍；左右镜像对称到 0.01px）。
  真机槽长/内端止程按实测标定后替换槽表即可（07-16 定案不变）。
  ② **定步积分** `ARCH_STEP_DT = 1/120`：台架 rAF 循环改累加器定步推进（长卡顿丢时间
  不追帧）——分岔源「每帧 Δθ = ω·dt 随设备帧率变化」就此消除，任何设备走同一条逐步
  相同的轨迹（arch.test「逐位相同」测试锁定）。
  ③ **槽线改静态设计范围**（每脚一条）——取代 07-16「运行时包络」拍板，彼时前提
  「停靠点随帧历史漂移」已被 ①② 消除；预盘种子随之删除。
  验证：arch.test 新增 3 项（槽表几何/定步 3 圈不塌/切分不变性），全套 168 绿；
  真浏览器整圈实测峰值残差 0.562px、四脚全程在槽内、脚距恒 20.37 无并拢。
  另：伸缩外壳1.3dm 实测内侧脚槽画作 38mm、外侧无槽（07-16 误读为固定铰的出处），
  但该图环几何与当前 S4 不同尺寸，38 不可直接迁移——故内端取本模型自身运动学行程。
- 遍数预算：spin 48 / drag 64（`ARCH_SPIN_SWEEPS/ARCH_DRAG_SWEEPS`）+ 每子步
  archStopPass 48 遍；定步 120Hz 下 91 杆 × 96 遍 × 120 步/秒 ≈ 105 万投影/秒，无压力。
- ω = 0.8 rad/s 尚可再调（真机呼吸更慢）；**手感参数留给用户拍板**（ROADMAP C 类）。

## 台架交互约定

可抓手柄 = 拱顶（呼吸自由度）+ 四脚（滑槽方向）+ 曲柄销（手动盘轮）；中段销不设手柄
（扁板对法向按压敏感，拖拽入口只开在机构自由度方向上）。渲染只画物理件——导轨长杆、
远锚点、支撑节点一律不画。

## 边界与遗留

- **触手模拟（触手模拟1.3dm/gh）明确不做**：柔性附肢是软体/绳索问题，范围锁死外
  （IDEAS 第二梯队「绳索元素」挂着，动前先写 spec）。
- S1–S5 五环同拓扑（S1 已交叉验证：14 板/23 销/四脚共线），生成器改环名即可出全族——
  但**五环族演示属于新内容，未列入当前范围**，需要时再议。
- 本实例在 case study 中的角色：FIG.12「求解器工艺的最小可交互实例」的候选实现——
  网页四杆件讲「求解器数学」，本件讲「同一求解器直接吃下真机机构」。措辞与取舍
  归骨架文档（轮回机器_案例页骨架.md）与用户。
- `arch.html` 是工程台架（对标 index.html 的地位），不是站点页面；挂不挂上站、
  怎么挂，等 S2/M3 时按 SITE_SPEC §7 议。
