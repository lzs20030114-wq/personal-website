# 轮回机器_拱环求解器.md — 真机 S4 环的求解器实例（2026-07-10）

**做了什么**：把轮回机器伏丘壳体的 S4 环（角化 Hoberman 剪式拱 + 曲柄滑块驱动）从最初的
`模型求解器参考/伸缩外壳1.3dm` 提取为 PBD 求解器实例。**内核零修改**（LINKAGE_SPEC
§1.2 范围锁死内：只用距离约束 + 锚点 + 软拖拽），复用 `LinkageController` 原样驱动。

台架：`npm run demo` → `/arch.html`（四杆台架首页有互链）。测试：`arch.test.ts` 8 项。

## 几何提取（权威来源与口径）

- 来源为 `伸缩外壳1.3dm` 的原始 S4 截面（objects 97–148，毫米，2D，z=0）：14 块角化三角板、
  23 个关节点、轮心 (0,0) + 曲柄销 r=12 + 中央杆 + 顶部竖直导轨。
- **2026-07-16 运动边界纠错**：首轮修复误把外侧两脚改成固定铰；用户指出最外两点同样参与滑动。
  复核完整 S4 杆系后确认，错误根因是把 `求解器结构演示.3dm` 的展开态杆系与
  `伸缩外壳1.3dm` 的轨道边界混用。修正后四脚全部水平滑动，且杆系、四脚、轨道和曲柄均来自
  同一个原始截面；完全展开后四点同步向心，不再进入外翻运动状态。
- 原始 S4 四脚相对轮心为左 `−82.723 / −50mm`、右 `+50 / +82.722mm`；每侧轨道从外脚位向中心
  延伸到 `±12mm`。换算到 viewBox 后为左 `[48.6164, 306.2806]`、右 `[393.7194, 651.38]`。
- 变换到 viewBox：`x_px = 350 + 3.643287·x_mm`，`y_px = 430 − 3.643287·y_mm`（y 翻转）。
- 中央杆按同一截面取 `|销−拱顶| = 276.35px（75.853mm）`；曲柄半径 43.72px（12mm），
  呼吸行程严格为 `2R = 87.44px（24mm）`。
- 生成链路（工具在仓库 `scripts/model-extract/`，依赖 `pip install rhino3dm`）：
  `dump3dm.py`（对象普查）→ 原始 S4 连接图 `ring_S4_shell.json` →
  `gen_arch.py`（→ `src/lib/linkage/arch-data.ts`，勿手改）。中文路径注意先拷贝为
  ASCII 文件名再读（rhino3dm 在 Windows 中文路径上 Read 失败）。

## 两个工程决策（面试/case study 可讲）

1. **导轨/脚槽 = 超长杆近似**：「点在直线上」不在求解器约束集内。顶部导轨与四个滑脚
   用 rest=10⁶px 的杆
   连到远锚点——半径 10⁶ 的圆弧在 ±170px 行程内偏离直线 <0.01px（sagitta = s²/2R）。
   **这正是 Watt/Chebyshev 直线机构的原理**（IDEAS 画廊第一梯队），把范围外需求
   变成了范围内的机构学示范。实测：整周运转拱顶 |x−350| < 0.1px、四个滑脚 |y−430| < 0.1px。
2. **支撑节点（brace）**：角化板极扁（高 ~10.5px），高度对杆残差的放大率 ≈ 臂长/高 ≈ 5.7——
   1px 瞬态残差即可把板压过共线、镜像隧穿（实测折叠中段翻面，同 SPEC §5 条 6b 现象，
   驱动瞬态即可触发、无需暴力拖拽）。每板加 1 个偏置 45px 的支撑点 + 3 杆成 K4 全连接，
   势垒从 ~10px 提到 ~45px（对标四杆板 73px）。**语义也更诚实：真机角化件是有面积的
   实体板，不是线段**。修后整周 144 步苛刻扫描零翻面（min|签名面积| ≥ 388，原先穿零）。

## 实测回填（arch.test.ts 锁定）

| 项 | 实测 | 测试上界 |
|---|---|---|
| 图纸姿态装配残差 | ~0（settle 240 遍后） | <0.5px |
| 整周瞬态峰值（同源原始 S4，64 遍/步） | 0.70px | <0.8px |
| 松手恢复到 <0.5px | 64 遍/帧，测试锁定 | ≤24 帧 |
| 呼吸行程 | = 2R = 87.44px（24mm，曲柄滑块运动学） | ±3px |

死点「变软」= SPEC §3.4 的雅可比降秩，**真机拿同一现象当自锁限位**（盘点原理 4）——
数值行为与机械行为在这里是同一句话，case study 的天然素材。

- 遍数预算：spin 64 / drag 64（`ARCH_SPIN_SWEEPS/ARCH_DRAG_SWEEPS`）；
  91 杆 × 64 遍 × 60fps ≈ 35 万投影/秒，无压力。
- ω = 0.8 rad/s 尚可再调（真机呼吸更慢）；**手感参数留给用户拍板**（ROADMAP C 类）。

## 台架交互约定

可抓手柄 = 拱顶（呼吸自由度）+ 四个水平滑脚（两侧向心轨道）+ 曲柄销（手动盘轮）；中段销不设手柄
（扁板对法向按压敏感，拖拽入口只开在机构自由度方向上）。渲染只画物理件——导轨长杆、
远锚点、支撑节点一律不画。

## 边界与遗留

- **触手模拟（触手模拟1.3dm/gh）明确不做**：柔性附肢是软体/绳索问题，范围锁死外
  （IDEAS 第二梯队「绳索元素」挂着，动前先写 spec）。
- S1–S5 五环同拓扑，但当前生成数据只锁定最初模型中的 S4 截面；
  **五环族演示属于新内容，未列入当前范围**，需要时必须逐环从同源运动模型提取，不能混用展开态。
- 本实例在 case study 中的角色：FIG.12「求解器工艺的最小可交互实例」的候选实现——
  网页四杆件讲「求解器数学」，本件讲「同一求解器直接吃下真机机构」。措辞与取舍
  归骨架文档（轮回机器_案例页骨架.md）与用户。
- `arch.html` 是工程台架（对标 index.html 的地位），不是站点页面；挂不挂上站、
  怎么挂，等 S2/M3 时按 SITE_SPEC §7 议。
