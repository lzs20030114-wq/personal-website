# 交接备忘（2026-07-10）——换账号续作指南

**读者**：下一个会话（任何账号/模型）。先读 CLAUDE.md（文档地图 + 当前状态 + 纪律），本文只讲「从哪继续」。

## 今天完成的三件事（均已 commit 并 push）

1. **网站架构 review**（`e7ef2e3`）：四路外部调研（获奖站/GitHub 架构/动效/case study 叙事）验证现有方向全部同向、无需推翻。产出 **SITE_REVIEW_2026-07.md**——§二 代码发现、§九 弱模型工单（11 条）、附录 A–D 调研原文（含全部出处 URL）。
2. **三项拍板落档**（`ba6347c`）：用户已拍板通过——① hover/active 状态反馈最小集（SITE_SPEC §1 克制条款修订）；② 正文换自托管 Source Serif 4 Variable（§8）；③ about 页 colophon 席位（§4）。**SITE_SPEC 已改，工单 9–11 条对应落地项。**
3. **拱环求解器实例**（`d7d2cc8`）：真机 S4 环（角化剪式拱+曲柄滑块）从 `模型求解器参考/求解器结构演示.3dm` 提取为求解器实例，**内核零修改**。台架 `npm run demo` → `/arch.html`。决策与实测：**轮回机器_拱环求解器.md**。测试 30/30 绿、typecheck/build 干净。

## 立即可做（按优先级）

- **弱模型工单**：SITE_REVIEW_2026-07.md §九 全部 11 条（graphite 对比度、链接下划线、focus-visible/skip-link、about h1、getAllWork 缓存、SlotSize full、next-project 尾链、hover 微反馈、字体自托管、colophon 席位；第 8 条字幕轨是素材期备忘）。一次 commit，注明「SITE_REVIEW §九 工单」。
- **S4**（OG/sitemap/robots/JSON-LD/check-links）：按 SITE_SPEC §10 节奏（≤09 中），做法与 Satori 三条铁律见 SITE_REVIEW §三。
- **连杆 M3（Session 4）**：耦合曲线 + 工程标注/题栏 + release 阻尼——**需用户在场**（阻尼手感拍板，ROADMAP C 类），别自行开工。

## 边界提醒（防漂移）

- 求解器内核（solver/controller/trace/presets）与 Vite 台架**不许动**；拱环新文件（arch\*.ts）动前先读 轮回机器_拱环求解器.md。
- 触手模拟（软体）明确不做；五环族（S1–S5）能做但不在范围。
- 模型二进制在 `模型求解器参考/`（已 .gitignore，不进 git——权威副本在用户机器上；提取工具链在 `scripts/model-extract/`，依赖 `pip install rhino3dm`）。
- case study 正文与一切叙事文字**不代写**；TOEFL 优先级高于一切作品集工作。

## 环境速记

`npm run dev`=站点 :3000；`npm run demo`=连杆台架 :5173（`/`=四杆，`/arch.html`=拱环）；`npm test`=Vitest 30 项；push 即 Vercel 部署（arch.html 是 Vite 台架页，不进 Next 产物）。
