# 轮回机器_触手 spec

**版本**：v0.3（2026-07-10。v0.1 = 提案；v0.2 = 用户拍板；v0.3 = B+C 实现落地）
**地位**：IDEAS 第二梯队纪律（先 spec 后码）的产物。
**触发**：用户 2026-07-10 提出「把触手的也做了」。

## 实现记录（v0.3，2026-07-10）

- **落地形态**：`tentacle-data.ts`（实例）+ `tentacle.html` / `src/demo/tentacle.ts`（台架，线上 `/demo/tentacle.html`）+ `tentacle.test.ts` 10 项。内核修订入 LINKAGE_SPEC：§2.1 `Bar.stiffness`、§3.6 门控 Verlet、§4.1 `step()`、§4.2 driver 可选。**动力学默认关，旧机构回归逐字节锁死**（46 项测试全绿）。
- **实例参数**（调参基线，待用户真机手感拍板后定版）：14 段 × 20px；弯曲刚度根 0.03 → 梢 0.003（per-sweep，随 28 遍复合——见 types.ts 注）；g = 900 px/s²（向下，决策点 ④）；damping = 0.998/子步（≈0.79/秒）。taper 渲染 13→3px（决策点 ④，做了）。
- **实测**（Playwright 真浏览器 + 单测）：悬垂静息 err 0.04px；侧拉 160px 松手过冲至 ~178px（甩鞭动量），摆过中线，12s 振幅衰减至 ~23%；暴力甩（目标 10⁴px）全程有限、链不散；切后台 dt=2s 不瞬移（子步封顶 6）。
- **决策点 ③ 的处置**：GH 参考录屏未提供，验收标准改为「用户在场调参拍板」（同 M3 流程，用户 2026-07-10 口头同意开工即视为选择此路）。若日后拿到 GH 录屏发现形态偏差，按 spec 修订处理。
- **已知局限（诚实入档）**：① per-sweep 刚度与遍数耦合（XPBD 才是正解，IDEAS 挂着）；② maxError 混计刚性杆与软杆，动态中读数含软约束偏差（HUD 仅示意）；③ dynamics 与 driver 不组合（触手无曲柄，暂无需求）。

## 拍板记录（2026-07-10，用户）

- **选路：B+C 一并立项**（软约束 + Verlet 门控动力学，§3），**排期在 M3（Session 4）之后**——原「触手不做」决策由此改判为「已立项、按队列执行」。
- 2026-07-10 追补：用户拍板删除 TOEFL 优先条款（备考与作品集两线独立）。本项仍按队列排 M3 后，不紧急、不催。
- §7 决策点 ③④ **仍开放**，动工前补齐即可：GH 参考录屏（或一句话验收标准）、重力方向、taper 渲染与否。
- 开工条件：M3 关闭 + 用户提供 ③ + 用户在场（调参手感需拍板，同 ROADMAP C 类）。

---

## 0. 参照物与本 spec 的盲区

- 参照物：`模型求解器参考/触手模拟1.3dm/.gh`（早期运动逻辑演示，软体/Kangaroo）。权威副本在用户机器上、已 .gitignore，**本会话读不到**——本 spec 的行为目标基于盘点描述写成。若立项，动工前需用户提供 GH 演示录屏，或口头给一句验收标准（「拖起来/松手后应该像什么」）。
- 外形以 `伸缩外壳1` 为准（盘点 §附录 A）——触手仅指柔性附肢的**运动逻辑**，不含外壳造型。

## 1. 目标与非目标

**目标**（若立项）：网页可交互触手——基座固定、尖端可拖，拖拽跟手，松手后有可信的松弛/摆动，观感支持「生命感」论点而非拆台。

**非目标**：软体体积/碰撞、肌肉驱动、与真机行为引擎联动、触手数量 > 1。

## 2. 内核现状盘点（2026-07-10 对照源码核实，非猜测）

| 事实 | 出处 | 对触手的含义 |
|---|---|---|
| 只有刚性距离约束——`Bar` 无刚度字段，投影恒全额 | types.ts / solver.ts sweepOnce | 软弯曲约束**必须改类型**（正是 IDEAS 陷阱区「绳索 = Bar 加 type 字段：现在不做、不改类型」那扇门） |
| 无动力学：纯位置投影，无重力/惯性/速度 | LINKAGE_SPEC §1.2「真动力学明确排除」 | 无外力时链条**松手即定格**——触手的「活」正好全在时间维上 |
| 软拖拽 α=0.4 + 每帧 40px 限步 | solver.ts | 拖拽手感现成，链条直接继承 |
| controller 必须有 driver（曲柄） | controller.ts ControllerOpts | 触手无曲柄——但可给**基座装微曲柄**当搅动源，spin⇄drag 状态机白得：松手回搅动、永不冻结 |
| 有限迭代（drag 36 遍/帧）造成传播滞后 | controller.ts | 长链尾端天然滞后于头端——**免费的伪柔性**，可利用 |

## 3. 方案阶梯（三级，红线各不同）

### A+ 级：纯求解器实例（零红线，拱环同款待遇）

刚性短杆链 12–16 段（总长 ≈300px，渲染由粗到细 taper），基座锚点 + 基座微曲柄（半径 ~8px、omega 慢）做常驻搅动，尖端 handle 可拖。**内核、controller、类型全部零修改**——只新增 `tentacle-data.ts` + 台架页 + 测试，工作量半天。

- 观感上限：**被缓慢搅动的水草/链条**。拖拽跟手、尾端滞后有柔感；松手回搅动模式，不会定格。但**无重力垂坠、无惯性甩尾**——不是触手，是运动链。
- 定位：台架玩具。**不建议上作品集**（对生命感论点无贡献）。

### B 级：软约束（改类型——解锁 IDEAS 陷阱区那扇门）

`Bar` 加 `stiffness?: number`（缺省 1 = 现行为，全部现有实例不受影响）。弯曲刚度 = 跨节点软杆（node i ↔ i+2，rest = 2×段长，stiffness ≈ 0.1–0.3）——数学上仍是距离约束，投影公式只乘一个系数。

- 收益：链条有形状记忆，拖拽呈平滑曲线，「绳」变「杆状软体」。同时为 IDEAS 第二梯队「绳索元素/tensegrity」铺路。
- 局限：**仍无时间维**——形状好看了，松手行为不变。**单独做 B 不值得**，只作为 C 的组成部分。工作量 +半天（改类型 + 回归全套）。

### C 级：Verlet 动力学（改内核——正面踩 LINKAGE_SPEC §1.2「真动力学排除」红线）

节点加 `px/py`（上帧位置），每帧投影前积分：`x += (x − px)·damping + g·dt²`，投影后照旧。这是 PBD 原始论文（Müller 2007）的完整形态，现内核相当于其 dt→∞ 的准静态特例。

- **门控铁律**：`dynamics` 为 opt-in 构造参数，缺省 undefined 时走现有代码路径**逐字节不变**；四杆/拱环不开启；现有 30 项测试 + 新增快照回归锁死「off = 旧行为」。
- 收益：松手后垂坠、摆动、指数衰减——**触手的活物感全部来自这里**。
- 风险（不粉饰）：① 调参黑洞（g、damping、段数、迭代数四者耦合）；② 变帧率稳定性——必须固定步长或 dt clamp（M2 切后台已有先例可抄）；③ 与 MAX_DRAG_REACH、软拖拽 α 的相互作用需重新手测；④ spec 红线正式失守，需在 LINKAGE_SPEC 记「§1.2 修订：动力学作为门控扩展解锁」。
- 工作量：1 个 session（内核 + 门控回归 + 调参 + 真机手测）。

## 4. 与论点、素材路的关系

- THESIS_NOTES「手感即论点」的主承载是**四杆的 M3 阻尼**，不是触手。触手做好了是加分项，做坏了（冻僵链/参数失调的抽搐）是**减分项**。
- case study 里触手的展示需求，素材路零代码可满足：GH 原演示录屏 / 真机视频——**本来就在盘点附录 B 素材缺口清单里**。网页触手不是必需品。

## 5. 建议（模型立场，用户拍板）

- 想要「活触手」：**B+C 一并立项**（约 1.5 session），排期在 **M3 之后**——M3 优先级更高（手感即论点）。
- 只想快速摸一下：A+ 半天可出，当台架玩具，明确不上作品集。
- 都不急：维持不做，走素材路。**按 §1.2 惯例，默认答案仍是「不做」。**

## 6. 验收与测试（若批准，Vitest 与实现同步写）

- 回归：dynamics 关闭时四杆/拱环全部现有测试通过 + 位置快照逐位一致。
- 新增：链条残差 ≤ 现行容差；暴力甩无 NaN（EPS 守卫复用）；C 级加「能量单调衰减」「切后台大 dt 不炸」；B 级加「stiffness=1 等价旧投影」。
- 真机触屏手测，标准同 M2（拖拽/双指/切后台/暴力甩）。

## 7. 待拍板决策清单

1. 选路：A+ / B+C / 维持不做？
2. 若 B+C：确认排期在 M3 之后？
3. GH 参考录屏（或一句话验收标准）何时能给？
4. 若 C：重力方向（竖直向下 or 可调）？渲染要不要 taper 触手造型？

---

*修订规则同 LINKAGE_SPEC：实现中发现本 spec 错误 → 改本文档并在 git message 注明「触手 spec 修订」。*
