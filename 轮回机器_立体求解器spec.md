# 轮回机器_立体求解器 spec

**版本**：v0.1（2026-07-10，用户立项并即日开工：「先把能转换视角的做了」）
**上游**：轮回机器_触手spec.md v0.5（平面版）、IDEAS 大招区立项记录。
**目标**：三肌腱 120° 分布的真机触手（触手模拟1.ghx 机制）在 3D 中可视、可驱动。

## 1. 范围

**渲染红线修订（2026-07-10 用户拍板）**：3D 研究线台架解锁 **WebGL**（零依赖裸
WebGL，不引 three.js）——SVG 画家算法历经四版（边线框→半透明三角→逐面不透明→
剪影+切片）仍无法给互穿零件像素级正确遮挡，用户否决后拍板解锁。z-buffer 逐像素
深度测试 = 物理精确遮挡；肌腱线同样参与深度测试。哑光纸墨风格不变。
**2D 连杆线仍守 SVG 红线**（LINKAGE_SPEC §1.3 不动）。装备：`gl3d.ts`
（FlatRenderer + bakeIndexed，静态 VBO/胞 + 逐胞刚架 uniform，9 次 draw call）。
**网格 v6（用户拍板「直接导入」）**：凸包减量废弃（SVG 时代遗产——无细节且复杂
支架碎成方块），模型嵌入渲染网格**全量导入**：10.7 万三角 / 0.05mm 逐零件焊接 /
剔除零面积，二进制资产 `tentacle3d-mesh.bin`（1.4MB，vite ?url 引入，运行时
bakeIndexed 展开平面着色流）；基座取干净文件全量（含支腿底板，37 零件）。
headless SwiftShader 软渲染 9fps（验证下限），真 GPU 满帧。
**连接件蒙皮 v7（用户纠偏 2026-07-11）**：节间盘轴联接件与方盒是**榫卯插接**
——方盒刚性材质、联接件 TPU 软材质，弯曲全部由联接件裸露段吸收、插接绝不分离。
v6 按零件质心分桶把联接件整只塞进单侧盒组，弯曲时视觉上从对侧插槽拔出（用户
否决）。修正三层：① 提取脚本零件级分类——x 范围跨节间边界（±1.5mm）的零件判
为联接件，单独成组 j0..j5（每缝恰 1 件，插接深度实测约 7mm/侧），blend = 两侧
方盒端面间裸露带（16–20mm）；② gl3d 双骨蒙皮通道（`bakeSkinned`+`drawSkinned`：
顶点权重 smoothstep(blend)，插接段恒 0/1 = 随盒刚动，裸露段在站 g/g+1 两刚架
间位置与法向同步混合）；③ 台架逐缝 drawSkinned。**求解器零改动**——弯曲柔度
本就全部在节间（方盒刚化 + 软杆 = TPU 弯曲刚度），与真机材质分工一致。
**二轮修正（同日用户复检「连接件没弯」）**：权重逐顶点，但联接件侧壁是通长
大面（中间无顶点），GPU 三角形内部只线性插值 → 整件渲染成直弦、弯角挤到
接口。bakeSkinned 加**沿臂细分**：与裸露带相交且 ax 跨度 > 1/8 带宽的边取中点
对分（带外 w 恒定 = 刚性，不细分）。
**三轮修正 v7.2（用户复检「弯得不自然」）**：位置线性混合（LBS）出来是
弦 + 中部软折、截面缩水。改**螺旋插值**：相对运动 CPU 分解为螺旋
（`jointScrew`：轴 k̂ 过点 c 转 θ + 沿轴平移 pitch），顶点着色器按 w 比例
**刚性**旋转/平移每个截面——截面不缩水；权重同步改**线性** ramp（smoothstep
废除）→ 等曲率圆弧 = 恒弯矩梁（TPU 柔性件）的真实形状。w=0/1 端点数学上
精确重现两侧刚架映射（单测锚定），插接不分离性质不变。

**v0.1 做**：
- **独立 3D 内核** `solver3d.ts`——距离约束 + 锚点 + stiffness + 门控 Verlet 的 3D 版。
  **与 2D 内核（solver.ts）完全平行，不改其一行**：2D 是封盘资产 + 教学叙事载体，
  3D 是研究线，各自演化。代码有意保持同构（公式同源 §LINKAGE_SPEC §3.1/§3.6）。
- 3D 触手实例：脊柱链 + 每节三导点椎盘（120°）+ 三肌腱软杆链 + 收缩 setRest（公式同平面版）
  ——**肌腱已升 v2 真机制，见 §1.7**（逐段软杆链收缩废弃）。
- **SVG 正交投影渲染 + 轨道相机**（yaw/pitch，拖拽转视角**不动模型**——用户设想）+
  画家算法深度排序 + 深度明暗提示。**WebGL 红线不碰**（LINKAGE_SPEC §1.2 维持）。
- 台架 `/demo/tentacle3d.html`（交互 v2，2026-07-10 用户反馈迭代）：
  三肌腱滑块 + **联动**（无/1+2/2+3/1+3——双腱同值同步，弯向夹角平分方向）+
  **放松**（目标归零、动力学回摆）与**归位**（硬复位：重建实例、笔直零速度）分立 +
  视角：**trackball 全自由旋转**（v2 修订，用户拍板「左右也要能看」——拖拽绕屏幕轴，
  姿态存旋转矩阵，无俯仰锁、可翻顶翻底；v1 turntable 绕世界竖轴转轴对称机构时
  画面几乎不变，读作「转不动」，废除）+ 基座参考虚线环（旋转的视觉锚点）+
  首次拖拽后自动旋转永久停止 + 滚轮/双指捏合缩放（0.5–3×）+ 视角归位。
  **默认机位横躺 + 正侧视（v3→v4 同日两轮，用户拍板 2026-07-10）**：基座在左、
  臂指右、**无斜倾**（`yaw0 = pitch0 = 0`，`roll0 = −π/2`——OrbitCamera 新增
  初始滚转参数，最后施加、不影响拖拽）；视角归位即回此正机位。
  **空闲自转 = 绕屏幕竖轴的整体横向环视**：v3 曾改绕臂轴（姿态不漂移、椎节
  原地自旋），用户否决——要的是整体横向转动，不是自转。

**v0.1 不做**：模型拖拽（3D 拾取 = 射线求交，另议）、碰撞、透视投影、WebGL、
性能优化（台架量级 ~140 杆无压力）、与 2D controller 的统一抽象（时机未到）。

## 1.5 架构（复用就绪——用户拍板 2026-07-10「空间机制以后要复用」）

3D 线的公共装备全部下沉到 `src/lib/linkage/`（零 DOM、有测试），台架页只剩
「数据实例 + 事件/SVG 接线」——**加新 3D 机构 = 新数据实例 + 新台架页，不改装备**
（对标 2D 的拱环先例）：

| 模块 | 职责 | 对标 2D |
|---|---|---|
| `solver3d.ts` | 距离约束/锚点/stiffness/门控 Verlet/**穿环滑缆**（3D） | `solver.ts` |
| `camera3d.ts` `OrbitCamera` | 正交投影、**trackball 自由旋转**（屏幕轴、矩阵姿态、无万向锁）、初始滚转 roll0（横躺机位）、屏幕竖轴空闲自转（整体横向环视）、捏合/滚轮缩放、自转让位、视角归位——指针逻辑零 DOM 可测 | `controller.ts` |
| `scene3d.ts` `projectScene` | 投影 + 画家排序（远→近）+ 深度明暗，纯函数 | 渲染约定 §4.4 |
| `motion.ts` `CriticallyDamped` | 肌肉缓动（零速起步/无过冲/jumpTo 归位），2D/3D 通用 | — |
| `tentacle3d-data.ts` | 触手实例（几何 + 收缩 API）——**机构专属** | `arch-data.ts` |
| `src/demo/tentacle3d.ts` | 接线与贴附——**机构专属** | 台架页 |

## 1.6 真实结构（v5，干净版 11.3dm——用户纠偏「是七节」并提供专用模型，2026-07-10）

- **数据源**：`模型求解器参考/11.3dm`（用户导出的触手专用文件）。此前从
  求解器结构演示.3dm 空间检测只找到 5 站——站 0（x=46.9）被错划进基座包围盒、
  站 6（x=404.7）孔太小未建成曲线，**正确结构是 7 方盒椎节**（x 46.9→404.7，
  节距 72→47 收锥）+ 基座舵机总成 + 节间盘轴联接 + 梢端盖。
- 肌腱孔方位 30°/150°/270°、半径 10.3→4.9 收锥（站 1–5 实测，站 0/6 收锥外推）。
- **物理两处 v5 修正（扭转治理，单腱深弯实测横向漂移 110 → 7mm）**：
  ① `solver3d` 换**对称 Gauss-Seidel**（正反向交替扫描，削顺序偏差；2D 封盘内核不动）；
  ② 椎盘刚架改**前后双邻站对称锥**（单侧拴结的锥面前倾在深弯下耦合出系统性扭矩）。
  副产收益：弯矩沿臂均布，深弯是光滑弧线而非根部折角。
- **渲染**（v4——三轮用户实测迭代定版）：**逐零件凸包 + 剪影多边形 + 不透明哑光**。
  提取：顶点量化 1mm → 3D 凸包（QJ 兜底救回共面薄板），**>30mm 长件沿主轴切
  25mm 片分别求包**（单一深度的画家排序对互穿长件必错——用户实测）；247 单元 /
  6k 三角。运行时：投影点 2D 凸包 = 精确剪影（凸体投影必凸，无剖分线无接缝，
  `scene3d.hull2d`），可见面积加权 lambert 定 12 级灰阶，逐单元质心深度画家排序，
  不透明填充 + 0.4px 描边。实测 60fps。历版教训入档：边线框（读不出体积）→
  面积抽样半透明三角（预算截断留洞 + 透视内部 = 碎玻璃）→ 逐面不透明（剖分线
  外露 + 互穿长件遮挡错乱）→ 本版。理论上限：画家排序对残余互穿仍非像素级
  正确，如需完美遮挡须解锁 WebGL（§1.2 红线，另行拍板）。

## 1.7 肌腱 v2——真机制复活：穿环滑缆（用户还原设计意图，2026-07-10）

- **设计意图（用户原话）**：「gh 文件里面的肌腱其实是一条完整的线，固定在最远端
  一个节点上，最根部的那一点往后拉，整体地被往后拉让触手弯起来。gh 现在这个
  分段一起收缩的写法其实只是一个妥协。」——即 GH/Kangaroo 因无滑索约束，用
  逐段等比缩短杆长模拟；实物是**一根完整缆线锚在梢节导孔、无摩擦穿过沿途导孔、
  从根部抽线**。本版在求解器里复活真机制，逐段收缩（v1）废弃。
- **约束数学**（`solver3d.projectCable`）：对路径 `C = Σ|pᵢ₊₁−pᵢ| − rest`，
  各点梯度 `∇ᵢ` = 相邻段单位向量之和，`λ = C·k / Σ wᵢ|∇ᵢ|²`，`Δpᵢ = −wᵢ·λ·∇ᵢ`。
  **单边**（oneSided 缺省 true）：`C ≤ 0`（松弛）零作用力——绳只拉不推。
  每次扫描末尾投影，锚点 w=0 严格不动。
- **API**：`Linkage3Def.cables`（rest 缺省 = 初始路径总长）、`setCableRest` /
  `cableLength` / `cables` getter；`maxError` 含单边缆的 `max(0, 超长)`。
  实例侧 `applyContraction3` = `setCableRest(rest0 − c·pullMax)`——
  **滑块语义从「收缩比例」变为「抽线行程」**（c=1 → 抽入 55mm）。
- **行为差异（真机制的物理红利，探针实测 2026-07-10）**：
  - 张力沿缆自动分配，**弯曲自发集中在阻力最小处**（梢段更软 → 曲率向梢集中，
    c=0.5 呈光滑 J 弧）；v1 逐段等比是人为均布，弧形是规定出来的。
  - 静息 err 0.000 原地不动；单腱 c=0.5 沿向 ≈203、横向 ≈−11（方位正确）；
    放松 15s 回直残留 0.0（单边松弛 = 完全零力，比 v1 软杆残余应力更干净）；
    三腱全收有界 r≈279。8/8 单测绿（新增缆线收敛 + 松弛零力逐位回归）。
  - **c=1 饱和**：55mm 行程后段几何拉到头，实现 ≈36mm、余 ≈19 转为缆内张力
    （HUD err ≈19 即此值，物理正确非数值病）。
- **pullMax=55mm 依据**：全周卷曲所需 ≈ 2π·r̄ ≈ 47mm，取 55 留深卷余量。
  行程上限与手感**待用户真机拍板**（嫌饱和段空行程可降至 ~45）。

## 1.8 肌腱 v3——真实走线：穿心 V 形（用户剖面图，2026-07-11）

- **设计意图（用户还原，二轮修订「不是穿心是绕心」）**：v2 的「一站一孔直穿」
  也是 GH 妥协。真实设计：缆线锚在梢端件，每节经**两端板腱孔**、节内下潜
  **越过轴线、绕过中央球体导件的背面**（球半径实测 12.2→5.9 收锥，`BALLS`
  提取自轴上零件顶点最大径，梢节锥度外推），节间贴端板孔跨缝——滑动缆在
  每个过孔/绕点的折角处把张力合力压在件上（|F| = 2λ·sin(折角/2)，指向内凹
  侧），「滑动产生推力」。绕点由活体节点实时算（站心沿本腱方位反向推
  球半径+1mm 余隙，`tendonVisual3`）。
- **几何**：端板腱孔 = 站孔径向位置平移到端板沿臂位置（`PLATES` 提取自
  11.3dm 网格 x 范围）；节点 `PLATE3(k,i,end)`，刚性挂装 = 本节脊柱 +
  前后双邻站（对称，防前倾偏置螺旋）+ 本方位与两侧邻方位导点。
- **约束路径只走端板孔（关键数值决策）**：节内 V 腿两端同挂一节 → 长度
  恒定 → 对刚体节净扳矩为零（内部走线不改变外部合力），力学与真 V 严格
  等价；若入约束路径，其大折角节点吃掉 PBD 梯度预算，肌力耗散在「挤压
  刚性模态 ↔ 杆弹回」死循环（实测卷曲锁死 45°、刚杆残差 10.9mm）。
  穿心 V 由渲染层按 `tendonVisual3` 原样画出。
- **配套三参数（实测定标）**：
  - `slack = 12`：装配松弛。拮抗腱路径在弯曲时变长 ≈ 0.5·r·θ，零松弛会把
    卷曲原地锁死（err 全是拮抗缆被硬拉的量）；12mm ≈ 容许 ~180° 总卷曲。
  - `maxStep = 6`（缆单遍投影修正上限，solver3d 新参数）≈ 舵机力矩上限：
    深抽饱和时张力有界，不把刚性结构挤出行程。
  - `fasciaK 0.2 → 1.0`：诚实走线下深卷滑进螺旋能量谷（实测梢端扭 50°、
    横漂 −154）；刚性斜杆 = 真机盘轴联接抗扭刚度的等效。实测不吃弯曲力
    （c=1 沿向 53→176）、横漂 −154→−43。
  - `pullMax 55 → 32`：缩短全部来自跨缝段（dL/dθ ≈ r̄ ≈ 7.5mm/rad·关节），
    ~200° 卷曲 ≈ 26mm；v2 的 55 标定于抄近道假杠杆，废除。
- **实测（c=1 单腱）**：逐关节折角 [10,16,19,25,30]°——J 形、曲率向软梢
  递增、无死折（≤33° << 盘面接触上限 ~60°）。**此前「梢端诡异死折」被
  诚实走线根治**（缆被按在结构上，不再抄近道碾梢端），拟议中的盘面碰撞
  挡块（单边下限杆）不再需要，搁置。静息 err 0.000、放松回直残留 0。

## 2. 已知风险与处置（入档）

- **扭转自由度（已修，3D-M3 提前执行）**：v0.1 首版无抗扭约束，扭转沿臂累积——
  **用户实测单腱收缩卷成螺旋，否决**。修正 = **fascia 双手性斜杆**（相邻椎盘导点
  交叉软杆，每隙 6 根，k=0.1）：扭转使全部斜杆同向变形 → 强抗扭；弯曲差动变形 →
  弱阻力。实测 c=0.5 横向漂移 38.5 → 1.2px，c=0.75 = 5.6px（平面 C 弯成立）；
  c=1 深卷仍余 ~20px 扭转，接受。
- **收缩冲击（已修，两轮）**：滑块跳变瞬移 rest = 给 Verlet 动量层注入冲击 → 抽搐/过冲
  （用户实测否决）。第一轮 = 恒速限速渐变（起步仍有速度突跳）；第二轮（用户拍板
  「平滑地从零开始收缩」）= **临界阻尼二阶跟踪**（ω=5 rad/s，速度零起步、平滑加减速、
  无过冲，满程 ≈0.9s）+ damping 0.992。保持态末端抖动 0.2px/帧。
- **重力取消（用户拍板 2026-07-10）**：触手是被驱动机构、非悬垂物——gravity=0，
  动量与阻尼保留。副产收益：静息完美直立（err 0.000）、释放回直残留 0.2px
  （原重力对抗下 ~3px）、收缩幅度更舒展（c=0.5 沿向 126 → 198px）。
- **画家算法**：逐元素平均深度排序，元素相交时可能闪序——台架可接受，不追求 z-buffer 正确性。
- 三腱同收的屈曲侧偏（欧拉失稳）：真物理，同平面版（触手 spec v0.5 已档）。

## 3. 里程碑

| | 交付 | 状态 |
|---|---|---|
| 3D-M1 | solver3d + 3D 触手实例 + 单测（投影收敛/锚点/自由落体/setRest/确定性） | 完成 |
| 3D-M2 | 轨道相机台架 + 三肌腱滑块 + 真浏览器验证 | 完成 |
| 3D-M3 | fascia 抗扭 + 肌肉限速（用户实测反馈驱动，提前执行）；3D 拾取拖拽未排 | 部分完成 |
| 3D-M4 | 肌腱 v2 穿环滑缆真机制（§1.7：单缆锚梢穿孔、根部抽线、单边零推力） | 完成（pullMax 待手感拍板） |

*修订规则同 LINKAGE_SPEC。*
