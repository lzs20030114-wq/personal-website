node_modules/
dist/
.next/
next-env.d.ts
*.local
*.tsbuildinfo

# Rhino 锁文件不进 git（模型文件夹本体已于 2026-07-10 应用户要求入库）
*.rhl

# Vite 台架构建产物（build 时生成，随部署上线，不进 git）
public/demo/

# 站内台架组件用的资源副本（prebuild/predev 生成，不进 git）
public/mesh/
