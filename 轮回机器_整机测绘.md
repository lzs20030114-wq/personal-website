# 轮回机器 · 整机测绘（729新参考.3dm）

> 参考文件：`模型求解器参考/729新参考.3dm`（48.9 MB，2026-07-29 用户上传，commit `03b9f9b`）
> 测绘脚本：`scripts/model-extract/measure_729.py` → `scripts/model-extract/machine_729.json`
> **本文件的数字全部由脚本产出，手改无效**——改脚本重跑。单位：mm。
> 状态：**测绘完成，Lab.05 范围未定**。本文只记录「文件里有什么、量到了什么」，不含实现决定。

---

## 1. 一句话结论

这是**整机装配图**，不是又一张环的图纸。前四个参考文件给的都是单个机构的形状；这个文件第一次把**传动链**画全了——一根中间轴、五个曲柄、五根连杆、五副滑轨架、底盘、三条触手、舵机总成，而且五个环的机构参数彼此闭合到小数点后四位。

用它能回答的问题，之前只能靠推断。

---

## 2. 图层结构

38 个图层，分两簇（按 x 坐标天然分开）：

| 簇 | 位置 | 内容 |
|---|---|---|
| **摊开位** | 原点附近，全部落在 z=0 平面 | `摊开骨架::S1..S5::{销, 杆三角, 导轨, 驱动}` —— 五个环各自摊平在 XY 面上的运动学骨架 |
| **装配位** | x ≈ −5489，y ∈ [−1474, −1104] | 同样五个环立起来插进整机，外加 `中间轴`/`中间轴驱动`/`驱动杆`/`实体_单杆轮`/`滑轨架`/`骨架`/`大触手`/`小触手` |

摊开位是给求解器用的（平面机构，直接就是二维内核的输入形状）；装配位是给整机用的（刚体位姿）。同一套骨架画了两遍，这是这个文件最省事的地方——**平面解算与三维摆位不用我们自己配准**。

`图层 01` 是原点附近的一组舵机总成样件（4 个 SG90 图块 + 若干 Body），不在整机里，是零件详图。

---

## 3. 五环机构参数（摊开位，地线 y=0、中线 x=0）

| 环 | 基型 | 比例 | 曲柄 r | 连杆 l | 导轨行程 [下, 上] | 行程长 | 中线拱顶 y | 最高销 y | 半跨 x | 销 | 三角板 | 装配面 y |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| S1 | M3 | 1.057 | 29.882 | 97.096 | [67.214, 126.978] | 59.764 | 126.294 | 126.294 | 167.621 | 23 | 14 | −1112.406 |
| S2 | M1 | 1.184 | 48.928 | 173.512 | [—, 222.440] | — | 221.228 | 221.228 | 172.618 | 23 | 14 | −1197.406 |
| S3 | M5 | 1.584 | 34.733 | 124.195 | [89.462, 158.928] | 69.465 | 157.275 | 157.275 | 161.915 | 17 | 10 | −1282.406 |
| S4 | M3 | 1.000 | 28.096 | 91.978 | [63.882, 120.075] | 56.193 | 119.216 | 119.216 | 158.623 | 23 | 14 | −1367.406 |
| S5 | M3 | 0.870 | 24.178 | 80.345 | [56.167, 104.522] | 48.356 | **103.281** | **103.855** | 138.052 | 23 | 14 | −1452.406 |

> **「拱顶」= 中线上最高的销**（驱动接的就是它），不是全环最高销。
> 四个环两者相同，**只有 S5 不同**——它的最高销在 x = −27.135 处（103.855），
> 比中线拱顶（103.281）还高 0.574。首版本表只列了「最高销」并标作拱顶，
> S5 一栏因此是错的；两列并列以杜绝再误读。

图层名里的 `M3x1.057` = **基型 M3、相对 S4 放大 1.057 倍**。S4 是 1.000 的基准环。三个基型：M3（S1/S4/S5，23 销 14 板）、M1（S2，23 销 14 板）、M5（S3，17 销 10 板）。**S3 的异拓扑因此有了出处**——它不是画错，是另一个基型。

每环四个落地销（S4：x = ±147.900, ±158.622），即每侧两只脚。

---

## 4. 四项校验（这些是文件的价值所在）

### 4.1 导轨端点 = 曲柄滑块的两个极限位

导轨（`导轨` 图层的两个点）画的就是滑块行程的上下端。逐环验证 **导轨端点 ≡ l ∓ r**：

| 环 | 上端偏差 | 下端偏差 |
|---|---|---|
| S1 | +0.0000 | +0.0000 |
| S2 | +0.0001 | 图上只画了一端 |
| S3 | +0.0001 | +0.0000 |
| S4 | +0.0001 | +0.0000 |
| S5 | −0.0000 | −0.0001 |

也就是说**顶部止程不是估的、也不用真机标定**——它由曲柄半径和连杆长度解析确定，图纸只是把结果标了出来。S2 少画的那个下端点按同一条式子补出来是 124.585。

### 4.2 曲柄圆 = 驱动点半径

`驱动` 图层里那条 73 点闭合多段线是曲柄圆，半径与驱动点到原点的距离**五环全部零偏差**。曲柄中心在地线上、机构中线处（x=0, y=0）。

### 4.3 五个曲柄同相，且同轴

装配位五个曲柄销的 x 坐标**完全相同**（−5488.623），即五个曲柄在图纸姿态下**全部竖直朝上**（上死点 = 全开位）。各环由销高反推轴心：

| 环 | 销 z | 反推轴心 z = 销z − r |
|---|---|---|
| S1 | −10.368 | −40.250 |
| S2 | +8.678 | −40.250 |
| S3 | −5.517 | −40.250 |
| S4 | −12.153 | −40.250 |
| S5 | −16.072 | −40.250 |

五条相互独立的推导给出同一个数，小数点后三位全同。**中间轴轴心 = (x −5488.623, z −40.250)**。

> 注：`中间轴` 实体的包围盒中心是 z = −40.021，与轴心差 0.229——因为轴是带切边的 D 形截面，包围盒中心不等于轴心。**以五环反推的 −40.250 为准**，不要用实体包围盒。

### 4.4 摊开位 → 装配位 是纯刚体变换

四个导轨端点齐全的环，`y_flat − z_asm` 恒为 **40.2496**，即摊开图的地线 y=0 正好落在中间轴轴心高度上。变换写全就是：

```
x_asm = x_flat − 5488.623
y_asm = −1112.406 − 85·(n−1)        n = 1..5
z_asm = y_flat − 40.250
```

### 4.5 五环站距 = 85.000

相邻装配面间距逐对量得 85.000 / 85.000 / 85.000 / 85.000。

---

## 5. 传动链（这部分是全新的）

| 图层 | 件数 | 量到的 |
|---|---|---|
| `中间轴` | 1 | 沿 y 长 370.0，贯穿全部五个环面 |
| `实体_单杆轮` | 5（+4 件原点侧样件） | 每环一个单臂曲柄，键在同一根轴上；轮的 z 向尺寸逐环 = r + 约 16 的轮毂，与表 §3 的 r 排序完全一致 |
| `驱动杆` | 5 | 每环一根连杆，5.5 厚的板，贴在环面旁约 3；z 向尺寸逐环 = l + 约 11 的端头 |
| `中间轴驱动` | 2 | 轴上一个 19×11×19 的小件（联轴/齿）+ 侧下方 92×63×61 的电机箱体 |
| `滑轨架` | 5 | 每环一副，22.6 厚，x 向跨度比该环全跨大 15–22 |
| `骨架` | 8 | 底盘：两块 100.6×362.6 侧板 + 两根纵梁 + 四根横梁，z ≈ −60 |

**读出来的机器**：一台电机 → 中间轴 → 五个不同半径的单臂曲柄（同相）→ 五根不同长度的连杆 → 五个环各自的拱顶滑块。一个自由度驱动全机，五个环因 r/l 不同而行程不同、但相位锁死。

> **轴只在 180° 内往复，不整周转**（用户 2026-07-29 告知，图纸看不出来）。故机器做的是张合而非循环转动。
> 区间两端恰是曲柄滑块的上下死点，**180° 正好走完整个行程 2R**——本节的表与 §4.1 的止程结论不受影响，
> 只是「整周」这个词在这台机器上不成立。

触手：`大触手` 1 条（图块 20，247 个子对象）在环组之外（y ≈ −967，沿 y 长 531）；`小触手` 2 条（图块 22，20 个子对象）分列机器左右（x −5607 与 −5370）。图块内是实体和曲线，**不是机构定义**——形态能取，运动学取不到。

---

## 6. 与站上既有实现的关系

- **S4 环几何与 07-10 提取的完全一致**。抽查 `arch-data.ts` 节点 0 反算得 (129.401, 50.826)，与本文件逐位相同；`ARCH_CRANK_RADIUS` 53.3834 px ÷ 1.9 = 28.097 mm 亦同。**新文件不推翻旧提取，是同一套图纸的整机版。**
- **85mm 站距**：CLAUDE.md 记的是「展示排推断值待真机确认」，本文件是装配图，逐对量得 85.000 ——**推断值坐实**。
- **同相呼吸**：Lab.04 的同相假设，本文件 §4.3 证实（五曲柄同轴同相）。
- **顶部止程**：§4.1 给出解析值。这是**顶部导轨**的止程。
- **脚槽止程**：本文件**没有**画脚槽几何。`arch.ts` 的 `ARCH_FOOT_SLOTS` 与 `shell3d.ts` 的逐环余量标定 [0,2,4,8] 所依据的仍是仿真扫掠包络，本文件不能替换它们。真正可能藏着槽长的是 `滑轨架` 的五块实体（槽是铣出来的孔），需要从 Brep 面里反解——**尚未尝试，成不成不敢打包票**。

---

## 7. 已知缺口

| 缺口 | 影响 |
|---|---|
| S2 导轨只画了上端 | 无实害，下端由 l−r 解析补出 = 124.585 |
| 无脚槽几何 | 五环脚槽止程仍是仿真推断值，见 §6 |
| 触手图块只有形体没有机构 | 触手若要在整机里动，只能挂现有触手求解器，或只做静态摆件 |
| 电机/减速比未标 | 转速、方向、行程用时全无出处；做动画只能自定速度，不能声称还原真机节律 |
| `图层 01` 舵机总成用途未标 | 4 个 SG90，猜是触手驱动，但图上没写，不臆断 |

---

## 8. 复现

```bash
cd scripts/model-extract
python -m venv .venv && .venv/bin/pip install -r requirements.txt
.venv/bin/python measure_729.py ../../模型求解器参考/729新参考.3dm -o machine_729.json
```

`machine_729.json` 含：轴线、五环全部参数（含 23/17 个销坐标与 14/10 块三角板顶点）、32 个图层的类型与包围盒普查。
