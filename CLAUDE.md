# CLAUDE.md

作品集网站核心组件（PBD 平面连杆机构）的工作目录。Next.js 脚手架尚未搭建——见下方状态。

## 文档地图（用户说人话即可调档，不要求记文件名）

| 用户可能怎么说 | 打开 |
|---|---|
| "AI 披露"、"披露声明"、"用 AI 吃不吃亏" | AI_DISCLOSURE.md |
| "申请调研"、"截止日期"、"哪些学校要 PDF" | ADMISSIONS_RESEARCH.md |
| "论点"、"生命感"、"轮回机器笔记" | THESIS_NOTES.md |
| "轮回机器盘点"、"装置档案"、"五环参数/硬件/研究方法" | 轮回机器_盘点.md |
| "工作日志原稿"、"项目 1 日志双语稿"、"log 条目从哪来" | 轮回机器_工作日志原稿.md（站上 log 条目的双语来源，未删减） |
| "日志的表"、"表格附录"、"人格参数表/五环几何表/采购表在哪" | 轮回机器_工作日志表格附录.md（项目 1 全部表格 + 上站与否逐表说明） |
| "项目 2 日志原稿"、"空间仿真日志"、"双层智能体日志" | 项目二_工作日志原稿.md（项目 2 的 log 条目双语来源，未删减；项目尚未定名） |
| "智能床日志原稿"、"睡眠项目日志"、"慕思/未来实验室" | 智能床_工作日志原稿.md（智能床项目的 log 条目双语来源，未删减；暂列 other work） |
| "案例页骨架"、"轮回机器页面结构"、"图目录" | 轮回机器_案例页骨架.md |
| "展示逻辑"、"总框架"、"主论证链"、"四出口" | 轮回机器_展示逻辑总框架.md |
| "点子"、"发展方向"、"画廊/余族/反问题" | IDEAS.md |
| "路线图"、"铺路计划"、"什么给强/弱模型做" | ROADMAP.md |
| "spec"、"规格"、"数学推导/容差/里程碑" | LINKAGE_SPEC.md |
| "网站架构"、"脚手架"、"内容池/双轨/部署" | SITE_SPEC.md |
| "自己传日志"、"后台/编辑器"、"studio 怎么登录"、"环境变量配哪些" | SITE_SPEC.md §12（`/studio` 编辑后台） |
| "设计稿"、"对稿"、"案例页/主页/Lab 稿在哪" | design-ref/（`.dc.html` 副本 + MAPPING.md 映射表） |
| "横屏排版"、"版式研究"、"排版原则" | LAYOUT_NOTES.md |
| "网站 review"、"调研报告"、"获奖站案例"、"弱模型工单" | SITE_REVIEW_2026-07.md |
| "拱环"、"S4 环"、"真机机构求解器"、"伸缩外壳演示" | 轮回机器_拱环求解器.md |
| "整机"、"729新参考"、"传动链/中间轴/曲柄"、"整机测绘" | 轮回机器_整机测绘.md |
| "底盘桁架"、"方形框架减薄"、"框架有限元"、"FEA" | 轮回机器_底盘桁架FEA.md |
| "Lab.05"、"整机台架"、"整机 spec" | 轮回机器_整机spec.md |
| "五环"、"伏丘立体"、"壳体编排"、"shell3d" | 轮回机器_五环立体spec.md |
| "触手"、"触手 spec"、"绳索/软体/动力学扩展" | 轮回机器_触手spec.md |
| "立体"、"3D 求解器"、"轨道相机/三肌腱立体" | 轮回机器_立体求解器spec.md |

## 定位与边界（网站 IA——用户拍板 2026-07-07，非讨论稿）

- **作品集主体 = SlideRoom 提交的 25–30 页 PDF 里的四个主项目**；本网站 = 容器 + CV 上的一个链接。
- 连杆组件 = 四个主项目之一「**轮回机器**」（英文名/slug 待用户定）的**结构实现部分**，角色仅三重：
  ① 网站签名件——主页 hero 的作用等于封面，**不等于目录**；
  ② 「轮回机器」case study 的深度证据段——交互文章、教学图示、收敛可视化全部挂其 `/work/[slug]` 之下，**不单独成路由**；
  ③ 申请叙事中「求解器实现者」身份的那一个干净实例。
  **它不是第五个项目，不给独立展区。**
- IA 要求：主页 = hero 机构 + 极简文字 + 四项目索引，四项目结构地位同等，主页文案不围绕连杆长篇解说；FIG 题栏加入口链入「轮回机器」case study——hero 是通往项目的门，不是独立神龛。
- 时间硬边界：连杆按既定计划完成即**封盘**——之后只修 bug、不加功能，模型**不得主动提议打磨它**；网站建设优先级 = 四个项目的 case study 页面与内容 > 主页组件周边 > 其他。（原「TOEFL 优先于一切作品集工作」条款经用户拍板于 2026-07-10 删除——备考与作品集两线独立，互不挂钩。）
- 分工防漂移：本文件只管 IA 与定位，**不复制求解器数值**；求解器技术硬事实（代码、几何、不变量、容差）以 LINKAGE_SPEC.md 与账号级 skill `linkage-pbd`（稍后安装）为准。

## 必读

- **LINKAGE_SPEC.md 是唯一权威来源**：数学推导、API、边界情况、里程碑、测试容差全在里面。动手前通读；实现与其 §3 公式不符时，改实现不改公式；想加范围之外的功能，先看其 §1.2，默认答案是「不做」。
- 用中文交流；直接、诚实、先讲约束和风险，不乐观粉饰。
- IDEAS.md：发展方向的策展清单（著名机构画廊、余族定理、综合反问题等）——**不改变当前范围**，动任何一项前先写 spec。
- THESIS_NOTES.md：**连杆属于「轮回机器」项目**（机器的生命感论点），手感即论点——Session 4 的阻尼/衔接是论点本身不是抛光，不许砍。
- 教学义务：关键数学（约束投影、权重分配、收敛性）要讲到用户能在文书/面试里脱稿复述。

## 当前状态（2026-07-07）

- Session 1 完成：LINKAGE_SPEC.md v1.1（含评审修订）。
- **Session 2 / M1 完成**：`src/lib/linkage/`（solver / presets / trace）+ 单测全绿 + typecheck 干净 + vanilla 验收页（`npm run dev`，脚手架落地后改名 `npm run demo`——见 SITE_SPEC §2）。实测数字与两处 spec 修订均已回填 LINKAGE_SPEC（数值一律以彼为准，此处不复制）。
- **Session 3（M2）关闭**：交互状态机抽为纯类 `LinkageController`（零 DOM、驱动参数化，vanilla 页与 React 封装共用）+ 23 项测试全绿 + 浏览器验证（含 mobile 视口缩放拖拽）。**真机触屏手测通过（2026-07-07，用户在 Vercel 部署页实测：拖拽/双指/切后台/暴力甩均无异常）**。已部署：GitHub `personal-website`（私有）→ Vercel 自动部署，push 即上线。
- **M3+M4 关闭（2026-07-10，用户真机手感拍板通过）**：release 阻尼（继承甩动末速、指数松弛接回，默认参数即定版，τ=0 = 论点实验硬切开关）+ 耦合曲线实时描绘 + 工程标注/题栏 + 架构注记入档。36 项测试绿。**四杆连杆按 SPEC §8.1 全里程碑完成，正式封盘：只修 bug、不加功能、不主动提议打磨。**
- **网站 S1+S2+S3 完成（2026-07-13）**：Next.js 16 脚手架、内容池、四路由、占位插槽系统与轮回机器八段骨架页就位；正式 React `LinkageFigure` 复用 solver/controller，已挂主页 FIG.01 与案例页 FIG.12，含 SSR 首帧、IntersectionObserver 停启和 reduced-motion。正文仍全为作者意图占位，模型不代写。余项：S4（OG/check-links）。
- **拱环求解器实例完成（2026-07-10）**：真机 S4 环（角化剪式拱 + 曲柄滑块）从 `模型求解器参考/求解器结构演示.3dm` 提取为求解器实例——**内核零修改**（导轨=超长杆近似、扁板加支撑节点防镜像隧穿），复用 controller，台架 `/arch.html`（`npm run demo`），`arch.test.ts` 7 项 + 全套 30 项绿。决策与实测见 轮回机器_拱环求解器.md。触手模拟明确不做（软体，范围外）。
- **网站 review 完成（2026-07-10）**：四路外部调研（获奖站/开源架构/动效/叙事）验证现有方向，无需推翻。产出 SITE_REVIEW_2026-07.md：§二 代码发现、§九 弱模型工单（11 条，随时可派）。三项拍板已通过并入 SITE_SPEC 修订（§1 hover/active 最小集放宽、§4 about colophon 席位、§8 字体换自托管 Source Serif 4）。
- **台架静态上线（2026-07-10）**：`build`=`vite build && next build`（SITE_SPEC §2 修订），台架双页随 Vercel 部署上线——线上 `/demo/`=四杆、`/demo/arch.html`=拱环（产物落 `public/demo/`，.gitignore，构建时生成）。本地 `npm run demo` 行为不变。用户拍板推 master 上生产。
- **立体求解器 3D-M1+M2 完成（2026-07-10，用户当日立项当日开工）**：独立 3D 内核 `solver3d.ts`（与 2D 内核平行、不改其一行）+ 三肌腱 120° 立体触手（GH 机制全量升维，contraction1/2/3 全部可用）+ 台架 `/demo/tentacle3d.html`（**拖拽=轨道相机不动模型**、三肌腱滑块、SVG 正交投影 + 画家排序 + 深度明暗，WebGL 红线不碰）。59 项测试绿；真浏览器验证视角旋转（err 不动）与三腱各自弯向自身方位。**用户实测否决首版（单腱收缩成螺旋+抽搐）→ 3D-M3 提前执行：fascia 双手性抗扭斜杆（横向漂移 38.5→1.2px，平面 C 弯成立）+ 肌肉限速渐变（1.2/s）+ damping 0.992**。spec = 轮回机器_立体求解器spec.md v0.1（§2 处置记录）；3D 拾取拖拽未排。后续同日迭代：重力取消+临界阻尼收缩（用户拍板）、trackball 全自由旋转+参考环（用户拍板「左右也要能看」）、装备复用化（camera3d/scene3d/motion 下沉 lib）、**真实结构（用户逐轮纠偏迭代至 v5）：几何取自用户提供的干净模型 11.3dm——7 方盒椎节 + 基座舵机总成，站心/孔位/杆长全真实（spec §1.6）**；渲染历经边线框→半透明着色→剪影切片四版后，**用户拍板解锁 WebGL（仅 3D 线，2D 连杆仍守 SVG 红线）**——零依赖裸 WebGL z-buffer 物理精确遮挡（装备 gl3d.ts）。
- **五环立体编排 v0.2（2026-07-17，用户当日立项 + 两轮纠偏）**：S1–S5 全族提取（`gen_shell3d.py`，S3 异拓扑通吃）→ 五个 2D 环实例（内核零修改）+ 刚体位姿嵌入 3D（85mm 等距站位用户拍板、站距是展示排推断值待真机确认；**v0.2 纠偏：无 roll、五环全竖直四脚共地**——盘点 §7 roll 列非装配侧倾，语义待澄清）+ gl3d/camera3d 装备零修改复用。同相呼吸 + 每环脚槽两端止程 + 1/120 定步（跨设备一致）。台架 `/demo/shell3d.html`（trackball + φ 滑块，开场轴测机位用户截图圈定）。**v0.3 S3 冤案平反（用户观察「开局对、随即坍缩」破案）**：S3 图纸无辜（等臂/闭合与全族一致），坍缩是外止程钉死图纸位堵死其折叠分支（需先外冒 ~2mm 再收拢）；而一律放宽又会放跑 S5 进压平分支——**止程余量逐环阶梯标定**（[0,2,4,8]mm 按扫掠健康度取首个；实测 S3=2、其余=0），标定后全族峰值 0.24–0.42mm、五环动作同款。机理发现与细节见 轮回机器_五环立体spec.md。
- **拱环止程转正（2026-07-17，用户双机分岔事故后拍板）**：四脚零刚度滑移模态（曲柄只锁拱顶高度、槽端点从未入模）致同一 URL 两台设备形态各异（一台几秒并拢压扁——合法解不是算错）。修正三件套全在实例/台架层、内核零修改：双向槽端钳制（`ARCH_FOOT_SLOTS`+`archStopPass` 与投影交错；外端=全开位、内端=模型运动学行程实测，真机标定后替换）+ 定步积分（`ARCH_STEP_DT`=1/120 累加器，任何设备同一轨迹）+ 槽线改静态设计范围（取代 07-16 运行时包络，前提已变）。168 测试绿；真浏览器整圈峰值残差 0.562px。细节：轮回机器_拱环求解器.md 止程转正条目。
- **触手肌腱驱动版完成（2026-07-10，待真机手感拍板后关闭）**：用户纠偏（v0.3 被动软链方向错误）后，从用户上传的 `触手模拟1.ghx` 逐组件解析出真机制——16 刚体椎节 + 3 肌腱 × 14 段 Kangaroo Spring + contraction 滑块按公式收缩 rest。平面映射（用户拍板：先平面，**立体求解器研究已立项**——见 IDEAS 大招区）：椎板横杆三角化 + 左右双肌腱软杆链 + `solver.setRest` 收缩驱动（SPEC §4.1 修订）+ 门控 Verlet。台架 `/demo/tentacle.html` 双滑块（左/右肌腱）+ 拖拽。49 项测试绿；真浏览器实测：c=0.5 J 形卷曲、c=1 全螺旋、放松回直。**余项：用户真机拍板（g/damping/肌腱刚度/卷曲手感）。**机制与局限：轮回机器_触手spec.md v0.5。
- **主页交互重设计（2026-07-23 设计冻结 / 2026-07-24 落地）**：landonorris.com 拆解（五机制）→ Claude Design 多轮迭代（Monet 手风琴稿否→频道舞台→整屏分幕→卡片枢纽）→ `Home-Screens.dc.html` 逐字副本入 `design-ref/`：三幕整屏（S0 卡片组+预览舞台三态 / S1 About / S2 Lab+Log+页脚）+ 阻力翻页引擎（wheel 接管、手感常量表）+ 光标读数。映射与落地注意 = design-ref/MAPPING.md §6。**两项已拍板（2026-07-24）：Stage 默认位先空着（落地按稿内占位实现，不自行填充）；克制条款冻结不回填（SITE_SPEC §1 原文不动，主页差异以 MAPPING §6 为准、视为主页专属例外）。**落地（2026-07-24）：主页重写为 `components/site/HomeScreens.tsx`（引擎常量原样移植、Stage 空占位、reduced-motion/<1024 回落文档流）+ nav/页脚移入 `app/(site)/` 路由组（主页无顶导、页脚在 S2 幕内，/#lab 深链引擎态直落 S2）；统计条改实测 103 tests（文档旧数 168/176 系漂移）；typecheck + 103 测试 + 双构建全绿，SSR/内页路由冒烟通过。余项：用户真机手感拍板（COMMIT_DIST 等常量可调）。**迭代稿同步（2026-07-26，用户上传 standalone 导出）**：视觉全面换 om 语言（发丝线/网格/颗粒/深色渐变段、卡片改发丝线列表项、顶缘进度条、双色内核编码）+ 新增 goPT 页面转场（媒体块生长跳页，SPA 适配 router.push；卡片点击从驻留改跳页）；紫色由设计源启用；翻幕扫光+stagger 已移植但默认关（07-24 静置拍板维持，启用翻 FX.staggerFx 一处常量）。映射与决定=MAPPING §6.1。余项：真机手感拍板（含扫光是否启用）。**case/log 深色化 + 转场进入段落地（2026-07-27）**：迭代稿 standalone 内含的 pgCase/pgLog 深色改版补齐落地——`.pg-dark` scope 重定义语义 token（现有组件类零改自动深色）+ ground-plane 深底 + 深色 nav 变体 + case hero 着陆块；新增 `PageEnter` client 组件补齐 goPT 的转场进入段（着陆平面 clip 揭开，强兜底防白屏，CDP 实测残留归零/直接访问/​reduced-motion 全过）。返回转场与 /about 深色未做（about 现仍浅色，三内页两深一浅待拍板）。映射=MAPPING §6.2。case 仍内容池+MDX 驱动、四路由不动、连杆零改。**Lab 台架进站（2026-07-27）**：Lab-Modernist 稿判定——稿内 `lab-kernel.js` 自述是我们 TS 内核的 1:1 移植，**没有内核改进**，可搬的是表现层（暗色 HUD / 可嵌入 / 规格表）。落地 `components/lab/*` 四台 React 台架（FourBar / Arch / Tentacle / Rings），**直接 import 站内 TS 内核**、渲染画法照搬 `src/demo/*.ts`；`src/demo/*` 与 `npm run demo` 零改（避回归）。★3D 用**真实扫描网格**（10.7 万三角，prebuild 复制到 `public/mesh/`）——设计稿因导不进 .bin 退化成程序化方盒，本站不接受（用户拍板）。新增 `/lab` 深色路由；S2 Lab 四卡与 nav 改指 `/lab`；**Stage 默认位放入活的四杆台架**（用户拍板，此前空占位）。映射=MAPPING §7。**Stage 轮播 + 项目 01 临时主图（同日追加，用户拍板）**：待机位改为四台台架顺序轮播（4s/台、懒挂载、非当前片停跑、交互即暂停 15s）；项目 01 预览主图临时用 Lab.04 五环活件顶替占位块（作者供图后换回）；3D 台架加 `controls` 开关，主页语境不出控制条。映射=MAPPING §7.6。**对稿返工（同日，用户指出视觉细节全丢）**：首版是「剥掉 style 后读稿」照骨架重建，丢了蒙皮/配色/控制条/版式度量。已按稿返工：Lab 页版式（300px 左栏、竖排规格行、3px 彩色顶线）+ 台架 HUD/控制条 CSS 忠实移植 + 五环补齐**织物蒙皮 / 族系配色（S1 绿→S5 紫）/ 视角预设 + 透视**；gl3d 加 drawDynamicMesh 可选明暗端色（加法式，旧调用零影响）。教训入档：**读设计稿不许剥样式**。映射=MAPPING §7.7。

- **Work log 并入项目 1 + 双语化（2026-07-27，用户拍板）**：用户提供的「轮回机器」2026.03–07 工作日志 19 条并入 log 内容池（原稿存 轮回机器_工作日志原稿.md，站上条目是结论式压缩、不是逐句翻译）；条目 `lead`/`body`/标签全部改双语对象，zod 强制中英两份齐全（缺一构建期 fail）。`/archive` 加 EN/中文 滑块（**Log 页专属，全站不做 i18n 路由**，选择记在 localStorage、SSR 首帧恒 EN）；版式按用户「不要全堆在一起」重排（引句独占一行、正文另起限宽、日期+标签退左栏、按月分组）。主页 S2 的 Log 预览由硬编码改接内容池最新三条。新增守门测试 `src/lib/site/log.test.ts`（排序 / 同日 tie / 双语补全 / 译名一致），全套 108 测试绿；统计条同步 103→108。已知偏离：中文标题走系统 CJK 回退字体（Source Serif 4 无中文字面）。映射=MAPPING §8。**同日两轮返工**：① 文案返工——首版写成警句加内部黑话（封盘/止程转正/全族峰值残差），用户指「文绉绉、看不出做了啥」，27 条中英全部改写为说明性文字（做了什么→为什么→结果如何，术语首次出现即解释）；② 条目改**折叠展开**（收起只显示一句概括，点开看详情，默认全收起，键盘可达），主页预览随之只取 lead。**图/表格插槽已预留但本轮不实现**（内容池加可选 blocks 字段 → 面板内正文之后渲染，落点已在组件注释标出；zod 不加未使用字段）。见 MAPPING §8.5。

- **项目 2 日志并入（2026-07-27，用户拍板「与项目 1 同样的要求」）**：项目 2「双层多智能体空间仿真（真实的人，梦境的空间）」2026.06–07 日志 11 条按同一套要求入池（原稿存 项目二_工作日志原稿.md；条目为结论式压缩 + 说明性文字，术语首次出现即解释）。**log 从此是多项目共用的主线记录**——机制零改（无新字段/组件/路由），项目归属靠新增的 outline 项目桶标签 `Space 空间`（与 `Machine 机器`/`Lab 实验室` 平级）；新增守门测试「每条至多 1 outline + 1 neutral」防两个项目桶挂同一条并守住左栏宽度。池 27→38 条，统计条 108→**109**。两条区间日期钉成单日（06 上旬→06-07、07 中下旬→07-24，后者正文注明记录读不回来、日期为近似）；同日 tie 一律既有条目在前、项目 2 在后。项目 2 尚未定名（原稿暂定名《孤室》已被其 07-09 愿景修正推翻，`content/work/project-ii` 仍占位），故原稿文件名用「项目二」，定名后连同 slug 一并改。映射=MAPPING §8.6。

- **Log 项目筛选（2026-07-27，用户拍板「可选只看某一个项目，也可总览看全部」）**：`/archive` 标头下加筛选条（全部=默认 + 每个项目桶，各带条数；点已选中的 = 取消回总览；筛选态出「显示 38 条中的 N 条」读数）。**桶 = outline 标签现算**（`getLogBuckets()`，条数多的在前、同数按 key 序），不新开字段、不加路由。前置条件是每条恰好一个 outline——原先 3 条没有桶（会在所有筛选视图里消失）已归位：两条建站条目 `Site` 由 neutral 升为项目桶，连杆 spec 那条补 `Lab`；守门测试从「至多 1 个」收紧为「恰好 1 个」+ 新增桶测试。四桶 Machine 19 · Space 11 · Lab 6 · Site 2。展开态 id 改用全池下标（原「日期+月内下标」在筛选后会串位）。筛选不记 localStorage（语言才是长期偏好）。**坑**：`LogList` 是 client 组件，不能 import 内容池模块的运行时导出（`node:fs` 撞 Turbopack），桶在服务端算好当 prop 递。统计条 109→**110**。映射=MAPPING §8.7。

- **项目 01 主图接活件 + 预览→主图变位缩放转场（2026-07-27，用户拍板）**：主页项目 01 预览位那台 Lab.04 五环活件同时放进案例页主图位（`.case-hero--live`，两端同组件同内核；作者供图后删 `liveHero` 一支即回占位），并把「点卡片 → 进新页面」的转场接到底——**一次直飞**（用户当轮纠偏「不要中间放大一下再缩小过去」：首版是放大铺满再缩回 hero，两段各自连续、合起来是一次没必要的鼓胀）：克隆钉在卡片原位跳页，目标页量到 hero 后与本页 hero 沿同一条几何路径一次飞到位、途中交叉淡出，几何拆成纯函数 `src/lib/site/flip.ts`（等比按宽 + 中心对齐 ⇒ 两个起点盒不同、终点盒相同的元素全程重合）+ 9 项测试。起点取克隆此刻的视觉框，第一段末帧＝第二段首帧，接缝为零。两个坑入档：① `cloneNode` 不复制 canvas 像素（3D 台架当预览时点下去机构会凭空消失）——台架登记「重绘一帧 + 立即读回」闭包换成快照 `<img>`，**不开 preserveDrawingBuffer**（代价会加在常态渲染上），gl3d 装备零改；② 收尾必须连动画一起 cancel，只复原内联样式会让 hero 失去 z-index 后继续放大压在正文底下。无 `data-pt-target` 的路由（/archive /lab）原样走老的着陆平面路。131 测试绿；CDP 实测五条路径（转场落位 / 直接访问 / reduced-motion / 回退后二次转场 / 窄视口无克隆兜底）残留归零、无 pageerror。跳页前**底板从预览块自己的框沿四边铺开**（clip-path inset 展到满屏，EXPAND_MS 380，铺满即 push；用户当轮再纠偏「背景是直接一下闪过来的，不太自然」——首版是全屏遮罩淡入，像平白多糊一层。用 clip 不用 scale：缩放会把渐变拉变形），落地后遮罩淡出且必须压到 hero 之下（夹在 hero 与克隆之间会让交叉淡出变成「亮的淡成暗的」）。无落点的路由仍走生长铺满。手感常量 EXPAND_MS 380 · MORPH_MS 660 · CROSSFADE 0.55 待真机拍板。映射=MAPPING §9。

- **Log 三级筛选 + 日历热力图（2026-07-27，用户拍板）**：单级项目筛选扩为**项目 → 方面 → 月份**三级下钻，右侧配 GitHub 式热力图（列=周、行=周日→周六，值域=池内首末条不铺满一年，点某天滚到那天条目并闪一下）。仍不新开字段、不加路由。**派生逻辑拆出新模块 `src/lib/site/log-facets.ts`（纯函数、不碰 fs）**——这是 §8.7「桶在服务端算好递 prop」的正式修正：三级要在客户端实时重算，改为服务端与 client 组件共用同一份实现（`log.ts` 只剩读文件+zod+类型，`getLogBuckets`/`logBucketKey` 已删）。方面级新增 `Other 其他` 接住没打 neutral 标签的 4 条，守住「每级各视图之和 = 总览」。计数按 faceted search 惯例现算（选项集合恒取全池，空的变灰 disabled、不消失）；热力图筛选只改深浅**不缩网格**（值域恒全池），月份筛选生效时其余月份格子退到背景。日期算术全程 UTC。新增守门测试 `log-facets.test.ts` 13 项，统计条 110→**121**。**月份改横向滚轴**（用户「不然以后就太长了」）：月份是唯一无限增长的一级，「全部」留轨道外、月份进 `overflow-x` 轨道 + 紧凑标签 `2026.07`；选中项自动滚进视野，位置用 `getBoundingClientRect` 相减（`offsetLeft` 会量到 `.pg-dark` 上去——首版即此错）。方面级不改（词汇表有界）。122 测试绿。映射=MAPPING §8.8。

- **智能床日志并入（2026-07-27，用户拍板「同样的工作，传上去」）**：第三份日志——2030 智慧睡眠前瞻研究（清华未来实验室 × 慕思）的智能床项目，2026.05–07 共 9 条——按同一套要求入池。**该项目用户明说暂列 other work、不算项目 3**，但在 log 里照样占一条独立工作线：新增项目桶 `Sleep 睡眠`（**不叫 Lab**——那个桶已指连杆台架，会混读），五桶 Machine 19 · Space 11 · Sleep 9 · Lab 6 · Site 2 = 47 条。方面级新增 5 个设计研究词汇（Research 调研 / Writing 写作 / Survey 问卷 / Personas 画像 / Interviews 访谈，Concept 复用）——作者给的是两个一组的标签，站上按「1 outline + 1 neutral」预算只留主要那个，被舍的（竞品/文献/可视化）在原稿里原样保留。两条区间日期取末日钉成单日（06-05 / 07-14），跨度写进正文。原稿存 智能床_工作日志原稿.md；中文侧补了原文只在英文侧解释的术语（RU-SATED 六维度、关键事件技术），标点统一全角。机制零改（无新字段/组件/路由），新增守门测试「无重复条目（同日+同 lead）」——三份原稿汇一池，重复并入是最可能的事故且形状合法。统计条 122→**123**。映射=MAPPING §8.9。

- **Log 一级筛选改按「项目」呈现（2026-07-27，用户拍板「按具体的项目，比如项目 1 / 项目 2 / other works 这样分别展示」）**：原一级选的是 outline 标签字面（Machine/Space/Sleep/Lab/Site）——那是主题词不是项目：`Lab`（连杆求解器台架）按本文件「定位与边界」本就属于项目一，`Site` 根本不是作品集项目。新增 `PROJECT_GROUPS`（log-facets.ts）把标签归组成四个项目：**Project I · 轮回机器（Machine+Lab=25）· Project II · 空间仿真（11）· Other work · 智能床（9）· 本站（2）**；顺序固定为登记顺序（作品集编号，不随条数抖动），另两级不变。`bucketOf` 保留、新增 `projectOf`。代价：条目左栏标签与筛选项不再同名（点 Project I 出来的条目标签是 Machine 或 Lab）——有意为之，标签说「哪一类工作」，筛选说「哪个项目」。新增守门测试「每个 outline 标签都登记在某个项目组里且不重复」；统计条 123→124，与转场分支（§9）合并后全套实测 **133**、统计条按此。映射=MAPPING §8.10。

- **案例页对稿返工（2026-07-28，用户指出「没完全对齐参考」）**：07-27 的 case 深色化只搬了配色（`.pg-dark` 重定义 token），**稿里的构件全丢**——与 Lab 那次「剥掉 style 读稿」同一类偷工，且深色稿当时**没存档**、无从复查。用户重传后存 `design-ref/Case-Screens.dc.html`，逐项对齐：主图去重（此前 hero 与 MDX 里同名 FigSlot 各画一次）、图注行补说明 + 状态改纯文字小签、编号 h2 补 26×3 色刻度、占位框改绿虚线、概念四卡改 1px 发丝缝无外框、图框/竖线/分隔线一律发丝线、Fig.05/06 并置、Fig.12 连杆裱框回浅纸底、ground plane 补点阵层、页脚换渐变+点阵、nav 度量归位、标题下补双线尺、summary 回正色。另补上稿里一直没做的**返回转场 goBack**（点 brand/Work 平面淡入换页；SPA 适配与三重防残留见 MAPPING §6.3）。四路由不动、内容池机制不动、连杆内部零改；133 测试绿、双构建过、四路由 CDP 无 pageerror。**教训入档：稿要存档；token 对得上不等于对过稿。**

- **项目 01 正文入池（2026-07-28，用户供稿「你可以往网站里面填了」）**：用户交来 `reincarnation_machine_site_content.md`（17 节，中英各自独立成文），案例页正文占位**首次被真文字顶掉**——`content/work/reincarnation-machine/index.mdx` 里的 `IntentNote` 全撤，八段叙事弧结构与图插槽编号一律不动。取英文侧（SITE_SPEC §1 英文优先、v1 无 i18n），中文只留在概念卡双写与图框标签。新增两段编号 section（07 Where it stands / 08 Specifications）承接供稿 §13/§14/§16。**这是转录不是代写**，SITE_SPEC「模型不代写」条款未破。三件配套：① 稿从没给过正文排印（`Case-Screens` 正文位全是虚线框），按站内版式语言补 `.case-body` 段落/列表/子标题/表格/code 排印——文本列仍 62ch 而表可越出，层级只用字号字重发丝线不加颜色；② 四张 GFM 管道表要 `remark-gfm`（`MDXRemote` 挂 `mdxOptions`）；③ **隐蔽机制不发布**——原 summary 的「your touch secretly hastens its death」正是供稿 §10 明令预实验前不得上网的剧透（被试搜到即毁设计），已换成供稿给的模糊表述，实验后再补全。坑：Tailwind preflight 清了 `list-style`，正文列表要显式写回。133 测试绿（无新增）、双构建过、CDP 全页实测无 pageerror。映射=MAPPING §10。
- **Work log 自助编辑后台 `/studio`（2026-07-28，用户拍板）**：用户要「像写博客一样把内容传上去，且已交的日志可被修改」，不必每次找模型改后端。**存储 = 仓库本身**（三选一里用户选的）：站内多一个受密码保护的路由，改完点一次「发布」→ GitHub API 打**一个** commit（`content/log/entries.json` + 新图 `public/log/*`）→ Vercel 自动部署，约 1–2 分钟上线；没配 token 时退化成写本地文件（本地 `npm run dev` 即可写日志）。**「改多条 → 一次发布 = 一次部署」是硬设计**，逐条发布会重演 07-10 的部署限额事故。核心约束是「不可能因为在网页上写日志而把线上构建搞挂」：校验只有一份代码（`log-schema.ts` 形状 + `log-guards.ts` 规则），编辑器实时跑（有问题「发布」按钮禁用并列人话报错）、发布接口服务端再跑一遍（不信客户端）、构建期 `log.test.ts` 跑同一份；另加图片引用必须真实存在（已有名单服务端自己看）与 blob sha 并发检查（两个标签页不会互相覆盖）。内容能力按用户勾选落地：**受限 Markdown 正文**（自写小解析器 `md.ts`，零新依赖，输出数据不输出 HTML ⇒ 无注入路径；不安全链接降级成原文；**池内既有 47 条解析后逐字不变**）+ **图片块**（浏览器端先缩到长边 1600px 转 WebP 再传，单张 ≤900KB、单次 ≤3MB；src 只许 `/log/` 下）+ **表格块**（单元格可「双语」或「统一」，数字不用抄两遍）。双语仍中英并排必填。落盘用自写 `serializeEntries` 而非 `JSON.stringify(…,2)`——与手写时期格式逐字一致，实测发一条新日志 = 36 行纯新增 diff，不是整文件重排。门禁 = 密码 + HMAC 签名 httpOnly cookie（30 天）+ noindex + robots 屏蔽；局限写进档：会话不可撤销、无持久暴力破解计数，**密码要随机 24 位以上**。预览复用站上 `LogBody`（预览与线上同一段代码）。175 测试绿（+42）；真浏览器 12 步实测（登录/密码错/编辑既有条目/新建+图+表/预览/发布/线上展开+图真加载+中英切换/筛选/robots/匿名接口 401/删除后回 47 条）全过、无 pageerror。**余项：用户在 Vercel 配 `STUDIO_PASSWORD` 与 `STUDIO_GITHUB_TOKEN` 才能在线上用**（配置说明在页面上）。spec = SITE_SPEC §12。

- **案例页中英切换（2026-07-28，用户拍板「和 log 页面同样的中英文切换滑块，以后每个项目的详情页也都这样」）**：设计稿里没有这件东西（Case-Screens 是纯英文页），属站方增补——滑块皮肤、SSR 首帧恒 EN、不新开路由全部照 log 页那套，**全站仍然不做 i18n**。内容模型 = 两个文件：`index.mdx`（英文正文 + frontmatter）、`index.zh.mdx`（中文正文、无 frontmatter），元数据单一来源是 frontmatter 新增的 `zh` 块；**published 条目缺中文两件套即构建期 fail**（draft 不卡，项目 II–IV 不受影响）。**不整页渲两遍藏一半**——主图那台五环活件只能有一份（两份 = 两个 WebGL 上下文），故切换点打散成若干 `<Pick en zh>`，主图留在 Pick 外由薄客户端壳读语言上下文传 prop（CDP 实测切换后 canvas 恒为 1）。插槽仍是服务端组件，语言靠**按语言绑定 MDX 组件表**注入（状态签英文字面取自稿：ready / to shoot / tbd / after study）。中文侧排印只关掉大写与拉丁字距、抬 CJK 行距，度量与层级一律不动。语言偏好全站一个键 `site-lang`（log 页与案例页共用，旧键 `log-lang` 弃用）。新增守门测试 `content.test.ts` 5 项——双语正文最可能的事故是**两侧漂移**（一边加图另一边没加），故卡「两侧插槽编号与顺序完全一致」。统计条 133→**138**，双构建过，CDP 实测切换/刷新记忆/跨页互通全绿、无 pageerror。映射=MAPPING §11。

- **项目二日志增量 7 条（2026-07-28，用户交来已成条目形态的 JSON）**：2026.07-20 至 07-29 的项目二（人宠共享空间）工作——三十行原型、人类智能体重写、占用代价二分、双时钟砍成单钟、三次文献核查——按既有要求并入 log 池，47→**54 条**。机制零改（无新字段/组件/路由/测试）。**正文一字未动，只改标签**：交来的 `variant` 与本站用法正好相反（`Concept`/`Simulation` 标成 outline、`Study`/`Strategy` 标成 neutral），照收会让 7 条**一条都没有项目桶**——构建期守门直接 fail，放行则从所有一级筛选里消失；已把 outline 一律换成项目桶 `Space 空间`，原主题词降为方面。另挡下两处中文译名冲突（`Study` 交来译「文献」而池内是**实验设计**、`Strategy` 交来译「策略」而池内是**战略**——同名两译会把筛选项裂成两个同名桶），三条文献核查按实际内容归既有的 `Research 调研`，方面词汇零新增。落盘用站内 `serializeEntries` 而非 skill 的合并脚本（后者会重排全部既有条目），diff = 105 行纯新增。**已向用户点明：其中 2 条日期为 07-29，晚于并入当日，未擅自改；最新条从 07-24 变 07-29 ⇒ 主页 S2 的 Log 预览三条随之全换。**原稿追加进 项目二_工作日志原稿.md。统计条不动（180）。映射=MAPPING §8.11。**同日第二版：同一批 7 条各带一张表重新交来**，整条替换（正文也换——作者把数字从散文抽进表格、散文相应收短），**表格块首次在站上真用起来**（§8.5 预留、/studio 落地，此前池内零使用）。三处形状问题并入时修掉：① 交来的表是「按语言分开的两张表」（`head:{en,zh}`/`rows:{en,zh}`），站内 schema 是「一张表、每格双语」，需转置，两语相同的格子塌成字符串；② 五张表的表头角格是空串，`LogCell` 不收（也是个 `<th>`），三张补真标签 `Parameter 参数`、两张对比型用 `—` 占位——**这五个词是站方补的**；③ 散文里被抽走的数字不全在表里，故**原稿存档同步补了 7 张表**（存档必须是超集）。diff 369 增 12 删，180 测试绿。

- **项目一表格附录并入（2026-07-28，用户交来「之前遗漏的各个工作日志的表格」）**：用户汇总的 3–7 月全部表格（存 轮回机器_工作日志表格附录.md）挂到 `/archive` 六条既有条目上（03-20 / 04-03 / 06-15 / 07-07 / 07-09 / 07-18，条目正文与标签**一字未动**，只加 blocks）。首版按「现在还成立吗」筛掉了被推翻/残缺的十余张，**用户当轮否决并拍板「被裁的也上，标记一下被推翻就行」**——遂全部上站：27 张（池内表格 7→**34**），04-03 单条挂 10 张。**状态写在表注开头，措辞固定四种**：`已被取代 Superseded`（中央驱动中间版与最早版、传感侧采购单）·`部分被推翻 Overturned in part`（人格顺序表、生理表 HRV 三行，逐行另标「已删」）·`待核对 Needs checking`（两组量表，07-07 裁剪后未确认）·`残缺 Incomplete`（距离编码只剩 4 条、Fischer 八类只剩 2 类，**缺口不补造**）。此前略去的两列也照上并注明存疑：五环几何的侧倾角（本站 07-17 记录判定它不是装配侧倾）、中央驱动的 `ratio`（原文未给定义）。**单元格英文侧全部由站方补译**（用户明说没翻，站是英文优先、双语必填；数字/型号/事件名原样）。唯一未上站的是附录末尾三节（版本冲突汇总 / 还缺的 / 要不要补做表）——那是待办与索引、引用的表编号站上不存在，且它点名的每处冲突现在都以表注形式出现在对应表上。逐表去向见附录文件开头的「上站情况」表。180 测试绿、双构建过、CDP 实测 34 张表全渲染、中英两侧均无横向溢出、无 pageerror。映射=MAPPING §8.12。

- **条目标项目 + 图表移到右栏（2026-07-29，用户拍板）**：`/archive` 每条条目左栏第一行加**项目标记**（`PROJECT_GROUPS` 新增 `short`：项目一 / 项目二 / 其他工作 / 本站；紫色短刻度 + 微型大写，刻意不做成 `.tag` 盒——盒子已被「标签」占着；点它 = 只看这个项目，与一级筛选同一个 state）。这结清了 §8.10 的已知代价：标签说「哪一类工作」，项目标记说「哪个项目」。图 / 表不再顺着正文往下铺（一条挂十张表时正文被整段切断）——**收起态**条目行右端出角标（`▦ 10` / `▣ 2`，一眼看见有东西）；**展开态**正文在左、右栏列清单（编号 + 列×行 / 像素尺寸 + 说明两行封顶，贴右缘与角标同列）；**点某张卡才展开那一张，且占满整幅宽度**（表要宽度才读得动，一次只开一张）。编号按类各自数，派生逻辑在新模块 `src/lib/site/log-blocks.ts`，角标与清单共用同一份计数（守门测试挡「角标说 3 张、点开只列 2 张」）。两个坑入档：① 分栏用 **container query 不是 `@media`**——同一个 `LogBody` 也长在 `/studio` 的窄预览面板里，按视口判断会把 300px 右栏硬塞进预览；② 角标与箭头都贴右缘时，**两个 `margin-left:auto` 会把剩余空间对半分**（角标飘到行中间），没附件的条目改用相邻选择器把 auto 交还箭头。185 测试绿（+5），统计条 180→185。映射=MAPPING §8.13。

- **Lab.04 蒙皮改「可调遮罩 + 点阵织纹」（2026-07-29，用户两轮拍板）**：稿内那层不透明烟灰织物在暗底上是一整片压过来的灰布，把十四板桁架/曲柄轮/脚槽全盖住（用户：「死灰一片，柔和一些」）；首版换成纯点阵后用户再纠偏「**别纯波点，加个滑块能调遮罩透明度**，可以变成不透明/半透明/全透明」。**这是对稿的一处有意偏离**（§7.7 刚按稿补齐蒙皮，两轮换的都只是它的呈现方式，几何与锚固点一字未改）。同一张直纹带出两层、共用一个 0–1 滑块：**遮罩面**（41 列重采样的三角面，α=滑块值）+ **点阵织纹**（格心点云 58×11/带、~2.5k 点，α=min(0.72, 滑块×1.8) 故刚离 0 就到位、低透明度读作纱不是空）。两层都吃族系配色——点色沿带宽从 A 环色渐到 B 环色、遮罩面取两环色中值压到稿内烟灰明度区间，色相铺满整个身体，这是不再死灰的主因；点的形体交给不透明度、颜色只轻微压暗（暗底上照实体面那样乘明暗会把背光点压成灰渣，实测即此病）；点径世界单位（1.75mm，着色器 `/w` 透视缩放）故缩放时密度观感恒定。**半透明三条硬约束**：深度只测不写（否则 α=0.2 的面照样挡光）⇒ 绘制序反过来（实体线稿先画、半透明最后画）⇒ 半透明面之间无 z 排序，四条带按视深**从远到近自己排**；α≥0.985 切回实体路径（写深度、先画，遮挡交回 z-buffer）。控件上**滑块取代原「蒙皮」复选框**（开关两端就是滑轨两端，并存会出「蒙皮开着但透明度 0」的自相矛盾态），默认 0.35。装备仍是加法式扩展：`gl3d` 新增 `drawPointCloud` + 纯函数 `bakeRuledPoints`（新增 6 项单测）+ `drawDynamicMesh` 可选 alpha；`MESH_FS` 加的 `uAlpha` 由 beginFrame 每帧置 1 ⇒ 实体路径逐像素不变，既有绘制调用一行未改；`src/demo/*` 与 `npm run demo` 零改（台架双页仍是实体蒙皮）。同组件的三处调用（/lab、主页 Stage 轮播、案例页项目 01 主图）一并变，CDP 实测滑块五档（0/0.2/0.35/0.6/1）+ 透视/顶视图/案例页侧栏无 pageerror。191 测试绿，统计条 185→191。手感常量（列数/点径/默认位/织纹增益）待用户真机拍板。映射=MAPPING §7.9。

- **Lab.05 整机台架（2026-07-29，用户当日上传参考当日立项开工）**：用户交来 `729新参考.3dm`（48.9MB，直接推的 master）——**整机装配图**，前四个参考只到单机构。先做可复现测绘（`measure_729.py` + `machine_729.json` + 轮回机器_整机测绘.md），四项校验数字自洽到小数点后四位：导轨端点 ≡ 曲柄滑块极限 `l∓r`（五环偏差 ≤0.0001mm ⇒ **顶部止程解析确定、无需真机标定**）、曲柄圆 ≡ 驱动点半径（零偏差）、五曲柄销 x 全等（**同相**，且各环独立反推轴心均为 z=−40.250）、站距逐对 85.000。**结清三件此前只能推断的事**：85mm 站距坐实、Lab.04 同相呼吸假设坐实、S4 环几何与 07-10 旧提取逐位相同（新文件不推翻任何既有实现）。
  用户拍板「整机传动 + 真实实体」「与 Lab.04 并存」。spec = 轮回机器_整机spec.md。**运动学一行没新写**——新旧文件五环销坐标逐位相同（差值 ≤0.001mm，四舍五入位数差），故复用 `shell3d` 的解算/止程标定/定步；Lab.05 新增的是传动链的可见形体与真实实体网格（`gen_machine.py` → `machine-shape.ts` + `machine-mesh.bin`，113,906 三角 / 1.3MB）。绑定：66 块角化板一一对应无歧义 + 3 件配件绑最近销只平移 + 5 单杆轮随 θ 转 + 5 驱动杆两点定位姿 + 机架/触手烘死为静件。位姿计算在 `machine.ts`（纯函数、可单测、与渲染无关），台架 `components/lab/MachineBench.tsx` 挂 `/lab` 第五台。装备只做加法式扩展（`drawMesh` 加可选 dark/lite，不传即旧行为）；`shell3d`/`RingsBench`/`src/demo/*` 一行未改。
  **四个坑入档**：① 件绑板不能用质心落点——`杆三角` 是细长杆的三角化、面积极小，质心法在任何容差下都同时命中相邻板，判据须改为「件的二维轮廓是否包住板的三个销」；② 图块变换要**逐层累积**（小触手→SG90→子图块，嵌套 ≥3 层，首版只传上一层，第三层起零件掉回原点）；③ **体轴与图纸 y 反向**（S1 y=−1112.4→站位 −170，S5 −1452.4→+170），取正号会把整机沿体轴镜像、映射行列式变 −1 连法向都朝里——M1 的环局部回算闸门抓不到它，另加了「滑轨架落位 vs 站位」自检；④ 测绘表首版把「全环最高销」标作拱顶，四环相同、**只有 S5 不同**（最高销在 x=−27.135 处 103.855，高于中线拱顶 103.281），现两列并列。
  实测：整周 720 步各环行程 vs 2r 差 ≤0.0096mm、峰值残差 0.545mm、连杆漂移 0.0017mm；206 测试绿（+15）、双构建过、CDP 实测 /lab 五台并存、整机随相位动作、视角预设可用、无 pageerror（页面唯一 404 是 favicon.ico，站上本就没有该文件，与本次无关）。统计条 191→**216**。**中间轴只在 180° 内往复（用户 2026-07-29 更正，图纸看不出来）**：机器做的是**张合**不是循环转动——区间两端恰是曲柄滑块的上下死点，故 180° 正好走完整个行程 2R，一步不多一步不少；端点换向不需缓动（死点处 d(拱顶)/dθ=0，匀速反转时输出速度本就从零平滑折回）。落地 `reciprocate`/`runMachine`（纯函数，**反射而非钳制**——钳制会停在端点等人改方向），相位滑块量程随之 0–180°，「整周」相关的测试与文档一并改口径（+5 测试）。**转速模型定 0.8 rad/s**（用户授权）：半程 3.9s、一次完整张合 ≈7.9s，与人的呼吸节律同量级，与 SHELL_OMEGA 同值故两台并读节奏一致；仍非真机节律。**摆向翻正（用户「转反了，应该是另一侧」）**：`MACHINE_DIR = −1`，从伸展位起经 +u 一侧扫过 180°；两个摆向的拱顶轨迹完全相同（关于死点对称），差别只在曲柄与连杆走哪边，故这是纯外观量、不动运动学。**往复在整机视图里看不出来**——环的动作在整周转与 180° 往复下逐帧相同，唯一区别在曲柄，而默认机位下曲柄小又被机架挡着；故 HUD 加方向指示（伸展 ↑ / 折叠 ↓），要直接看曲柄摆动则用控制面板「部件」只留传动。**历史文档口径已统一**：盘点 §「已验证的机构原理」第 4 条原文「连续单向转轮 = 循环呼吸」是本项目此前的错误假设，已改为 180° 往复（φ=0 伸展 / φ=180 折叠的口径本来就一致，站上沿用）；五环 spec 补注（Lab.04 用连续转推进相位，对环的动作无影响，看真实往复去 Lab.05）；拱环文档补注（文中「整周」指求解器扫掠测试范围，非真机行为，扫整周是更苛刻的工况、有意保留）。**默认机位定为「全体都在画面里」**（用户：后续还要做触手的运动），不再考虑只框环身。CDP 实测：φ 176→180 折返下行至 2 再折返上行，两次换向，最大 179.6 从不越界、最小 4.0 不低于 0，端点 apex 读数与全开/全折叠位对得上。**控制面板做成整机专属（用户当日追加：「控制面板这里能复用其他 lab 的，然后做一个自己的控制面板」）**：版式语言仍复用 `.lab-ctl`（CSS 一行未加，本就是 flex-wrap、多组自己换行），控件换成整机该有的六组——驱动（运转/透视）· **转速滑块**（0.1–2.4 rad/s；转速本就是待拍板的手感常量，与其替用户定一个数不如给滑块）· 相位 · **部件显隐**（环身/传动/机架/触手；关掉机架能看清传动链怎么走，关掉环身即「一轴五曲柄」独照）· **单环隔离**（全部/S1–S5，只筛环件——机架与轴不跟着消失，否则「只看 S3」会连驱动它的轴一起切掉）· 视角。为此把中间轴从静件里拆出单独成组（否则「传动」开关切不动它）；分类逻辑 `partKind`/`groupRing`/`visibleGroups` 落在 machine.ts 可单测（+5 项）。HUD 读数跟着单环走。CDP 实测四个部件开关各自减掉预期占比、五环隔离 HUD 报各自拱顶（S1 117.9 / S3 147.7 / S5 96.5mm，半径不同故各异）、回「全部」像素数逐位复原。**余项**：用户真机手感拍板（转速默认值、默认机位三常量 pivot/scale、静件明暗）；主页 S2 那组 Lab 卡片已加第五张、网格 4 列→5 列（用户 2026-07-29：卡片后续再优化，先把新的加进来）；小屏两列时 5 张仍有一张单行，属待优化项。**规格表已写明三条局限**：转速非真机节律（减速比无出处）、两条小触手是静态形体不参与运动、脚槽止程仍是仿真标定值。

- **Lab.05 加蒙皮 + 大触手三肌腱牵拉（2026-07-29，用户追加）**：① **蒙皮**——与 Lab.04 同一套直纹带遮罩 + 点阵织纹，纯几何件抽成共享模块 `src/lib/linkage/skin.ts`（两台画同一种蒙皮，留两份副本必然漂；RingsBench 本轮不动，不给刚定版的 Lab.04 添回归风险）。整机默认 0.22 比 Lab.04 的 0.35 更透——这台的看点是里面的传动链。半透明三条硬约束照 Lab.04 定案。② **大触手活了**：查明 729 里那条大触手**就是 Lab.03 那条三肌腱触手**——椎节沿臂位置与 `tentacle3d-shape.ts` 的 STATIONS 逐位吻合，基座舵机总成位置亦对得上。故 spec §1.2 的「触手不动」条款解禁（**依据是查明它有出处，不是放宽标准**），运动学整套复用 tentacle3d，新模块 `machine-arm.ts` 只管把它摆到机器上（+11 项单测）。落位由 `gen_machine.py` 实测生成、不手写：sim x̂→世界 +Y、沿臂 ŷ→−X、ẑ→+Z，行列式 +1。**滚转靠离轴地标定**——椎节质心全在轴线上定不出绕臂姿态，用梢端三绑线柱方位与 TIES 比对，残差 2.6°（测量噪声量级），生成期 >6° 拒绝出表。**坑**：梢端有两组半径≈6 的三件、彼此差 60°，按「最靠梢端」取会选错（首版算出 61° 滚转）；正确做法是先用椎节质心拟合 ax 原点（与滚转无关），再按 `ax = TIES 沿臂位置` 定位该取哪一组。大触手不再烘进 machine-mesh.bin（载荷 113,906→82,553 三角），改用 Lab.03 那份网格（同一文件，浏览器命中缓存）。控制面板加蒙皮滑块 + 三肌腱滑块 + 松开，共八组。**机位改为只由固定视角按钮控制（用户当日追加：「取消拖拽的视角移动，只保留底下几个固定视角的选择」）**：不挂任何指针/滚轮监听 ⇒ 拖拽不转、右键不平移、滚轮不缩放；**这与 Lab.01–04 的差异是有意的**，那几台仍可拖拽。连带好处是滚轮不再被画布吞掉、鼠标停在这台上能正常滚页。「归位」按钮随之撤掉——没有拖拽就没有要复位的东西，它与「轴测」完全等价。CDP 实测：左键/右键拖拽画面零变化、滚轮时页面 scrollY 正常推进而画面不变、视角按钮来回切换后画面逐像素复原。**大触手待机摆动（用户当日追加：「待机状态下让三条肌腱交替稍微收缩，展示摆动舒卷，不要直挺挺伸着」）**：运转中且没碰滑块时三腱按 120° 相位轮转（≈15s 一圈）+ 更慢的整体舒卷（≈27s），一碰滑块即交出控制权、「交还待机」交回。**一个差点发出去的坑**：肌腱有松弛量，实测收缩率 **0.28 以下拉的全是空行程**（单腱 c=0.10/0.15/0.22 的梢端侧移都是 0.0mm；0.30→15.9mm、0.40→74.2mm、0.50→128.5mm 的 J 形卷曲）。首版按「稍微收缩」把幅度设成 0.22，**对触手完全无效**——参数设了、波形跑了、臂一动不动，而屏幕上在动的是五环，肉眼与「画面有差异」的粗测都会误判成做完了。定案改为三腱共同施加基线预张力（先吃掉松弛，三向恒和为零故不产生弯向）+ 差动（弯向由它给）。幅度经用户两轮上调：0.22（完全无效）→ 0.42（侧移 18–78mm，用户「有点不明显」）→ **base 0.34 + span 0.34 = 峰值 0.68**，实测梢端侧移 **43–150mm** = 臂长 358mm 的 12%–42%，卷与舒落差三倍多。**残差是稳态量不是收敛滞后**——波形放慢 2.6 倍残差纹丝不动（6.44→6.63mm），静态保持亦然；上界按既有工况校准：Lab.03 已上线并经用户真机拍板的 c=1.0 全螺旋残差 9.19mm，本波形约 7.8mm 仍在其下。验证也随之改法：只留触手、关掉环身/传动/机架，此时画面任何变化都只能是它——每 4 秒间隔 900–1600 个采样点变化。**起步缓入（用户第三轮：「第一下的收缩有点猛」）只能加在差动上**：首版乘在整体上没解决——松弛门槛以下臂完全不动，整体从 0 缓入等于前 3 秒空等，等驱动腱越过门槛时缓入已走大半，一越过照样是「一下子」。定案基线 1.5s 就位（三腱共缩 0.34 实测侧移仅 3.2mm、不压屈，故它可以早给）+ 差动 5s smoothstep 拉起；判据取**起步速率 ≤ 稳态速率**，实测 67.0 vs 65.9mm/s（比值 1.02）。排查中另有一次自摆乌龙：打印固定腱 0 的收缩率，而当时出力的是腱 1，看成「收缩涨 0.02、侧移窜 112mm」的诡异过冲——三腱系统必须看最大腱。统计条 239→**246**。239 测试绿（+23）、双构建过、CDP 实测静止笔直落位与原烘死件重合、肌腱拉满卷成钩形、无 pageerror。统计条 216→**239**。
- **第一下往上 + 主图换整机 + 机位按包络重定（2026-07-29，用户两轮追加）**：① **「第一下活动往上移动，现在是往左下」**——腱 0 的收缩把臂梢推向世界 **+Z（正上）**，波形从腱 0 起本来是对的，坏在**缓入那 5 秒里回转会走满 0.42×5 = 2.1 rad = 120°**，恰好把主导权交给腱 1（推左下）：等幅度终于起来，方向已经换人了。修法是让**回转速率与幅度同步缓入**——新增 `idleSwayAngle(t)` 用 idleEase 的积分（∫smoothstep = u³ − u⁴/2）当时钟，而不是拿 t 直接乘 sway；稳态下只等价于一个固定相移，周期不变。实测 t=3/4/5/6s 位移 (2,−2,21)/(8,−2,65)/(23,1,110)/(26,−11,117)，全程 +Z 主导，到 t=7s 才转向左下。+2 项守门测试。② **蒙皮默认 0.22 → 0.67**（用户「调高一点」）：初版求「看清里面的传动链」，这台成了主图之后该理由让位——主图要先读出形态，传动链交给蒙皮滑块（拉到 0 即看穿）。③ **项目 01 主图换成这台**（用户「同时放在项目 1 详情页第一个图和主页预览图」）：`CaseHeroLive` 与 `HomeScreens` 预览位的 `RingsBench` → `MachineBench`，两台 props 形状逐字相同，故只改 import 一行——`.case-hero--live` 宽高预算、`data-ptm` 克隆源、goPT 转场几何、案例页「主图只能有一份」的单份约束全不动。作者供图后仍是删 `liveHero`/`supplied` 一支即回占位。④ **默认机位「往左上挪一点，确保任何时候都全部在画内」**：旧值实测下缘余量 **0**（贴边，已在裁）、右 21，而左 121 上 134 全空。改由**实测运动包络**定——先缩到不裁的比例，按 60s（覆盖摆动 15s 与舒卷 27s 两周期）每 2s 采一帧取并集，换算回视空间解出让包络居中的枢轴，现值 `pivot(−158.02,39.22,−24.37)/scale 0.75`，实测四边余量 左115·右115·上72·下71 px（画布 878×652）。**两个坑入档**：`cx`/`cy` **在 WebGL 台架上是死参数**（gl3d 只读 matrix/pivot/viewScale/pan、屏幕中心恒取画布中心，`camera3d.project` 那条 SVG 老路才用它俩）——首版就是改 cx/cy 去挪画面，scale 生效而位移毫无变化，差点误判成 HMR 没热更；量包络前**必须先藏 `.lab-hud`**，读数逐帧变会被逐像素 diff 当成「形体」，表现为右下角边距恒被吃满、怎么调机位都不动。249 测试绿（+10）、双构建过、CDP 实测案例页与主页各恒一个 canvas、中英切换后仍一个、/lab 五台并存、无 pageerror。统计条 246→**249**（此前文档写 248 系漂移，以 vitest 实测为准）。手感常量（pivot/scale/蒙皮默认/转速）仍待用户真机拍板。
- **转场保留工作状态 + 详情页面板重排（2026-07-29，用户两条追加）**：① **「点进详情页时图的工作状态要保留，不受影响地继续展示，而不是刷新了一下」**——主页预览位与案例页主图位是同一台台架，但换页时前者卸载后者新挂，新实例从 θ₀ 起步、触手笔直、待机摆动重新缓入 6.5s，转场接得再准也是「倒带」。做法是**状态交接不是保活**（新模块 `components/lab/handoff.ts`，模块级单槽）：不把 canvas 抬到路由树外（那要一套持久门户，牵动 SSR/布局/z-index，代价远超收益），改为出场实例把**机构此刻的位形**留下（五环节点坐标+曲柄角、触手节点坐标+三腱收缩率+待机时钟+运转/摆向/转速），入场实例摆好再继续跑。**位形直接摆、不能「设个 θ 让它自己解过去」**——同一个 θ 有不止一个合法解（07-17 S3 坍缩即此）。五环是拟静力学，setNode 摆上即可；触手是动力学件，**两次 setNode 夹一个空步**（setNode 不动 Verlet 的 px，只摆位置会让第一个子步算出「目标位−笔直位」的巨大速度把臂甩飞；px 在子步开头才从 x 拷，故走恰好一个子步再摆一遍即可，dt=1/120），**内核零改**。状态在**快照那一刻**留（与快照像素同一瞬间；改在卸载时留会比飞来的快照超前 380ms，交叉淡出变「跳一下」）。**三个坑都不报错、只表现为功能像没写**：取状态必须在挂载流程最前面取（createMachine 里的止程标定开发期要跑几秒，第一版取到时 age 已 7.1s 被保质期挡掉）；取走后要留宽限期（严格模式开发期挂两次，**留下的是第二个实例**，取走即清会让恢复落在被丢弃的那一个上）；保质期本身要有（留了状态没换成页时不该套用陈的）。守门测试 handoff.test.ts 7 项。**验证也踩了一次**：软件 GL + 主页五台台架帧率极低而每帧 dt 封顶 50ms ⇒ 仿真只跑到真实速度约 1/7，「悬停 6.6s 再点」的源状态其实只推进 0.8s，落地看着像重新开始；真判据是**捕获值 = 落地首帧**——capture 记 θ=0.9308（φ 36.7°）/三腱 0.093，落地首读 φ 36.7°/三腱 9%，逐位对上。② **「详情页的操作面板被截断了，重新排版」**——八组控件竖排一列 939px，而案例页主图高度被首屏预算封在 **356–449px**（随视口变）。改两列网格 + `align-content: space-between`（高度不是常数，压到最矮视口也装得下，高视口多出的空间摊进行距）；前四组半格、后四组通栏（组内要横铺），DOM 顺序据此把蒙皮提到相位之后；通栏组的组名收到行首（三个标签行 = 54px，正是最矮视口差的那截）；侧栏模式不出提示行；列宽 188→272px（画面 640→594——正文列把整件封在 868px，控制列每宽 1px 画面就窄 1px）。**两个坑**：量「有没有被裁」要拿 **hero 盒**当参照而不是控制列自己（overflow:hidden 在 hero 那层，拿列当参照则列比 hero 高时一律报「没超出」，第一版就是这么误判过关的）；复选框要 `flex:none`（四格一行每格只剩 48px，字撑不动就压那个 11px 方框，四个部件复选框全挤成 `|` 号）。CDP 六档视口 × 中英两侧全部无裁切无内滚、主图仍在首屏；`/lab` 横排控制条一行未改。案例页图注同步改「[顶替] Lab.05 整机活件」。256 测试绿（+7），统计条 249→**256**。
- **小触手活化（2026-07-30，用户给出机构说明 +「先这样做，以后还要改」）**：此前「小触手无机构数据」的判定被用户口头说明推翻——底座固定在机架上、**大节由 SG90 绕后端圆形轴心甩动**、中间一节软性结构连着下面的小块、按真实物理一起甩动（= 受迫大摆 + 柔性连接的被动小摆）。逐件核对图块 22 几何全部对得上（Ø32 轴心盘 / 55mm 大叉件 / 3.9×3.9×26mm 软杆 / 28mm 小叉件，两实例镜像装机身两侧、摆平面沿体轴前后）。落地三件：① `gen_machine.py` 新增 `smallarm_articulate`——静件 armsmall 退役，拆 sa_mount/sa_seg1/sa_soft/sa_seg2 四组（块局部烘一份、两实例位姿复用；零位复原闸门 0.00mm）；② 新模块 `machine-smallarm.ts`（**2D 内核**在摆平面解，内核零改）：大节全运动学（θ 是舵机输入不是解出来的）、软杆 3 段链 + 低刚度直化杆 = 弯曲刚度（**两端夹紧不是铰接**——A2–c1 把大节朝向传进软杆、c2–T 传给小块，真机软杆插进关节块）、重力真单位 9810 mm/s²（链自然频率 ≈1.5Hz，驱动 0.5Hz 在其下）；③ 台架接入：软杆双骨蒙皮（bakeSkinned，MachineGroup 加可选 blend）、波形 0.6 rad × 0.5 Hz + 2s 缓入、两条同 θ（**镜像装配 ⇒ 世界系天然反相**，一前一后读作步态）、/lab 面板加摆幅/频率滑块（**案例页侧栏不加**——§13.2 的高度预算已顶满，再加一组就重新被裁）、交接状态包加小触手位形。规格局限改口径：小触手已动，但波形是展示编排非真机节律（SG90 控制程序未见）。实测：默认机位含摆动的运动包络四边余量 115/115/72/71px 与转正前逐位相同（不用重定机位）；CDP 隔离验证（只留触手 + 大触手手动固定）摆幅 0→静止、60°→大幅甩动，无 pageerror。267 测试绿（+11），统计条 256→**267**。手感常量（幅/频/kBend/阻尼/相位差）待用户真机拍板——用户已预告还要改。
- **小触手：甩幅调小 + 点击受惊甩开（2026-07-30，用户追加「甩小一点」「点击它给一个比较大的反应，比如甩开我」）**：待机摆幅 0.6→0.32 rad（±18°）。点击交互：pointerdown 命中小触手骨架线（`saChainWorld` 世界折线按 gl3d 顶点着色器同式投到画布 CSS 像素、点到线段距离 ≤30px）→ 触发受惊波形 `startleSwing` = dir·1.15·e^(−t/0.9)·sin(2π·1.4t)——t=0 值零、斜率朝甩开向（**第一下就是甩离，不是先缩再甩**），首峰 ≈54° 远大于待机 18°，频率 1.4Hz 贴着链自然频率（≈1.5Hz）故被动小块甩出明显鞭梢，3.5s 寿命后自然衰减回待机；合成驱动角钳 ±75°。甩开方向 = 点击落在摆平面哪一侧的**反面**（世界 h 轴投屏取点击相对轴心的水平分量符号；视线恰沿 h 轴时投影退化，兜底远离当前倾角）。悬停出 pointer 光标、hint 行加「点一下小触手试试」。**这是 Lab.05「机位只由按钮控制」的有意例外**——两个监听都不动相机、不 preventDefault、不吞滚轮，滚页不受影响；暂停态点击同样有反应（受惊期间驱动覆盖，波形结束即停）。命中投影里 **cx/cy 不参与**（§12.3 的坑，屏幕中心恒为画布中心）。CDP 实测：静场 21px 噪声 → 点击后 670/717/708px 大幅甩动 → 衰减 214px，左臂零受扰（逐臂触发）；悬停探针 cursor=pointer 正确。273 测试绿（+6），统计条 267→**273**。
- **四个项目各进各的详情页（2026-07-29，用户拍板「主页进 1234 项目只有 1 进了自己的详情页，234 也都进一个各自的详情页」）**：此前只有项目 01 是 published，主页另外三张卡片的 href 一律写死 `/archive`，点 02/03/04 全落到同一个日志页——IA 红线是「四项目地位同等」，导航上却三个连地址都没有。改法：路由名单从「published」放宽为 **published ∪ selected**（`getRoutableWork()`，`dynamicParams=false` 不变；非 selected 的 draft 照旧不出路由），未发稿的三个走同一条 `/work/[slug]` 渲**「筹备中」页**（`components/site/WorkInPreparation.tsx`，版式全部复用 `.case-*` 类，发稿时换的是内容不是模板）。**页面里没有任何项目正文**——只有站方框架字（什么状态 / 发稿后长什么样 / 现在去哪看记录），「模型不代写」未破。左栏两格：状态签 + **该项目在 log 池里的条数**（`projectOf(entry)===slug` 现算，项目组 key 与 work slug 同名的才数得出来，现为 project-ii 实测 18 条；03/04 无记录时这格与正文那句自动换成「暂时没有可读的」）。中英切换/转场/深色与 case 页一致，hero 框带 `data-pt-target` 故主页卡片飞过来照样落位——与 Lab.05 那台活件互不冲突（未发稿的三个 slug 用的是占位 hero，不受活台架顶替影响）。元数据不用「待盘点」占位字当 description，且加 `robots: noindex`（空页不进索引）。新增守门测试 2 项（四个主项目都在路由名单里，否则主页卡片指向 404；非主项目 draft 不出路由），双构建过、CDP 实测四路由 + 中英切换 + 卡片转场 + 返回转场无 pageerror。统计条 273→**275**（本条与上方 Lab.05 系列并行开发，合并 master 后以 vitest 实测数为准）。映射=MAPPING §12。

- **底盘桁架有限元（2026-07-30，用户质疑「下部方形框架过厚」）**：对 `骨架` 层两件梯形桁架（362.6×100×8，弦杆 L 形 + 3.9 薄腹横撑/X 斜撑，104 g/件）做梁单元刚架 FEA（`scripts/model-extract/fea_chassis.py`，几何/截面全部实测自 729新参考.3dm，载荷=全机体积普查 1.53 kg + 批注件）。**判定：按强度确实过设计**（展示利用率 5%、最恶劣搬运工况对屈服 SF 8.1；X 斜撑结构上白给、纯视觉），**但减薄上限由中间轴对中刚度定，而刚度主要取决于展台托法**（同一副桁架四角托 vs 通长托挠度差 200 倍；对中预算 0.56mm=配隙几何推算）——四角支承连现状都超预算 5.7 倍。建议：展台先做两条通长托条，之后弦杆可减到 8×4 去斜撑（−58% 质量、侧高减半）。用户已确认：独立展台（托点未定）、FDM PLA 分段拼。结论与减薄路线=轮回机器_底盘桁架FEA.md（数字以脚本输出为准）。**分析仅供参考，改不改图纸由用户定；连杆/网站代码零涉及。**后续（2026-07-31）：用户按结论改出瘦身版框架（去 X 斜撑、构件减细）；这段工作并入 log 池成一条（55 条），三张分析图入 `public/log/`——**图片块首次在站上真用起来**（§8.5 预留、/studio 落地，此前池内零使用；池首图）。原/改对比两张渲染图**待入库**：聊天贴图不落盘拿不到原件，需用户把两张图以文件形式推进仓库（任意路径，模型来搬运改名压缩）或走 /studio，到位后补挂到该条目 blocks。原稿追加进 轮回机器_工作日志原稿.md。
- **Lab.05 底架换源（2026-07-31，用户推 `底架改进.3dm` 后「把模型更新成正确的模型」）**：gen_machine.py 改**双源**——环身板/单杆轮/驱动杆仍取 729新参考.3dm（逐件配对比对证实未变），底盘/滑轨架/轴系/触手摆位改取 底架改进.3dm。新文件**图层纪律缺失**（整机堆在 默认值 层）且装配簇整体平移 (Δx,Δy)≈(−749.7,+434.4)，故不能按图层取件：**平移量由五个曲柄圆中心解出**（五环互证，散布 0.0000mm，>0.01 拒收）、**件按尺寸签名分类**且数量强校验（1 轴+1 联轴+1 电机+5 滑轨架+5 底盘件+1 大触手+2 小触手，对不上即退出）。改动实录：底盘 8 件桁架+副框 → 2 整板+2 夹件（+S1 散横撑，首次入烘）、滑轨架五副换新版（厚 22.6→24.1–24.5、S5 加长 7mm）、电机箱移位 30.5mm、小触手右条内移 9.2mm、大触手 1.5mm；触手落位经新增 shift 参数搬回旧图纸系（块局部几何零改，只平移实例变换）。全部既有闸门带新源跑过：体轴自检去偏残差 0.12mm、小触手零位复原 7e-15、绑定回算 3e-14、大触手滚转 2.58°。84,125 三角 / bin 1.0MB。275 测试绿、双构建过、dev 实测 /lab 无 pageerror、正视图确认旧厚重底带消失。机位（pivot/scale 按旧包络定的值）与蒙皮默认未动——底架变薄后包络略变，待用户真机看过再说。

## 部署纪律（2026-07-10 事故入档）

- **Vercel Hobby 限额：100 次部署/天**——2026-07-10 因高频迭代烧穿（工作分支预览 + master 生产双倍计数），表现为「webhook 静默失灵、无任何新部署」，重连 Git 无效。
- 防复发：vercel.json `git.deploymentEnabled` 已关工作分支预览（**换新工作分支名时需同步加条目**）；主线迭代**攒批次再推 master**，不要每改一轮推一次。
- 限额撞墙时的辨认法：Vercel 项目 Deployments 页 Create Deployment 会报 `api-deployments-free-per-day`。

## 架构速记（M4 入档，2026-07-10）

- 三层，上层只调下层公开 API：`solver.ts`（纯数学，零依赖）→ `controller.ts`（交互状态机，零 DOM）→ 台架页 `src/demo/*.ts`（DOM 接线与 SVG 渲染）/ 未来 React 封装（SITE_SPEC S2）。
- 加新机构 = 新数据实例 + 台架页，**不改内核**（拱环先例：`arch-data.ts` + `arch.ts` + `arch.html`）。
- 手感参数全部集中在 `ControllerOpts`（driver / release / sweeps / hitRadius），渲染层不藏参数；数值以 LINKAGE_SPEC 为准。
- **3D 线同构（2026-07-10 复用化拍板）**：`solver3d`（3D 内核）→ `camera3d` OrbitCamera + `motion` CriticallyDamped（零 DOM 公共装备）+ `gl3d` FlatRenderer（唯一浏览器渲染边界）→ 台架页。旧 `scene3d` 画家算法路线已退役删除。加新 3D 机构 = 新数据实例 + 台架页，**不改装备**。详见 轮回机器_立体求解器spec.md §1.5。

## 可用工具

- 账号级 skill **case-study-logic**（已装，2026-07-08）：说"梳理逻辑 / 排结构 / 这页怎么讲"即触发——调档 → 接件 → 选叙事框架 → 排骨架 → 钢人红队 → 合规检查。只出骨架不代写正文。

## 分工边界（用户拍板 2026-07-08）

模型只管：**形态展示、原理展示、网页展示设计与开发**。不管用户个人日程、IRB/研究运营、实体机建造排期——不主动给排期建议、不追问进度。

## 纪律（spec §1 的摘要，冲突时以 spec 为准）

- 范围锁死：距离约束 + 锚点 + 软拖拽，别的不做。
- 求解器零依赖、与框架严格分离（`solver.ts` 里不许出现 React/DOM/window）；渲染只用 SVG。
- 测试与实现同步写（Vitest），不事后补；每个里程碑 git commit。
- 强模型额度不花在脚手架、排版、about 页上——那些留给弱模型。
