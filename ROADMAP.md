# ROADMAP.md — Fable 额度铺路计划（2026-07-07）

**背景**：ADMISSIONS_RESEARCH.md 确认——网站上线死线 ≈ **2026-11-16**（ITP 12-01 最早截止）；首选项目 MHCI 作品集只收链接（网站 = 提交物本身）；空间线（GSD/MSCD 等）要 25–30 页 PDF 且 GSD 明文拒网站；GSD 动态作品唯一通道 = ≤60 秒视频。

**甄别判据**（什么是"非 Fable 不可"）：不是弱模型能不能做，而是三个乘数——
**错误是否静默 × 返工是否昂贵 × 是否需要跨领域综合**。
排版歪了一眼看见 → 弱模型；架构歪了 11 月才发现 → Fable。

---

## A. Fable-gated（强模型提前铺，按优先级）

| # | 项 | 为什么 Fable-gated | 产出物 | 规模 |
|---|---|---|---|---|
| 1 | **SITE_SPEC.md 网站架构规格** | 内容池 schema、双轨（同一内容出网页+PDF 素材）、连杆 client island 的 SSR/hydration 边界、My Role/process 的结构化字段、性能预算——错误全部静默且 11 月才爆雷 | 一份弱模型可照做的 spec（同 LINKAGE_SPEC 规格） | ≈1 session |
| 2 | **导出管线 spec（LINKAGE_SPEC §7 升级）** | 150dpi/A3 印刷换算、线宽绝对单位、颜色内联、确定性重放的接口设计——数值细节静默出错 | §7 从"思路"扩成可执行规格 + renderStaticSVG 接口契约；**边界：只出 SVG 素材，30 页 PDF 的排版是用户在自己工具里做** | ≈半 session |
| 3 | **SKILL.md 工艺沉淀** | 把"spec 先行 → 测试同步 → 实测回填 → 修订入档"的工作流 + 视觉规范 + 坑清单写成可复用文档——它是所有后续弱模型 session 的乘数 | 原计划 M4 最低优先级 → **提前**，写完即可把 M3-减-阻尼 交给弱模型 | ≈半 session |
| 4 | **Case study 骨架** | 技术声明清单（每条带实测数字）、图目录（哪张图证什么）、叙事结构（含 My Role / process 合规块）、60 秒视频分镜——跨领域综合 | 骨架 + 素材清单；**声音和文字必须是用户自己的** | ≈半 session |
| 5 | Peaucellier 预置 + 验证测试 | 精确几何/种子/分支不变量是数值活，弱模型易静默做错；顺带证明求解器泛化到 N 杆（case study 素材） | presets 扩展 + 测试；画廊其余机构 deadline 后再铺 | ≈半 session |
| 6 | release 阻尼数学预案 | 模型选型（θ̇ 阻尼弹簧 vs ω 缓升曲线）与测试方式是数学；**调参是用户的手感活，已推迟** | spec 补一节，把"搭"与"调"分离——搭的部分解锁给弱模型 | 小 |
| 7 | 余族定理（cognates）推导 | Roberts 构造的数学推导 Fable-gated，但属 wow 内容 | **deadline 后**再做，IDEAS.md 挂着 | — |

## B. 弱模型就绪（现有/即将有的 spec 足够）

- Next.js 脚手架 + 内容池实现（等 SITE_SPEC）
- M3-减-阻尼：耦合曲线接线（buildTracePath 已测）+ 工程标注/题栏排版（§4.4 token 全在）
- renderStaticSVG 实现（等 §7 规格化）
- archive 结构、OG 图、about 页、部署
- 错误响亮、返工便宜、用户一眼能验收的一切排版/视觉工作

## C. 只有用户能做（模型无法代劳）

- **真机触屏手测**（M2 关闭的唯一余项）
- 阻尼手感拍板（Session 4 回归时）
- case study 与文书的声音；出镜视频（多校标配，需独立准备）
- **AI 披露策略**（Parsons/SVA 明文要求披露 AI 使用）：本项目的防御性恰恰在于"spec 先行 + 全部数学可脱稿复述 + git 历史完整"——如何表述由用户定，宜早不宜晚
- 2026 年 9–10 月逐校复核截止日期（调研文档的周期警告）

## 推荐执行顺序

```
① SITE_SPEC.md  →  ② 导出 spec（§7）  →  ③ SKILL.md
→（此后 M3-减-阻尼、脚手架均可交弱模型并行）
→ ④ case study 骨架  → ⑤ Peaucellier  → ⑥ 阻尼预案（随 Session 4 回归）
```

逻辑：① 解锁脚手架（时间最紧的外部死线）；②③ 解锁全部委托；④ 依赖 M3 出图后更顺但骨架可先行；⑤⑥ 补强。
