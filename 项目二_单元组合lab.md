# 项目二 · 单元组合 lab（Lab 2-11，2026-09-17 立项 · 2026-09-20 纠偏定向 + 三张图形）

> 立项：用户 2026-09-17 草图三张 +「研究不同形状的单元组合来形成不同的效果，比如通道型，共同构造的平台型，
> 密闭空间型」。**首版读错**（把「不同形状」读成目录五种形态换槽位，已撤）。
> 纠偏：用户 2026-09-20「假如我们就用平台，然后选择做一圈起伏……那是不是有存在前后两个单元可以实现首尾相接
> 一高一低的？」→ 线稿五种接法 → 用户「这个思路是对的」→ 交来三张图形：「以我们刚讨论的这个思路来把这三种单元
> 不同形态组合形成丰富的空间这件事在 lab 里搭建出来分别演示，并包括方单元版本的。验收标准就是这些形态都能够实现
> 并且运动过程流畅无抽搐和跳帧并能在控制台里切换形态」。
> 本文件 = 单元与旋钮 · 三张图形怎么读 · 接缝实测量 · 台架落点 · 边界。数字以 `src/lib/space/unit-combo.ts`
> 与其守门为准，这里不复制成第二份真理。

## 0. 一句话

同一种平台在「一圈起伏」下有高有低；几个单元挨着放，接缝处两家各是什么高度由**各自的旋钮**定，合起来读成坡、台阶、
凹、拱；捏分单元把缝口转向邻居就多出一格有顶的走面，两个缝口相对就是一个腔。两族（圆环 / 方环）各做一遍。

## 1. 单元与旋钮

| 族 | 起伏单元 | 捏分单元 | 起伏量程 | 走面基准（引擎实测） |
|---|---|---|---|---|
| 圆环 | Lab 2-5 圆筒环（202 节）+ 一圈起伏 | Lab 2-5 捏分编制（338 节，双平台 ↔ 整块镜像） | lead 58–14 = 88 px = **0.35 m** | 直挑台 77.2（蘑菇 82.3 · 方箱 101.5 · 袋无） |
| 方环 | Lab 2-6 方形环（305 节）+ 它的起伏 | Lab 2-6 一次循环（338 节，一条边双平台、对边整块） | lead 135–115 = 40 px = **0.16 m** | 箱顶 118.7（三档同高） |

- 起伏单元 `{kind:'wave', crest, lo, hi}`：`crest` = 峰落在哪条带（0 = 朝 +X、10 = 朝 −X）；`lo`/`hi` = 用量程的哪一段
  （0 = 谷底、1 = 峰顶，按族换算成 lead；lo = hi 就是平台）。走面 = 基准 + 2·(中心 lead − lead)，两族各真跑一遍
  lead 36 / 115 验「纯平移」（±0.5 px）。
- 捏分单元 `{kind:'split', face}`：缝口（双平台那一侧，pair 0）朝哪条带，对面是整块（pair 9）。缝口侧引擎实测：
  下板顶 78.4（圆）/ 78.5（方）· 上板底 177.8 / 177.9 ⇒ 净空 99.4 px = **0.39 m**；整块顶 144.9。
  方环的极点只能落在四条边上（`buildSquareSplitOrder` 加法式 `pole` 参数，默认 = SQSPLIT.POLE ⇒ Lab 2-6 逐位不变）。
- **量程是硬账**：一个组合里几个单元分着用，不叠加；更高只能加长带子。

## 2. 三张图形怎么读（模型读法，台架已按此搭）

| 图形 | 单元 | 接缝 | 读作 |
|---|---|---|---|
| ① 坡降 | 起伏（峰朝左，上半段）· 起伏（峰朝左，下半段）· 平（谷底） | 两条全齐平 | 从峰顶一路降到谷底，落在一个平台上 |
| ② 升台 | 捏分（缝口朝右）· 起伏（峰朝右，整段）· 平（峰顶） | 圆环：缝口下板 78.4 对坡谷 77.2，差 1.2 px；方环：**差 20 px = 8 cm**；坡顶对平台齐平 | 走进有顶的一格（净空 0.39 m），沿坡升到高台 |
| ③ 合腔 | 捏分（缝口朝右）· 捏分（缝口朝左） | 腔接通 | 两片下板成地、两片上板成顶，一个 0.39 m 高的腔 |

草图 ② 里坡是从缝口的**上**板起的；上板顶 194.7 px 比起伏能到的最高走面（圆 165.2 / 方 138.7）还高，接不上，
故台架从**下**板起坡（圆环齐平）。方环的起伏只有 40 px，下板也够不到，那道接缝如实标成 8 cm 的台阶。

另五种接法（台阶 / 续坡 / 三段坡 / 凹 / 拱）是 09-20 线稿里用户看过的，一并留作预设，两族通用。

## 3. 解什么、摆到哪

- 一种键谱一个 lead 一条引擎（方环按 (方位类, lead)），跨单元去重；捏分十条一族只解一次不论几个捏分单元。
  圆环：台阶 11 · 续坡 21 · ① 21 · ② 21 · ③ 10；方环：起伏按 **6 级**（每单元最多 6 个 lead × 3 方位类）· 捏分 10 条（338 节），
  ① 21 · ② 22 · ③ 10 · 三段坡 28。
- 站位一列沿 +X，相切 = 相邻半宽相加（圆：起伏 R + 101 = 131、捏分 R + 84.3 = 114.3；方：起伏最紧外缘 88.5、
  捏分 98.3），分离再加各族的环间缝；整列按外缘居中。
- 接缝织物网每条都搭，读**朝向对方那条带**的引擎（SkinSolidBench 本轮改法，同谱环逐位不变），落差大的接缝网即斜坡。
- 换族：相位偏移（方环 9° 半格）走 ref、半径量程变了就复位（SkinSolidBench 两处加法式）。房间固定 Lab 2-7 那一间。

## 4. 落地

- 模块 `src/lib/space/unit-combo.ts` + 守门 `unit-combo.test.ts` 22 项（两族走面基准与平移引擎实测 · 缝口下板 / 上板 /
  整块实测 · 整段 = 各族起伏逐位 · 相位落带 · 缝口朝向 · 三张图形 + 五种接法的接缝读数 · 引擎去重与编制表 ·
  站位 / 房间 / 取景 · `pole` 默认不变）。
- 线稿 `scripts/unit-combo/wave-draft.mjs`：两族各一张（八张图形的立面 + 平面 + 接缝读数），委托模块。
- 台架 `components/lab/SkinComboBench.tsx`：SkinSolidBench 薄壳。控制条第一层：图形 8 · 单元 2 · 形态 4（只管圆环起伏
  单元，方环或无起伏单元时变灰留位）· 距离 2；换距离只重摆。默认 ① 坡降 · 圆环 · 直挑台 · 相切。
- /lab 第七段 Ⅶ Compositions、主页第 16 卡、差分清单 290 → 418、`/lab#lab2-11-<图形>`。

## 5. 边界与待办

- 单元之间不相互作用（引擎单剖面）；起伏是余弦、峰谷各一处；接缝只能落在整数带上（18° 一格）。
- 图形都是一列；拐弯 / 围一圈要站位模型加二维（相邻 = 1 的排法），本轮没做。
- 方环 ② 的缝口接缝是 8 cm 台阶（量程所限）；要齐平得给方环更长的带子（Lab 2-6 加长的先例）。
- **流畅是量出来的**：方环一条带 305 节，首版起伏 11 级 + 捏分十条 = 43 条引擎，80 步/s 单步 30–35 ms、要 155–179% 一颗核
  （软件 GL 下就是抽搐 + 跳帧）。两处改：方环起伏收成 **6 级**（一列里各单元分着用量程，11 级肉眼读不出）；速率按总节点数
  自适应 `comboRate`——4500 节以内 80 步/s，超过按比例降、下限 30（圆环 ① 仍 80，圆环 ② 21 条 ≈64，方环 ① 21 条 12.5 ms/步 ·
  ② 22 条 15.7 · 三段坡 28 条 17.3 · ③ 10 条 7.9）。慢设备照旧靠每帧封顶 3 步只放慢不漂移。
- 验收时抓到的**冻结 bug 不在这台**：`useBenchLoop` 的 IntersectionObserver 只读第一条记录，一批里「先出后入」被读成「出」⇒ 所有 WebGL
  台架都可能在快速甩滚 / 视口临时改尺寸后钉死（CDP 打桩复现 records "01"）。已改读最后一条，LinkageFigure 同修。
- **捏分的跨腔橙键不画（2026-09-21，用户「取消上下两个平台之间跨平台的橙色链接键，只保留上下两个平台分别
  自己内部的链接键」）**：那把橙线是外箱梯挡 + 缝链，物理上正是撑开两片台的键，引擎照锁、HUD 键数照记；
  只是**画的时候按缝底分**——两端跨在缝底两侧的不画，各片台自己内部的（上下面角的同侧键，每条带两根）照画。
  标记 = `RingUnitDef.seam`（缝底节点绝对下标，只有开了缝的档才给；整块档 t=0 不给，梯挡就是这一块的内部键），
  台架 `SolidUnitDef.seam` 同名字段过滤（默认 undefined = 全画 ⇒ 别的台架逐位不变）。**四处捏分一起变**：
  Lab 2-4 序列 · 2-5 圆筒环 · 2-6 方形环 · 2-11 组合。守门 `skin-seam.test.ts`。
- 待用户真机拍板：默认图形 · 膜 0.35 · 取景 · 速率上限 80 / 节点预算 4500。
