# 项目二 · 单元组合 lab（Lab 2-11，2026-09-17 立项 · 2026-09-20 纠偏定向）

> 立项：用户 2026-09-17 草图三张 +「研究不同形状的单元组合来形成不同的效果，比如通道型，共同构造的平台型，
> 密闭空间型」。**首版读错**（把「不同形状」读成目录五种形态换槽位，已撤）。
> 纠偏：用户 2026-09-20「假如我们就用平台，然后选择做一圈起伏，你看是不是这种有高有低的平台，那是不是有存在
> 前后两个单元可以实现首尾相接一高一低的？」→ 线稿五种接法 → 用户「这个思路是对的，接下来给你几个图形，你来完成
> 这些图形的搭建工作」。
> 本文件 = 单元与两个旋钮 · 接缝怎么读 · 五种预设的实测量 · 台架落点 · 边界。数字以 `src/lib/space/unit-combo.ts`
> 与其守门为准，这里不复制成第二份真理。

## 0. 一句话

同一种平台（默认直挑台）在「一圈起伏」下有高有低；几个单元挨着放，接缝处两家各是什么高度由**各自的相位与高度段**定，
合起来读成台阶 / 坡 / 凹处 / 拱。这台是搭建这些图形的机制：用户的每张图形 = 几个单元 + 每个单元 (相位, 高度段) + 站位，
按预设追加。

## 1. 单元与两个旋钮

- 单元 = Lab 2-5 圆筒环（20 条 202 节带绕一根立杆，R 滑块 26–90 默认 30）+ 一圈起伏编制（同一张键谱、每条带一个 lead，
  余弦 11 级）。起伏是纯平移（skin-ring 守门「形状逐点不变」），走面高度 = 走面基准 + 2·(58 − lead)。
- **相位** `crest`：峰落在哪条带（0–19，方位 = crest × 18°，0 = +X）；谷在对面。
- **高度段** `low → high`：用量程 lead 58–14 的哪一段（整段 = Lab 2-5 那个环；半段 = 起伏减半）。
- 走面基准（lead 58，2D px，离下缘）：蘑菇 82.3 · 直挑台 77.2 · 方箱 101.5；袋没有走面。引擎实测、守门逐一核对，
  并真跑一遍 lead 36 验证「平移」：直挑台走面 = 77.2 + 2·22（±0.5 px）。
- **量程是硬账**：44 节 × 2 px = 88 px = **0.35 m**（带子 202 节钉死给的）。一个组合里几个单元分着用它，不叠加；
  要爬更高只能加长带子（方形环 305/338 的先例，筒会变高）。

## 2. 接缝怎么读

两个单元相切时，接缝落在 A 朝向 B 的那条带（`bandToward`）与 B 朝向 A 的那条带上；`comboJoints` 给每条接缝两家的走面高度
与落差，`comboReading` 由此写一行结论。齐平判据 3 cm（`JOINT_LEVEL_TOL` 7.6 px）。

| 预设 | 相位 / 高度段 | 接缝 | 读作 |
|---|---|---|---|
| 台阶 step | 两个同相（峰朝 +X），整段 | A 峰对 B 谷，落差 0.35 m | 走上 A 爬到顶、跨到 B 从底再爬——锯齿 |
| 续坡 ramp | 同相；A 58→36、B 36→14 | 齐平（同一个 lead 36） | 两个单元合起来爬完 0.35 m，各起伏 0.17 m |
| 三段坡 ramp3 | 同相；58→43→29→14 | 两条接缝全齐平 | 总爬升仍 0.35 m，每段 0.12 m，读作缓坡 |
| 凹 valley | A 峰朝 −X、B 峰朝 +X，整段 | 谷对谷，齐平且最低 | 两头高中间低，一个被高台围住的凹处 |
| 拱 arch | A 峰朝 +X、B 峰朝 −X，整段 | 峰对峰，齐平且最高 | 一道拱 |

直挑台默认下走面离地 1.39–1.73 m（下缘钉在 1.08 m）。

## 3. 解什么、摆到哪

- 用到的每个 lead 一条引擎（`comboBuild`）：台阶 11 条摆两处；续坡两段共用中间的 36 ⇒ 21 条。每个单元一份编制
  （二十条带各指自己的 lead），完全相同的单元共用一份。
- 站位以相切间距为单位（相邻 = 1，可排 2D），相切 = 2·(R + 挑出峰值)，分离再加 Lab 2-7 的环间缝；整组按包围盒居中。
- 接缝织物网：相切下每条接缝都搭；`SkinSolidBench` 本轮改为读**朝向对方的那条带**的引擎（起伏环每条带高度不同；
  一圈同谱时任一条都一样 ⇒ Lab 2-8 逐位不变），落差大的接缝网就是一道斜坡。
- 房间固定 Lab 2-7 那一间；取景按整组 + 一圈留距。

## 4. 落地

- 模块 `src/lib/space/unit-combo.ts`（纯几何）+ 守门 `unit-combo.test.ts` 17 项（走面基准与平移引擎实测 · 整段 = waveLeads
  逐位 · 相位落带 · 五种接缝读数 · 引擎去重与编制表 · 站位 / 房间 / 取景）。
- 线稿 `scripts/unit-combo/wave-draft.mjs`：委托模块画全部预设（立面高度带 + 真剖面 + 平面峰谷 + 接缝读数），
  用户的图形追加进 `COMBO_PLANS` 后跑一遍就是它的线稿。
- 台架 `components/lab/SkinComboBench.tsx`：SkinSolidBench 薄壳，Lab 2-8 同一条阵列路径。控制条第一层：接法（五预设）·
  形态（四）· 距离（相切 / 分离）；换距离只重摆。默认续坡 · 直挑台 · 相切。
- /lab 第七段 Ⅶ Compositions（Lab 2-11）、主页第 16 卡、差分清单 296 → 330（接法 5 × 形态 4 × 距离 2）、`/lab#lab2-11-<接法>`。

## 5. 边界与待办

- 单元之间不相互作用（引擎单剖面）。
- 起伏是余弦、峰谷各一处；接缝只能落在整数带上（18° 一格）。
- 站位模型支持 2D（相邻 = 1 的任意排法），预设目前只有一列；用户的图形到了按 `{positions, units}` 追加。
- 待用户真机拍板：默认接法 · 膜 0.35 · 取景。
