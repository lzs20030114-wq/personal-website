# 轮回机器_五环立体spec.md — 伏丘壳体 S1–S5 立体编排 v0.1（2026-07-17）

**立项**：用户 2026-07-17 拍板（原「五环族演示未列入范围」条款就此作废）：
把五个环按整体模型参考编排成立体版，**复用 3D 装备**。同轮拍板：**站距 85mm 等距**
（取自 求解器结构演示.3dm 拆件展示排，×4 段 = 340 ≈ 整机长 330；真机站位若另有
设计值，改 `gen_shell3d.py` 的 RINGS 表重生成即可）、**同相呼吸**（五曲柄同相位）。

**v0.2 对齐纠偏（2026-07-17 用户）**：「根本没有 roll，都是在同一平面上」——
五环全部**竖直站立、四脚共地 z=0**。首版把盘点 §7 的 roll 列（−14…+15°）误读为
装配侧倾（环面内旋转）致五条脚槽线各歪各的，已回退；该列真实语义待用户澄清，
**不用于装配**。`rollDeg` 字段保留（全 0），位姿变换代码不动。

台架：`npm run demo` → `/shell3d.html`（线上 `/demo/shell3d.html`）。
测试：`shell3d.test.ts` 7 项。

## 架构（三层纪律不破）

- **仿真 = 五个 2D 环实例**（`solver.ts` 内核零修改）：每环 = 刚性三角板 + 支撑
  节点 K4 + 曲柄→中央杆→拱顶 + 超长杆导轨/脚槽近似——S4 拱环同构，几何从
  `摊开骨架::S1…S5` 图层批量提取（`gen_shell3d.py` → `shell3d-data.ts`，勿手改）。
- **立体 = 刚体位姿嵌入**：世界系 X=体轴、Z=上；环平面 = 横截面 x=station；
  五环竖直站立、四脚共地（v0.2 纠偏后 rollDeg 恒 0，局部 (x,y) → 世界
  (station, x, y)）。
- **渲染 = gl3d/camera3d 复用零修改**：板件烘焙成薄棱柱（厚 3mm），剪式交叉板
  按朝向符号错层 ±2.5mm（防 z-fighting，兼真机双层板语义）；驱动链/轮圈/槽线
  = drawLines。trackball 旋转 + 右键平移 + 滚轮缩放 + 空闲自转，同 tentacle3d。

## 止程与定步（S4 2026-07-17 定案的全量继承）

- 每环四脚槽两端钳制（`FootSlot` + `ringStopPass` 8×6 交错）+ **定步积分
  `SHELL_STEP_DT = 1/120`**——任何设备走同一条逐步相同的轨迹。
- 槽端：外端 = 全开位（图纸装配位）；内端 = **细步半圈曲柄扫掠**（0.5°/步 ×
  48 遍交错外止程）的四脚最内到达位，页面/测试初始化时确定性实测（按环名缓存）。
  扫掠校验 apex 必须到达 apex₀ − 2R*，否则抛错拒绝上台。

## 关键机理发现（实测入档，2026-07-17）

1. **脚滑移与拱顶高度解耦**：外脚导引收拢实验里 apex 纹丝不动、残差 0——
   「四脚平移」本身就是零刚度自由模态，不是折叠的一部分。折叠传动只走
   曲柄→中央杆→拱顶→板链。
2. **步距决定分支**：S1 在 ≤0.5°/步 健康收拢（残差 ≤0.5mm、双向一致），
   1.5°/步 会在全开死点出口被踢进压平分支（apex 降、脚外挤、量出零长槽、
   运行时硬顶 6.7mm）。定步 1/120（0.38°/步）留了裕量——**改 ω 或步长前先
   重跑 shell3d 测试**。
3. **图纸 S3 与真机 S3 不一致（用户 2026-07-17 确认）**：图纸层的 S3（10 板/
   17 销）连接完好、数值自由度 = 1（雅可比零空间实测，与其余四环相同），但其
   唯一运动模态是「卷起、脚不动」（三种步密度残差全程 0.00 = 精确运动学）；
   用户确认真机 S3 与其他四环同款收拢——**图层内容是旧版或另一变体**。
   缩放代替不可行：S3 比例高瘦（高 157 / 脚 ±162），S4 均匀缩放无法同时匹配
   高与宽。**待用户补真 S3 图纸或指认来源后重提取**；在此之前 S3 以图纸版
   在台（会卷起）。脚距塌缩检查对它豁免。运行时瞬态峰值 S3 1.12mm（慢模态），
   其余四环 0.24–0.42mm。

## 实测回填（shell3d.test.ts 锁定）

| 项 | 实测 | 测试上界 |
|---|---|---|
| 五环装配残差 | <0.3mm | <0.3mm |
| 整圈运行时峰值残差（1/120 定步） | S3 1.12mm，其余 ≤0.42mm | <1.5mm |
| 四脚在槽内 | 全程 | 抽帧断言 |
| 定步切分不变性 | 逐位相同 | toEqual |
| 同相 | 五环 θ 恒等 | 精确断言 |

## 边界与遗留

- **站距 85mm 是推断值**（展示排间距），真机设计值出来后改 RINGS 表重生成。
- **S3 待换真图纸**（见机理发现 3）；盘点 §7 roll 列语义待用户澄清。
- ω=0.8 与手感参数留用户调；行波呼吸（错相）当前不做，加参数即可扩展。
- 蒙皮（织物跨环）、附肢挂载、3D 拾取拖拽：未列入，动前先议。
